<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://uv-xiao.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://uv-xiao.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-03T16:35:01+00:00</updated><id>https://uv-xiao.github.io/feed.xml</id><title type="html">Youwei Xiao</title><subtitle>Ph.D. candidate at Peking University. Research on compilers, DSLs, and hardware-software co-design for MLSys and EDA. </subtitle><entry><title type="html">Agent Harness, Part 1: Workflow Mechanisms in Humanize h1 and Codex</title><link href="https://uv-xiao.github.io/blog/2026/agent-harness-workflows/" rel="alternate" type="text/html" title="Agent Harness, Part 1: Workflow Mechanisms in Humanize h1 and Codex"/><published>2026-05-26T00:00:00+00:00</published><updated>2026-05-26T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2026/agent-harness-workflows</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2026/agent-harness-workflows/"><![CDATA[<p>This is the first post in a planned series about agent harnesses. The series is broader than workflow, but workflow is the right first mechanism to study because it is where long-running agent work becomes visible as state, gates, handoffs, and evidence.</p> <p>The question is practical:</p> <blockquote> <p>If a coding agent can already edit files, run commands, and respond to feedback, what does an agent harness add?</p> </blockquote> <p>The short answer is that a harness turns a one-turn assistant into a controlled work system. It gives the agent a durable objective, scoped tools, evidence requirements, review boundaries, lifecycle events, and recovery behavior when the work drifts.</p> <p>This post studies two concrete systems:</p> <ol> <li><strong>Humanize h1 RLCR</strong>: a workflow-oriented harness that wraps a builder agent with plans, round contracts, stop hooks, reviewer pressure, memory deltas, drift recovery, and methodology analysis.</li> <li><strong>Codex review and goals</strong>: two narrower but very clean mechanisms: review as a bounded service, and goals as a persistent thread-level objective contract with runtime policy.</li> </ol> <p>The goal of the post is not to praise either system. It is to make their mechanisms reproducible. If you had to rebuild the useful parts from scratch, this should tell you what objects, state files, prompts, gates, and event transitions you would need.</p> <p>The final section then asks why an outside framework such as Puppeteer is still useful. That discussion is intentionally kept separate from the case studies: first we understand the systems as they are; only then do we extract design pressure for Puppeteer.</p> <h2 id="sources">Sources</h2> <p>This post is based on two in-progress mechanism studies in the Puppeteer repo:</p> <ul> <li><code class="language-plaintext highlighter-rouge">docs/in_progress/design/case_study_humanize_h1_workflow.md</code></li> <li><code class="language-plaintext highlighter-rouge">docs/in_progress/design/case_study_codex_review_goal_workflows.md</code></li> </ul> <p>Those studies inspect:</p> <ul> <li>Humanize h1 source at <code class="language-plaintext highlighter-rouge">tmp/repos/humanize</code></li> <li>Humanize workflow presentation at <code class="language-plaintext highlighter-rouge">tmp/articles/humanize-workflow.pdf</code></li> <li>Codex source at <code class="language-plaintext highlighter-rouge">tmp/repos/codex</code></li> <li>OpenAI Cookbook notebook: <code class="language-plaintext highlighter-rouge">tmp/articles/using_goals_in_codex.ipynb</code></li> </ul> <p>The explanations below are self-contained. They paraphrase and simplify the source mechanisms so the reader does not need to open those files to understand the architecture.</p> <h2 id="a-working-definition-of-harness">A Working Definition Of Harness</h2> <p>An <strong>agent harness</strong> is not just a prompt template.</p> <p>It is the system around the agent that answers questions like:</p> <ul> <li>What is the durable objective?</li> <li>What evidence proves the objective is done?</li> <li>What tools and files may the agent touch?</li> <li>What must happen before the agent is allowed to stop?</li> <li>What should be reviewed by another model or by deterministic checks?</li> <li>What happens when the agent drifts from the original goal?</li> <li>How are state, traces, summaries, failures, and lessons preserved?</li> </ul> <p>In workflow language, a harness looks like steps. In implementation, the useful pieces are more varied:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Objective state
  durable goal, plan, acceptance criteria, budget, lifecycle status

Evidence surfaces
  tests, benchmarks, diffs, summaries, artifacts, claim ledgers

Agent adapters
  builder, reviewer, analysis agent, sub-agent, one-shot service

Gates
  deterministic checks before model judgement

Review services
  scoped model judgement with narrower capabilities

Runtime policy
  continuation, budget handling, recovery, stop behavior

Trace
  events, summaries, review output, state transitions, methodology notes
</code></pre></div></div> <p>That distinction matters because “workflow” is not always the right root abstraction. Sometimes the harness really is a workflow. Sometimes it is a service call. Sometimes it is persisted state plus event-driven runtime logic.</p> <h2 id="case-study-1-humanize-h1-rlcr">Case Study 1: Humanize h1 RLCR</h2> <p>Humanize h1 is easiest to understand as a judgement loop.</p> <p>It is not “many agents doing work.” It is one builder under pressure from a plan, a state directory, stop hooks, a reviewer, memory discipline, and final methodology analysis.</p> <p>The loop’s core idea is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>human plan
  -&gt; setup durable loop state
  -&gt; builder works one round
  -&gt; deterministic stop hook checks cheap invariants
  -&gt; Codex reviews the builder's summary
  -&gt; if incomplete, next round prompt narrows the work
  -&gt; if complete, Codex reviews the final diff
  -&gt; if clean, methodology analysis critiques the loop itself
</code></pre></div></div> <h3 id="humanize-reproduction-model">Humanize Reproduction Model</h3> <p>To reproduce the useful part of Humanize h1, implement these objects:</p> <table> <thead> <tr> <th>Object</th> <th>Purpose</th> <th>Durable Location</th> </tr> </thead> <tbody> <tr> <td>Plan</td> <td>Original work scope and acceptance anchor.</td> <td>User-provided Markdown plan.</td> </tr> <tr> <td>State file</td> <td>Round, budget, base commit, model config, drift counters.</td> <td><code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/state.md</code></td> </tr> <tr> <td>Goal tracker</td> <td>Immutable goal plus mutable work board.</td> <td><code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/goal-tracker.md</code></td> </tr> <tr> <td>Round contract</td> <td>One local mainline objective per round.</td> <td><code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/round-N-contract.md</code></td> </tr> <tr> <td>Round summary</td> <td>Builder’s evidence packet for the reviewer.</td> <td><code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/round-N-summary.md</code></td> </tr> <tr> <td>Review result</td> <td>Codex summary review output.</td> <td><code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/round-N-review-result.md</code></td> </tr> <tr> <td>Review phase marker</td> <td>Marks transition from implementation to code review.</td> <td><code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/.review-phase-started</code></td> </tr> <tr> <td>Methodology report</td> <td>Post-run critique of the harness method.</td> <td><code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/methodology-analysis-report.md</code></td> </tr> </tbody> </table> <p>The important design choice is that these are not chat memories. They are files that hooks and agents can inspect. A new turn can recover the loop state by reading the directory.</p> <h3 id="humanize-control-flow">Humanize Control Flow</h3> <pre><code class="language-mermaid">flowchart TD
  Plan[Human plan] --&gt; Precheck[Plan compliance check]
  Precheck --&gt; Quiz[Optional understanding quiz]
  Quiz --&gt; Setup[setup-rlcr-loop.sh]
  Setup --&gt; Tracker[goal-tracker.md]
  Setup --&gt; State[state.md]
  Setup --&gt; Contract[round-0-contract.md]
  Contract --&gt; Builder[Builder agent]
  Builder --&gt; Summary[round-N-summary.md]
  Builder --&gt; Stop[Stop attempt]
  Stop --&gt; Hook[Stop hook]

  Hook --&gt; Cheap{Cheap gates pass?}
  Cheap -- no --&gt; Block[Block with specific instruction]
  Block --&gt; Builder
  Cheap -- yes --&gt; Review[Codex summary review]

  Review -- findings --&gt; NextRound[next-round prompt]
  NextRound --&gt; Contract
  Review -- COMPLETE --&gt; DiffReview[codex review --base]
  DiffReview -- priority findings --&gt; ReviewFix[review-fix prompt]
  ReviewFix --&gt; Builder
  DiffReview -- clean --&gt; Methodology[methodology analysis]
  Methodology --&gt; Terminal[complete / stop / maxiter state]
</code></pre> <p>The loop uses two different reviewers:</p> <ol> <li><strong>Implementation review</strong> checks whether the builder’s claimed progress proves the original plan is done.</li> <li><strong>Diff review</strong> checks whether the actual final patch has blocking code review findings.</li> </ol> <p>Those are different questions. The first is about plan completion. The second is about patch correctness.</p> <h3 id="start-plan-pre-check-and-quiz">Start: Plan Pre-Check And Quiz</h3> <p>Humanize does not start from raw user text.</p> <p>The command first tries to read the plan and ask a plan-compliance agent:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>plan compliance checker:
  inspect repository structure
  check whether plan belongs to this repo
  reject branch-switching instructions
  return exactly:
    PASS
    FAIL_RELEVANCE
    FAIL_BRANCH_SWITCH
</code></pre></div></div> <p>If the output is malformed, the command fails closed.</p> <p>Then, unless the user opts out, it asks a quiz agent to produce two multiple choice questions and a plan summary:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>plan understanding quiz:
  QUESTION_1: mechanism question
  QUESTION_2: architecture question
  PLAN_SUMMARY

if user misses questions:
  show summary and correct answers
  ask whether to proceed or stop
</code></pre></div></div> <p>This quiz is advisory friction. It does not prove the plan is good. It catches the common case where a user accepts a generated plan without understanding what work is about to be delegated.</p> <h3 id="setup-state-becomes-inspectable">Setup: State Becomes Inspectable</h3> <p>Setup creates <code class="language-plaintext highlighter-rouge">.humanize/rlcr/&lt;timestamp&gt;/state.md</code>.</p> <p>The essential state shape is:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">current_round</span><span class="pi">:</span> <span class="m">0</span>
<span class="na">max_iterations</span><span class="pi">:</span> <span class="m">42</span>
<span class="na">codex_model</span><span class="pi">:</span> <span class="s">gpt-5.5</span>
<span class="na">codex_effort</span><span class="pi">:</span> <span class="s">high</span>
<span class="na">codex_timeout</span><span class="pi">:</span> <span class="m">5400</span>
<span class="na">base_branch</span><span class="pi">:</span> <span class="s">main</span>
<span class="na">base_commit</span><span class="pi">:</span> <span class="s">&lt;fixed merge-base or branch commit&gt;</span>
<span class="na">review_started</span><span class="pi">:</span> <span class="kc">false</span>
<span class="na">agent_teams</span><span class="pi">:</span> <span class="kc">false</span>
<span class="na">privacy_mode</span><span class="pi">:</span> <span class="kc">false</span>
<span class="na">bitlesson_required</span><span class="pi">:</span> <span class="kc">false</span>
<span class="na">mainline_stall_count</span><span class="pi">:</span> <span class="m">0</span>
<span class="na">last_mainline_verdict</span><span class="pi">:</span> <span class="s">unknown</span>
<span class="na">drift_status</span><span class="pi">:</span> <span class="s">normal</span>
</code></pre></div></div> <p>The state file is the loop’s source of truth. It lets the stop hook know which round is active, whether review phase has started, which base commit should be used for final diff review, and whether the loop is drifting.</p> <h3 id="goal-tracker-immutable-goal-mutable-board">Goal Tracker: Immutable Goal, Mutable Board</h3> <p>Humanize’s goal tracker separates the part that should not drift from the part that must evolve as the agent learns.</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># Goal Tracker</span>

<span class="gu">## IMMUTABLE SECTION</span>

<span class="gu">### Ultimate Goal</span>
<span class="nt">&lt;copied</span> <span class="na">or</span> <span class="na">extracted</span> <span class="na">from</span> <span class="na">plan</span><span class="nt">&gt;</span>

<span class="gu">### Acceptance Criteria</span>
<span class="p">-</span> AC-1: <span class="nt">&lt;observable</span> <span class="na">condition</span><span class="nt">&gt;</span>
<span class="p">-</span> AC-2: <span class="nt">&lt;observable</span> <span class="na">condition</span><span class="nt">&gt;</span>
<span class="p">
---
</span>
<span class="gu">## MUTABLE SECTION</span>

<span class="gu">### Plan Evolution Log</span>
| Round | Change | Reason | Impact on AC |

<span class="gu">### Active Tasks</span>
| Task | Target AC | Status | Tag | Owner | Notes |

<span class="gu">### Blocking Side Issues</span>
| Issue | Discovered Round | Blocking AC | Resolution Path |

<span class="gu">### Queued Side Issues</span>
| Issue | Discovered Round | Why Not Blocking | Revisit Trigger |

<span class="gu">### Completed and Verified</span>
| AC | Task | Completed Round | Verified Round | Evidence |

<span class="gu">### Explicitly Deferred</span>
| Task | Original AC | Deferred Since | Justification |
</code></pre></div></div> <p>This is one of the most important Humanize ideas. It prevents local findings from silently replacing the original plan. New issues are not ignored, but they must be classified:</p> <ul> <li><code class="language-plaintext highlighter-rouge">mainline</code>: plan-derived work that directly advances the current objective.</li> <li><code class="language-plaintext highlighter-rouge">blocking</code>: side issue that prevents current mainline success.</li> <li><code class="language-plaintext highlighter-rouge">queued</code>: valid follow-up that must not take over the round.</li> </ul> <h3 id="round-contract-one-round-one-mainline-objective">Round Contract: One Round, One Mainline Objective</h3> <p>Each round must write a round contract before coding:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># Round N Contract</span>
<span class="p">
-</span> Mainline Objective: <span class="nt">&lt;one</span> <span class="na">objective</span> <span class="na">for</span> <span class="na">this</span> <span class="na">round</span><span class="nt">&gt;</span>
<span class="p">-</span> Target ACs: <span class="nt">&lt;acceptance</span> <span class="na">criteria</span> <span class="na">this</span> <span class="na">round</span> <span class="na">advances</span><span class="nt">&gt;</span>
<span class="p">-</span> Blocking Side Issues In Scope: <span class="nt">&lt;must</span> <span class="na">fix</span> <span class="na">now</span><span class="nt">&gt;</span>
<span class="p">-</span> Queued Side Issues Out of Scope: <span class="nt">&lt;valid</span> <span class="na">but</span> <span class="na">not</span> <span class="na">steering</span><span class="nt">&gt;</span>
<span class="p">-</span> Success Criteria: <span class="nt">&lt;evidence</span> <span class="na">needed</span> <span class="na">before</span> <span class="na">stop</span><span class="nt">&gt;</span>
</code></pre></div></div> <p>This contract is the local steering wheel. Review findings can be severe, but they do not automatically become the new project. The next round must decide which findings are truly blocking the mainline objective.</p> <h3 id="summary-interface-what-crosses-the-inner-review-boundary">Summary Interface: What Crosses The Inner Review Boundary</h3> <p>The builder writes a round summary:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gu">## What Was Implemented</span>
<span class="p">-</span> AC-1: done, evidence: <span class="nt">&lt;test</span> <span class="na">or</span> <span class="na">artifact</span><span class="nt">&gt;</span>

<span class="gu">## Files Changed</span>
<span class="p">-</span> <span class="nt">&lt;path&gt;</span>: <span class="nt">&lt;why</span> <span class="na">it</span> <span class="na">changed</span><span class="nt">&gt;</span>

<span class="gu">## Validation</span>
<span class="p">-</span> <span class="nt">&lt;command&gt;</span>: pass/fail/not run, reason

<span class="gu">## Remaining Items</span>
<span class="p">-</span> [mainline] <span class="nt">&lt;still</span> <span class="na">required</span><span class="nt">&gt;</span>
<span class="p">-</span> [queued] <span class="nt">&lt;follow-up</span><span class="err">,</span> <span class="na">not</span> <span class="na">blocking</span><span class="nt">&gt;</span>

<span class="gu">## BitLesson Delta</span>
<span class="p">-</span> Action: none|add|update
<span class="p">-</span> Lesson ID(s): <span class="nt">&lt;IDs</span> <span class="na">or</span> <span class="na">NONE</span><span class="nt">&gt;</span>
<span class="p">-</span> Notes: <span class="nt">&lt;what</span> <span class="na">changed</span> <span class="na">and</span> <span class="na">why</span><span class="nt">&gt;</span>
</code></pre></div></div> <p>The summary is not trusted blindly. It is just the packet that crosses the inner review boundary. The hook checks that it exists. Codex reviews it against the plan, goal tracker, and recent history.</p> <h3 id="stop-hook-cheap-gates-before-model-judgement">Stop Hook: Cheap Gates Before Model Judgement</h3> <p>The stop hook is the loop’s judge of whether the builder is allowed to exit the current round.</p> <p>Simplified:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>on_stop:
  parse state.md
  reject malformed state
  block branch switch
  block plan mutation during implementation
  block incomplete mainline or blocking tasks
  block missing summary
  block missing round contract
  block uninitialized goal tracker
  block invalid BitLesson Delta
  block max-iteration overflow
  then call Codex summary review
</code></pre></div></div> <p>This is the cheap critique layer. Deterministic checks remove mechanical uncertainty before an expensive model reviewer spends judgement.</p> <h3 id="codex-summary-review-mainline-verdict">Codex Summary Review: Mainline Verdict</h3> <p>The implementation reviewer must classify findings into lanes and provide a mainline progress verdict:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gu">### Mainline Gaps</span>
<span class="p">-</span> <span class="nt">&lt;plan-derived</span> <span class="na">work</span> <span class="na">or</span> <span class="na">AC</span> <span class="na">progress</span> <span class="na">missing</span><span class="nt">&gt;</span>

<span class="gu">### Blocking Side Issues</span>
<span class="p">-</span> <span class="nt">&lt;bug</span> <span class="na">that</span> <span class="na">blocks</span> <span class="na">current</span> <span class="na">mainline</span> <span class="na">objective</span><span class="nt">&gt;</span>

<span class="gu">### Queued Side Issues</span>
<span class="p">-</span> <span class="nt">&lt;valid</span> <span class="na">follow-up</span> <span class="na">that</span> <span class="na">should</span> <span class="na">not</span> <span class="na">take</span> <span class="na">over</span><span class="nt">&gt;</span>

Mainline Progress Verdict: ADVANCED / STALLED / REGRESSED

<span class="nt">&lt;terminal</span> <span class="na">line</span><span class="nt">&gt;</span>
COMPLETE
or
STOP
or
<span class="nt">&lt;findings</span> <span class="na">to</span> <span class="na">fix</span><span class="nt">&gt;</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">Mainline Progress Verdict</code> is crucial. A round can fix real bugs and still fail to advance the plan. Humanize tracks that difference.</p> <h3 id="drift-recovery">Drift Recovery</h3> <p>If Codex says the mainline stalled or regressed, the hook increments a drift counter:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if verdict == ADVANCED:
  mainline_stall_count = 0

if verdict in {STALLED, REGRESSED}:
  mainline_stall_count += 1

if mainline_stall_count == 2:
  require drift recovery prompt

if mainline_stall_count &gt;= 3:
  stop with circuit breaker
</code></pre></div></div> <p>The drift recovery prompt asks the builder to re-read the original plan, update the goal tracker, and write a recovered round contract with exactly one mainline objective.</p> <p>This is how Humanize prevents a side quest from becoming the project.</p> <h3 id="final-diff-review">Final Diff Review</h3> <p>When summary review says <code class="language-plaintext highlighter-rouge">COMPLETE</code>, Humanize does not finish immediately.</p> <p>It moves into review phase:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>review_base = base_commit captured at setup
result = codex review --base review_base

if priority findings exist:
  create review-fix prompt
  keep original plan stable
else:
  enter finalize and methodology analysis
</code></pre></div></div> <p>The fixed base commit matters. Without it, the comparison target can move while the loop runs, making review ambiguous.</p> <h3 id="bitlesson-memory-with-selection-and-delta-validation">BitLesson: Memory With Selection And Delta Validation</h3> <p>BitLesson is Humanize’s memory discipline.</p> <p>Before a task, a selector reads the task, related paths, and the current BitLesson file:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>selector input:
  sub-task description
  related file paths
  current .humanize/bitlesson.md

selector output:
  LESSON_IDS: &lt;comma-separated IDs or NONE&gt;
  RATIONALE: &lt;one concise sentence&gt;
</code></pre></div></div> <p>After the round, the summary must include <code class="language-plaintext highlighter-rouge">## BitLesson Delta</code>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Action: none|add|update
Lesson ID(s): &lt;IDs or NONE&gt;
Notes: &lt;what changed and why&gt;
</code></pre></div></div> <p>The delta validator checks:</p> <ul> <li>The section exists.</li> <li>The action is one of <code class="language-plaintext highlighter-rouge">none</code>, <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">update</code>.</li> <li><code class="language-plaintext highlighter-rouge">none</code> has no concrete IDs.</li> <li><code class="language-plaintext highlighter-rouge">add</code> or <code class="language-plaintext highlighter-rouge">update</code> has concrete IDs.</li> <li>IDs use the expected format.</li> <li>IDs exist in the BitLesson file when claimed.</li> <li>Notes are present and not placeholder text.</li> </ul> <p>The lesson is that memory should be selected and accountable. The agent should not silently ignore reusable knowledge, and it should not silently claim it updated knowledge without evidence.</p> <h3 id="methodology-analysis-the-harness-critiques-itself">Methodology Analysis: The Harness Critiques Itself</h3> <p>After successful completion, Humanize can enter methodology analysis:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>enter_methodology_analysis(exit_reason):
  if privacy mode:
    skip
  rename state.md -&gt; methodology-analysis-state.md
  write .methodology-exit-reason
  ask analysis agent to inspect round summaries and review results
  require methodology-analysis-report.md
  require methodology-analysis-done.md
  optionally ask human to file sanitized issue
  rename methodology-analysis-state.md -&gt; complete/stop/maxiter-state.md
</code></pre></div></div> <p>This turns run traces into method improvement. The analysis is supposed to criticize the harness, not the project.</p> <h3 id="humanize-h1-mechanism-deck">Humanize h1 Mechanism Deck</h3> <section class="hcards" aria-label="Humanize h1 mechanism deck"> <div class="hcards__title">Humanize h1 mechanism deck</div> <div class="hcards__rail" tabindex="0"> <article id="h1-01" class="hcard"> <h4 class="hcard__title">Plan pre-check and quiz</h4> <div class="hcard__body"> <p>Start from a plan, not raw intent. Check repository relevance and branch-safety first. Then optionally quiz the human on the plan to catch delegated work the human did not understand.</p> </div> </article> <article id="h1-02" class="hcard"> <h4 class="hcard__title">Inspectable loop state</h4> <div class="hcard__body"> <p>Write round number, budget, model config, base commit, review phase, privacy mode, BitLesson mode, and drift counters into a state file. Hooks read state from disk instead of trusting chat memory.</p> </div> </article> <article id="h1-03" class="hcard"> <h4 class="hcard__title">Goal tracker</h4> <div class="hcard__body"> <p>Split immutable goal and acceptance criteria from mutable task state. This creates a stable plan anchor while still allowing the board to evolve with findings.</p> </div> </article> <article id="h1-04" class="hcard"> <h4 class="hcard__title">Round contract</h4> <div class="hcard__body"> <p>Require one mainline objective per round. Classify side issues as blocking or queued so reviewer findings cannot silently become the new project.</p> </div> </article> <article id="h1-05" class="hcard"> <h4 class="hcard__title">Summary packet</h4> <div class="hcard__body"> <p>Make the builder write what changed, why it changed, how it was validated, and what remains. The reviewer judges this packet against the plan and tracker.</p> </div> </article> <article id="h1-06" class="hcard"> <h4 class="hcard__title">Cheap stop gates</h4> <div class="hcard__body"> <p>Before model review, block missing summaries, missing contracts, invalid memory deltas, uninitialized trackers, branch switches, and max-iteration overflow.</p> </div> </article> <article id="h1-07" class="hcard"> <h4 class="hcard__title">Mainline verdict</h4> <div class="hcard__body"> <p>Require the reviewer to say whether the original plan advanced, stalled, or regressed. Local bug fixing is not automatically plan progress.</p> </div> </article> <article id="h1-08" class="hcard"> <h4 class="hcard__title">Drift recovery</h4> <div class="hcard__body"> <p>After repeated stalled or regressed rounds, force a recovery contract. After more repeated drift, stop instead of letting the loop consume unbounded work.</p> </div> </article> <article id="h1-09" class="hcard"> <h4 class="hcard__title">Diff review</h4> <div class="hcard__body"> <p>After summary review says complete, run a final patch review against the fixed base commit. Plan completion and patch correctness are separate questions.</p> </div> </article> <article id="h1-10" class="hcard"> <h4 class="hcard__title">Methodology analysis</h4> <div class="hcard__body"> <p>At the end, inspect the loop records from a method-improvement perspective. The harness becomes an object that can be criticized and improved.</p> </div> </article> </div> </section> <h3 id="humanize-setup-parameters">Humanize Setup Parameters</h3> <p>Humanize exposes several decisions that matter to reproduction:</p> <table> <thead> <tr> <th>Parameter</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">--max</code></td> <td>Maximum implementation rounds before termination.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--codex-model MODEL:EFFORT</code></td> <td>Reviewer model and reasoning effort.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--codex-timeout</code></td> <td>Review command timeout.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--full-review-round</code></td> <td>Cadence for deeper full-alignment review.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--base-branch</code> and captured <code class="language-plaintext highlighter-rouge">base_commit</code></td> <td>Stable diff-review target.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--track-plan-file</code></td> <td>Blocks silent mutation of the source plan.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--skip-quiz</code>, <code class="language-plaintext highlighter-rouge">--yolo</code></td> <td>Human can waive advisory plan-friction.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--agent-teams</code></td> <td>Optional thin parallelism wrapper.</td> </tr> <tr> <td>BitLesson strictness</td> <td>Controls whether empty memory can keep reporting no lesson.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">--privacy</code></td> <td>Skips methodology-sharing path.</td> </tr> </tbody> </table> <p>These parameters are not merely CLI details. They are the knobs that decide how strict, autonomous, private, and expensive the harness is.</p> <h2 id="case-study-2-codex-review-and-goals">Case Study 2: Codex Review And Goals</h2> <p>Codex offers two mechanisms that are narrower than Humanize but architecturally clean.</p> <ol> <li><strong>Review</strong> is a bounded service: choose a target, run a narrowed reviewer, parse output, record lifecycle events, exit.</li> <li><strong>Goal</strong> is a persistent contract: store an objective, track lifecycle and usage, continue only at safe boundaries, and restrict what status changes the model may make.</li> </ol> <p>The lesson is that not every harness feature should be modeled as a workflow graph. Review is naturally a service. Goal is naturally persisted state plus runtime policy.</p> <h3 id="codex-review-reproduction-model">Codex Review: Reproduction Model</h3> <p>To reproduce Codex review, implement:</p> <table> <thead> <tr> <th>Object</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td>Review target</td> <td>Defines what evidence the reviewer inspects.</td> </tr> <tr> <td>Review request</td> <td>Converts target into a prompt and diff context.</td> </tr> <tr> <td>Narrow reviewer config</td> <td>Disables builder-like powers.</td> </tr> <tr> <td>Review output parser</td> <td>Parses typed findings, falls back to text.</td> </tr> <tr> <td>Lifecycle events</td> <td>Entered review, exited review, assistant message.</td> </tr> <tr> <td>Inline/detached delivery</td> <td>Run review in parent thread or forked thread.</td> </tr> </tbody> </table> <p>The review prompt makes the role narrow:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>You are reviewing changes for bugs.
Report only findings the author would fix.
Prioritize correctness, regressions, security, data loss, and spec violations.

Output JSON:
  findings: [
    title,
    body,
    confidence_score,
    priority,
    code_location
  ]
  overall_correctness
  overall_explanation
  overall_confidence_score
</code></pre></div></div> <p>The target contract is explicit:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ReviewTarget:
  uncommittedChanges
  baseBranch(branch)
  commit(sha, optional_title)
  custom(instructions)

Validation:
  branch cannot be empty
  commit sha cannot be empty
  custom instructions cannot be empty
</code></pre></div></div> <p>Base-branch review tries to compute a merge base and review the diff from that base. If a merge base is unavailable, it falls back to a branch-diff prompt.</p> <h3 id="codex-review-narrowed-sub-agent">Codex Review: Narrowed Sub-Agent</h3> <p>Review runs as a one-shot sub-agent with reduced authority:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>start_review_conversation(request):
  sub_config = copy(parent_config)
  sub_config.web_search = disabled
  sub_config.spawn = disabled
  sub_config.collaboration = disabled
  sub_config.multi_agent = disabled
  sub_config.approval_policy = never
  sub_config.model = review_model if configured else current_model
  sub_config.base_instructions = REVIEW_PROMPT

  run_one_shot_sub_agent(source = Review, prompt = request)
</code></pre></div></div> <p>This is important. The reviewer is not a normal implementation agent wearing a different prompt. The configuration narrows what it can do.</p> <h3 id="codex-review-output-handling">Codex Review: Output Handling</h3> <p>Codex tries to preserve structure without failing the whole review when the model misses the schema:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>process_review_events:
  suppress streaming assistant deltas
  wait for final assistant message
  parse JSON review output
  else parse first JSON object found in text
  else treat text as overall_explanation

exit_review_mode:
  record user_action message containing review output
  emit ExitedReviewMode event
  record assistant message with human-readable review
  materialize rollout
</code></pre></div></div> <p>The fallback is pragmatic. It preserves review text, but the trace should know whether the output was structured JSON or plain text. Those are different evidence quality levels.</p> <h3 id="codex-review-inline-and-detached-delivery">Codex Review: Inline And Detached Delivery</h3> <p>The API supports two delivery modes:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>review/start(thread_id, target, delivery = inline):
  validate target
  build display text and ReviewRequest

  if delivery == inline:
    submit Op::Review to existing thread
    review_thread_id = thread_id

  if delivery == detached:
    materialize parent rollout
    load parent history
    fork new review thread
    optionally override model with review_model
    submit Op::Review to child thread
    review_thread_id = child_thread_id
</code></pre></div></div> <p>Inline review shares the parent thread. Detached review isolates the reviewer in a fork. That is a clean pattern for any harness: some checks should be part of the main conversation, while others should run in a separate review lane.</p> <h3 id="codex-goal-reproduction-model">Codex Goal: Reproduction Model</h3> <p>A Codex goal is thread-scoped durable state:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"threadId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"thr_123"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"objective"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Reduce p95 latency below 120ms while tests stay green"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"status"</span><span class="p">:</span><span class="w"> </span><span class="s2">"active"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"tokenBudget"</span><span class="p">:</span><span class="w"> </span><span class="mi">200000</span><span class="p">,</span><span class="w">
  </span><span class="nl">"tokensUsed"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w">
  </span><span class="nl">"timeUsedSeconds"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w">
  </span><span class="nl">"createdAt"</span><span class="p">:</span><span class="w"> </span><span class="mi">1776272400</span><span class="p">,</span><span class="w">
  </span><span class="nl">"updatedAt"</span><span class="p">:</span><span class="w"> </span><span class="mi">1776272400</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>External clients can manage it:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>thread/goal/set:
  create or update current goal
  may set objective, status, token budget
  emits thread/goal/updated

thread/goal/get:
  return current goal or null

thread/goal/clear:
  delete current goal
  emit thread/goal/cleared when state changes
</code></pre></div></div> <p>The model sees a narrower tool surface:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>get_goal():
  read objective, status, budget, token usage, time usage

create_goal(objective, token_budget?):
  only when explicitly requested
  fails if a goal already exists

update_goal(status):
  status must be complete or blocked
  cannot pause, resume, budget-limit, or usage-limit
</code></pre></div></div> <p>This separates authority:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>model:
  complete
  blocked

user/API:
  set
  pause
  resume
  clear

system/runtime:
  budget_limited
  usage_limited
  accounting
  continuation
</code></pre></div></div> <h3 id="goal-authoring-what-the-cookbook-adds">Goal Authoring: What The Cookbook Adds</h3> <p>The Codex source explains goal state and runtime. The OpenAI Cookbook explains how a human should write a goal that is worth running.</p> <p>A normal prompt says:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>do this next thing
</code></pre></div></div> <p>A goal says:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>keep working until this outcome is true
</code></pre></div></div> <p>Strong goals usually include:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>outcome:
  what should be true when done

verification surface:
  test, benchmark, report, command output, artifact, or source material

constraints:
  what must not regress

boundaries:
  files, tools, data, repositories, or resources Codex may use

iteration policy:
  how to choose the next attempt after each result

blocked stop condition:
  when no defensible path remains and what input would unlock progress
</code></pre></div></div> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Weak:
  /goal Improve performance

Strong:
  /goal Reduce p95 checkout latency below 120 ms on the checkout benchmark
        while keeping the correctness test suite green.
</code></pre></div></div> <p>The stronger version gives Codex an outcome, evidence surface, and constraint. If latency improves from 180 ms to 135 ms, the goal is not done. If latency drops below 120 ms but correctness tests fail, the goal is not done.</p> <p>The Cookbook’s research example is even more important. “Reproduce the paper” is too vague. A stronger research goal asks for:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>claim inventory
evidence mapping
feasible local reproductions
approximate support where exact replay is impossible
blocked claims
remaining uncertainty
final report that preserves those distinctions
</code></pre></div></div> <p>This is the right pattern for agentic research and optimization. The agent can continue through ambiguity without flattening uncertainty into a false success.</p> <h3 id="codex-goal-continuation">Codex Goal Continuation</h3> <p>When a goal remains active, Codex injects a continuation prompt:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Continue working toward the active thread goal.

The objective persists across turns.
Do not shrink it to what fits now.
Make concrete progress toward the real requested end state.

Before marking complete:
  derive concrete requirements
  inspect current evidence
  verify every requirement
  do not mark complete from partial progress

Before marking blocked:
  do not stop on first blocker
  same blocker must recur across multiple goal turns
  blocked means no meaningful progress is possible
</code></pre></div></div> <p>The goal is not higher-priority instruction. It is user-provided state that the runtime keeps visible and bounded.</p> <h3 id="budget-and-objective-updates">Budget And Objective Updates</h3> <p>When token budget is exhausted, runtime owns the transition:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>status = budget_limited
inject budget-limit prompt
tell agent:
  stop new substantive work
  summarize progress
  list remaining work
  do not call update_goal unless actually complete
</code></pre></div></div> <p>When the user edits the objective while a turn is running:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>external objective update:
  account current progress
  update state DB
  emit ordered notification
  inject objective-updated prompt
  tell agent new objective supersedes old objective
</code></pre></div></div> <p>This prevents the agent from acting on stale goal state.</p> <h3 id="goal-runtime-event-switch">Goal Runtime Event Switch</h3> <p>The runtime is event-driven:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TurnStarted:
  capture active goal id
  capture token/time baseline

ToolCompleted:
  account usage
  maybe inject budget steering

ToolCompletedGoal:
  account usage before goal update

TurnFinished:
  account final usage
  clear continuation reservation

UsageLimitReached:
  mark active goal usage_limited

ExternalSet:
  account before mutation
  update runtime state
  maybe inject objective-updated steering

ExternalClear:
  clear runtime state

ThreadResumed:
  restore active-goal accounting if status is active

MaybeContinueIfIdle:
  start continuation if active, idle, and no queued input
</code></pre></div></div> <p>This is why goals are not just workflow steps. They are state observed by runtime policy.</p> <h3 id="codex-mechanism-deck">Codex Mechanism Deck</h3> <section class="hcards" aria-label="Codex review and goal mechanism deck"> <div class="hcards__title">Codex review and goal mechanism deck</div> <div class="hcards__rail" tabindex="0"> <article id="codex-01" class="hcard"> <h4 class="hcard__title">Review target contract</h4> <div class="hcard__body"> <p>Review starts from a typed target: uncommitted changes, base branch, commit, or custom instructions. Empty targets are rejected before the reviewer runs.</p> </div> </article> <article id="codex-02" class="hcard"> <h4 class="hcard__title">Narrow reviewer service</h4> <div class="hcard__body"> <p>The reviewer is a one-shot sub-agent with web search, spawning, collaboration, and approval prompts disabled. It reviews; it does not build.</p> </div> </article> <article id="codex-03" class="hcard"> <h4 class="hcard__title">Structured output with fallback</h4> <div class="hcard__body"> <p>Codex parses JSON findings when possible, then tries a JSON substring, then falls back to plain text. The trace should preserve the parse quality.</p> </div> </article> <article id="codex-04" class="hcard"> <h4 class="hcard__title">Inline vs detached review</h4> <div class="hcard__body"> <p>Inline review writes into the parent thread. Detached review materializes history and forks a separate review thread for isolation.</p> </div> </article> <article id="codex-05" class="hcard"> <h4 class="hcard__title">Goal as durable state</h4> <div class="hcard__body"> <p>A goal is a persisted thread object with objective, status, budget, usage, and timestamps. It is not just text in the current prompt.</p> </div> </article> <article id="codex-06" class="hcard"> <h4 class="hcard__title">Caller-specific permissions</h4> <div class="hcard__body"> <p>The model can mark complete or blocked. User/API controls set, pause, resume, and clear. Runtime controls budget and usage statuses.</p> </div> </article> <article id="codex-07" class="hcard"> <h4 class="hcard__title">Goal authoring fields</h4> <div class="hcard__body"> <p>Strong goals name outcome, evidence, constraints, boundaries, iteration policy, and blocked stop condition. This is the user-facing half of the contract.</p> </div> </article> <article id="codex-08" class="hcard"> <h4 class="hcard__title">Completion audit</h4> <div class="hcard__body"> <p>Continuation prompts require requirement-by-requirement verification before <code class="language-plaintext highlighter-rouge">complete</code>. Improvement or plausible intent is not enough.</p> </div> </article> <article id="codex-09" class="hcard"> <h4 class="hcard__title">Idle continuation</h4> <div class="hcard__body"> <p>Continuation starts only when goals are enabled, the thread is idle, no input is queued, the mode allows it, and the goal is still active.</p> </div> </article> <article id="codex-10" class="hcard"> <h4 class="hcard__title">External mutation steering</h4> <div class="hcard__body"> <p>If the user edits the objective mid-run, runtime updates state and injects a prompt telling the agent the new objective supersedes the old one.</p> </div> </article> </div> </section> <h3 id="codex-goal-setup-decisions">Codex Goal Setup Decisions</h3> <table> <thead> <tr> <th>Decision</th> <th>Effect</th> </tr> </thead> <tbody> <tr> <td>Materialized thread required</td> <td>Goals need durable state DB storage.</td> </tr> <tr> <td>Objective text</td> <td>Human-facing canonical target, but weak without evidence semantics.</td> </tr> <tr> <td>Token budget</td> <td>Bounds autonomous continuation.</td> </tr> <tr> <td>Model tool restrictions</td> <td>Prevents model from owning pause, resume, budget, and usage states.</td> </tr> <tr> <td>Completion audit prompt</td> <td>Prevents shrinking the goal to local progress.</td> </tr> <tr> <td>Blocked recurrence</td> <td>Prevents premature “blocked” labels.</td> </tr> <tr> <td>Idle continuation guard</td> <td>Prevents background work when thread is active or user input is queued.</td> </tr> <tr> <td>External mutation handling</td> <td>Keeps active turns aligned with changed objectives.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">/goal pause/resume/clear</code></td> <td>Gives user lifecycle control.</td> </tr> </tbody> </table> <h2 id="comparing-the-two-harness-shapes">Comparing The Two Harness Shapes</h2> <p>Humanize and Codex solve overlapping problems with different shapes.</p> <table> <thead> <tr> <th>Concern</th> <th>Humanize h1</th> <th>Codex Review</th> <th>Codex Goals</th> </tr> </thead> <tbody> <tr> <td>Main abstraction</td> <td>Workflow loop</td> <td>Bounded service</td> <td>Persistent contract plus runtime policy</td> </tr> <tr> <td>Objective</td> <td>Plan and goal tracker</td> <td>Review target only</td> <td>Thread goal objective</td> </tr> <tr> <td>Evidence</td> <td>Summary, tests, tracker, diff, BitLesson</td> <td>Diff or custom target</td> <td>User-defined evidence in objective</td> </tr> <tr> <td>Stop control</td> <td>Stop hook blocks exit</td> <td>One-shot service exits</td> <td>Runtime continuation and goal status</td> </tr> <tr> <td>Review</td> <td>Codex summary review and diff review</td> <td>Review service itself</td> <td>Optional; not built into goal</td> </tr> <tr> <td>Drift handling</td> <td>Mainline verdict and drift recovery</td> <td>Not applicable</td> <td>Completion and blocked audits</td> </tr> <tr> <td>Memory</td> <td>BitLesson selector and delta</td> <td>Conversation review artifact</td> <td>Persisted goal state and usage</td> </tr> <tr> <td>Self-improvement</td> <td>Methodology analysis</td> <td>Trace only</td> <td>Goal trace and accounting</td> </tr> </tbody> </table> <p>Humanize is heavier but demonstrates a full long-task development loop. Codex is smaller but cleaner in separating service, contract, and runtime policy.</p> <h2 id="what-is-missing-in-these-harnesses">What Is Missing In These Harnesses?</h2> <p>Both systems are useful, but both leave gaps for project-wide strategy evolution.</p> <h3 id="1-objective-semantics-are-still-too-textual">1. Objective Semantics Are Still Too Textual</h3> <p>Humanize has a plan and goal tracker. Codex has a goal string plus runtime state. The Cookbook shows how to write strong goal prose.</p> <p>But prose is not enough for project evolution.</p> <p>A target project often needs objective services such as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>acceptance_criteria()
evidence_requirements()
allowed_artifacts()
benchmark_budget()
correctness_oracle()
domain_constraints()
blocked_evidence_threshold()
</code></pre></div></div> <p>Those should be programmable. For example, a kernel-evolution target should not rely on a prompt sentence to explain numerical tolerance, benchmark commands, GPU constraints, and allowed files.</p> <h3 id="2-evidence-needs-types-and-grades">2. Evidence Needs Types And Grades</h3> <p>Humanize has summaries and reviews. Codex goals ask for evidence. The research example shows that evidence can be exact, approximate, proxy, blocked, or unknown.</p> <p>Most harnesses still flatten this too much.</p> <p>For strategy evolution, evidence should be typed:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Exact:
  deterministic test, verified reproduction, exact metric threshold

Approximate:
  trained replacement, close numerical match, statistical support

Proxy:
  benchmark related to production but not identical

Blocked:
  missing data, unavailable hardware, missing upstream artifact

Unknown:
  not checked yet, inconclusive, contradictory runs
</code></pre></div></div> <p>Without typed evidence, an agent can overclaim success.</p> <h3 id="3-workflow-is-too-often-the-root-object">3. Workflow Is Too Often The Root Object</h3> <p>Humanize looks workflow-centric because the user runs one loop. Codex goals look less workflow-centric because continuation is runtime policy over state.</p> <p>For a general framework, workflow should not own everything.</p> <p>A better decomposition is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Service:
  bounded request/response, such as review(target)

Contract:
  durable state and allowed operations, such as objective or target

Policy:
  event-driven behavior over contracts and services, such as continuation

Trace:
  request, response, evidence, status change, degraded output, reviewer finding
</code></pre></div></div> <p>Workflow can be a policy. It should not be the only way to express the system.</p> <h3 id="4-harness-evolution-is-not-first-class-enough">4. Harness Evolution Is Not First-Class Enough</h3> <p>Humanize methodology analysis is a strong idea: the run should critique the method, not only the project. Codex traces lifecycle and usage events.</p> <p>But a general harness should make the harness itself inspectable:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Which service calls happened?
Which gates blocked work?
Which evidence was missing?
Which prompts caused drift?
Which review findings repeated?
Which contract fields were too weak?
Which policies launched wasteful work?
</code></pre></div></div> <p>Those traces should become inputs for improving the harness.</p> <h3 id="5-the-user-interface-for-harness-power-is-still-hard">5. The User Interface For Harness Power Is Still Hard</h3> <p>Codex’s <code class="language-plaintext highlighter-rouge">/goal</code> command is easy. Humanize’s RLCR is powerful but heavier.</p> <p>A general framework needs progressive disclosure:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Level 0:
  one prompt or one workflow

Level 1:
  add a goal/objective contract

Level 2:
  add evidence services and review gates

Level 3:
  add memory, drift recovery, and methodology analysis

Level 4:
  let agents inspect traces and improve the harness itself
</code></pre></div></div> <p>Users should not pay setup cost for features they do not need.</p> <h2 id="what-this-suggests-for-puppeteer">What This Suggests For Puppeteer</h2> <p>The case studies suggest Puppeteer’s position:</p> <blockquote> <p>Puppeteer should be an outside project-improvement harness that lets a target project expose programmable contracts and services to one or more agents, while the harness mediates communication, evidence, gates, policies, and traces.</p> </blockquote> <p>This is not the same as replacing Humanize or Codex.</p> <p>Humanize is a workflow-rich harness. Codex goals are a clean thread-objective mechanism. Puppeteer should learn from both while targeting a broader use case: strategy evolution inside arbitrary projects.</p> <h3 id="puppeteers-missing-layer">Puppeteer’s Missing Layer</h3> <p>The missing layer is not “more workflow nodes.” It is a communication and contract layer:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Harness-mediated request:
  caller -&gt; contract/service -&gt; response -&gt; trace

Examples:
  policy -&gt; target.benchmark(candidate) -&gt; BenchmarkResult
  policy -&gt; objective.check_completion(evidence) -&gt; CompletionDecision
  policy -&gt; review.review(packet) -&gt; ReviewDecision
  policy -&gt; memory.select(task, files) -&gt; KnowledgePacket
</code></pre></div></div> <p>This layer gives several benefits:</p> <ul> <li>Components are only defined when needed.</li> <li>Services can be traced uniformly.</li> <li>Agents can inspect the traces and improve the system.</li> <li>Workflow can stay decoupled from target-specific details.</li> <li>The same project can start simple and add structure over time.</li> </ul> <h3 id="what-puppeteer-should-provide-easily">What Puppeteer Should Provide Easily</h3> <p>The case studies point to a concrete product surface.</p> <p>Puppeteer should make these easy:</p> <ol> <li>Define an objective contract with optional authoring fields: outcome, evidence, constraints, boundaries, iteration policy, blocked condition.</li> <li>Define target services: run benchmark, check correctness, inspect artifacts, compare candidates, extract strategy scopes.</li> <li>Define review services: summary review, diff review, evidence review, claim-ledger review.</li> <li>Define gates: deterministic checks before model judgement.</li> <li>Define policies: simple workflow, Humanize-like RLCR, Codex-goal-like continuation, or custom target evolution loop.</li> <li>Record traces: every request, response, degraded output, evidence packet, review decision, state transition, and blocked reason.</li> <li>Support progressive disclosure: a user can run a workflow-only harness first, then add contracts and services only when the target needs them.</li> </ol> <h3 id="a-minimal-puppeteer-like-shape">A Minimal Puppeteer-Like Shape</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">objective</span> <span class="o">=</span> <span class="n">harness</span><span class="p">.</span><span class="nf">contract</span><span class="p">(</span><span class="sh">"</span><span class="s">objective</span><span class="sh">"</span><span class="p">)</span>
<span class="n">target</span> <span class="o">=</span> <span class="n">harness</span><span class="p">.</span><span class="nf">contract</span><span class="p">(</span><span class="sh">"</span><span class="s">target</span><span class="sh">"</span><span class="p">)</span>
<span class="n">review</span> <span class="o">=</span> <span class="n">harness</span><span class="p">.</span><span class="nf">service</span><span class="p">(</span><span class="sh">"</span><span class="s">review</span><span class="sh">"</span><span class="p">)</span>

<span class="nd">@policy.on</span><span class="p">(</span><span class="sh">"</span><span class="s">round</span><span class="sh">"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">evolve</span><span class="p">(</span><span class="n">ctx</span><span class="p">):</span>
    <span class="n">candidate</span> <span class="o">=</span> <span class="n">ctx</span><span class="p">.</span><span class="n">agent</span><span class="p">.</span><span class="nf">propose_change</span><span class="p">(</span><span class="n">objective</span><span class="p">.</span><span class="nf">current</span><span class="p">())</span>
    <span class="n">evidence</span> <span class="o">=</span> <span class="n">target</span><span class="p">.</span><span class="nf">evaluate</span><span class="p">(</span><span class="n">candidate</span><span class="p">)</span>
    <span class="n">decision</span> <span class="o">=</span> <span class="n">review</span><span class="p">.</span><span class="nf">check</span><span class="p">(</span><span class="n">objective</span><span class="o">=</span><span class="n">objective</span><span class="p">.</span><span class="nf">snapshot</span><span class="p">(),</span> <span class="n">evidence</span><span class="o">=</span><span class="n">evidence</span><span class="p">)</span>

    <span class="n">ctx</span><span class="p">.</span><span class="n">trace</span><span class="p">.</span><span class="nf">record</span><span class="p">(</span><span class="n">candidate</span><span class="o">=</span><span class="n">candidate</span><span class="p">,</span> <span class="n">evidence</span><span class="o">=</span><span class="n">evidence</span><span class="p">,</span> <span class="n">decision</span><span class="o">=</span><span class="n">decision</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">decision</span><span class="p">.</span><span class="n">complete</span><span class="p">:</span>
        <span class="n">objective</span><span class="p">.</span><span class="nf">update_by_model</span><span class="p">(</span><span class="sh">"</span><span class="s">complete</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">elif</span> <span class="n">decision</span><span class="p">.</span><span class="n">blocking</span><span class="p">:</span>
        <span class="n">objective</span><span class="p">.</span><span class="nf">record_blocker</span><span class="p">(</span><span class="n">decision</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">ctx</span><span class="p">.</span><span class="n">agent</span><span class="p">.</span><span class="nf">continue_with</span><span class="p">(</span><span class="n">decision</span><span class="p">.</span><span class="nf">next_prompt</span><span class="p">())</span>
</code></pre></div></div> <p>This sketch is intentionally not a fixed workflow DSL. It is normal code calling contracts and services through a harness.</p> <h2 id="takeaways">Takeaways</h2> <p>Humanize h1 shows why a long-task coding loop needs plans, round contracts, cheap gates, review pressure, memory discipline, drift recovery, and methodology analysis.</p> <p>Codex review shows how a narrowly scoped review service should work: typed target, reduced tool surface, structured output, fallback, lifecycle events, and optional detached delivery.</p> <p>Codex goals show how a durable objective should work: persisted state, caller-specific permissions, runtime accounting, safe continuation, budget/usage statuses, external mutation handling, and completion audits.</p> <p>The OpenAI Cookbook shows the missing human-facing layer: a goal should encode outcome, evidence, constraints, boundaries, iteration policy, and blocked stop condition.</p> <p>Puppeteer should not simply copy any one of these. Its job is to provide the outside framework where target projects can define their own contracts and services, agents can operate through those interfaces, and the harness can trace and improve the whole process.</p>]]></content><author><name></name></author><category term="agent-systems"/><category term="agent-harness"/><category term="workflow"/><category term="codex"/><category term="humanize"/><category term="puppeteer"/><summary type="html"><![CDATA[A workflow-focused study of two agent harness mechanisms: Humanize h1's RLCR loop and Codex's review and goal systems.]]></summary></entry><entry><title type="html">(Paper Reading) Event Tensor: A Unified Abstraction for Compiling Dynamic Megakernel</title><link href="https://uv-xiao.github.io/blog/2026/event-tensor-dynamic-megakernel/" rel="alternate" type="text/html" title="(Paper Reading) Event Tensor: A Unified Abstraction for Compiling Dynamic Megakernel"/><published>2026-04-21T00:00:00+00:00</published><updated>2026-04-21T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2026/event-tensor-dynamic-megakernel</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2026/event-tensor-dynamic-megakernel/"><![CDATA[<pre><code class="language-txt">@misc{jin2026eventtensor,
  title = {Event Tensor: A Unified Abstraction for Compiling Dynamic Megakernel},
  author = {Jin, Hongyi and Hou, Bohan and Wang, Guanjie and Lai, Ruihang and Chen, Jinqi and Ye, Zihao and Cai, Yaxing and Dong, Yixin and Cheng, Xinhao and Zhang, Zhihao and Zhao, Yilong and Huang, Yingyi and Yang, Lijie and Jiang, Jinchen and Oliaro, Gabriele and Ji, Jianan and Miao, Xupeng and Grover, Vinod and Mowry, Todd C. and Jia, Zhihao and Chen, Tianqi},
  year = {2026},
  eprint = {2604.13327},
  archivePrefix = {arXiv},
  primaryClass = {cs.DC},
  url = {https://arxiv.org/abs/2604.13327}
}
</code></pre> <p>This is a reading note for <a href="https://arxiv.org/abs/2604.13327"><strong>Event Tensor: A Unified Abstraction for Compiling Dynamic Megakernel</strong></a>, submitted to arXiv on April 14, 2026.</p> <p>The short version:</p> <blockquote> <p>Event Tensor turns fine-grained megakernel synchronization into a first-class tensor object in compiler IR, so a compiler can represent, transform, and lower dynamic task dependencies instead of hand-writing one-off persistent kernels.</p> </blockquote> <p>That is the real contribution. The paper is not claiming that event counters, semaphores, persistent kernels, or GPU task queues are new by themselves. The novelty is the <strong>compiler abstraction</strong>: represent events as symbolic-shaped tensors, express producer-to-event and event-to-consumer relations with coordinate maps, and then lower the same representation into either static or dynamic megakernel schedules.</p> <h2 id="why-this-paper-matters">Why This Paper Matters</h2> <p>LLM serving is increasingly bottlenecked by execution overhead, especially in low-batch decoding:</p> <ul> <li>A decode step can contain hundreds or thousands of small GPU operations.</li> <li>Kernel launch latency can be comparable to, or larger than, the fastest kernels.</li> <li>Kernel boundaries act as global synchronization barriers.</li> <li>CUDA Graph reduces launch overhead but still preserves kernel boundaries.</li> <li>Megakernels remove launch gaps and allow tile-level overlap, but they are hard to program and usually assume fixed shapes or simple dense workloads.</li> </ul> <p>The paper targets the missing piece:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CUDA Graph:
  less launch overhead
  still kernel-by-kernel dependency boundaries
  hard to handle many dynamic shapes without recapture

Manual megakernel:
  one persistent kernel
  fine-grained task overlap
  hard to write, hard to maintain, hard to support dynamism

Event Tensor:
  compiler IR for tile dependencies
  symbolic shapes for dynamic batches
  data-dependent event updates/triggers for MoE-like routing
  static or dynamic scheduling generated from one abstraction
</code></pre></div></div> <p>The key insight is very simple:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Data tensors:
  multidimensional values indexed by coordinates

Event tensors:
  multidimensional completion events indexed by coordinates
</code></pre></div></div> <p>Once synchronization becomes tensor-shaped, the compiler can reuse the same symbolic-shape machinery used for data tensors. This is what makes dynamic megakernels possible without recompiling or recapturing every concrete shape.</p> <h2 id="main-contributions">Main Contributions</h2> <p>The contributions can be read as four layers.</p> <h3 id="1-event-tensor-as-a-first-class-ir-object">1. Event Tensor As A First-Class IR Object</h3> <p>The paper defines an <strong>Event Tensor</strong> as a multidimensional array of event counters. Each element records whether some set of producer tasks has completed. Consumers can wait on the event or be triggered by it.</p> <p>This changes synchronization from scattered low-level code into an analyzable IR object:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before:
  hand-written semaphore counters
  custom wait loops
  task graph materialized or hard-coded manually

After:
  ETensor(shape, wait_count)
  producer out_edges: task_coord -&gt; event_coord
  consumer in_edges:  task_coord -&gt; event_coord
  compiler lowers notify/wait/trigger
</code></pre></div></div> <p>The paper’s formulation is roughly:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">E</span> <span class="o">=</span> <span class="nc">ETensor</span><span class="p">(</span><span class="n">shape</span><span class="o">=</span><span class="p">(</span><span class="n">n</span><span class="p">,),</span> <span class="n">wait_count</span><span class="o">=</span><span class="mi">4</span><span class="p">)</span>

<span class="n">B</span> <span class="o">=</span> <span class="nf">call_device</span><span class="p">(</span>
    <span class="n">partial_sum</span><span class="p">,</span>
    <span class="n">tile_num</span><span class="o">=</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="mi">4</span><span class="p">),</span>
    <span class="n">in_edges</span><span class="o">=</span><span class="p">{},</span>
    <span class="n">out_edges</span><span class="o">=</span><span class="p">{</span><span class="n">E</span><span class="p">:</span> <span class="sh">"</span><span class="s">ij-&gt;i</span><span class="sh">"</span><span class="p">},</span>
<span class="p">)</span>

<span class="n">C</span> <span class="o">=</span> <span class="nf">call_device</span><span class="p">(</span>
    <span class="n">final_sum</span><span class="p">,</span>
    <span class="n">tile_num</span><span class="o">=</span><span class="p">(</span><span class="n">n</span><span class="p">,),</span>
    <span class="n">in_edges</span><span class="o">=</span><span class="p">{</span><span class="n">E</span><span class="p">:</span> <span class="sh">"</span><span class="s">i-&gt;i</span><span class="sh">"</span><span class="p">},</span>
    <span class="n">out_edges</span><span class="o">=</span><span class="p">{},</span>
<span class="p">)</span>
</code></pre></div></div> <p>The string <code class="language-plaintext highlighter-rouge">"ij-&gt;i"</code> means that producer task <code class="language-plaintext highlighter-rouge">(i, j)</code> updates event <code class="language-plaintext highlighter-rouge">E[i]</code>. Since there are four values of <code class="language-plaintext highlighter-rouge">j</code>, each <code class="language-plaintext highlighter-rouge">E[i]</code> waits for four partial-sum tasks. The consumer task <code class="language-plaintext highlighter-rouge">final_sum(i)</code> can run as soon as <code class="language-plaintext highlighter-rouge">E[i]</code> is complete. It does not need every row’s partial sums to finish.</p> <h3 id="2-symbolic-shape-task-graphs">2. Symbolic-Shape Task Graphs</h3> <p>The Event Tensor shape can contain symbolic dimensions, such as batch size <code class="language-plaintext highlighter-rouge">B</code> or number of token blocks <code class="language-plaintext highlighter-rouge">n</code>.</p> <p>That means the compiler can represent a <strong>family of task graphs</strong>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Template:
  E: ETensor((B, H))
  task_grid: (B, H)

Runtime B = 1:
  materialized event grid has 1 * H events

Runtime B = 8:
  materialized event grid has 8 * H events
</code></pre></div></div> <p>The compiler does not need to generate a separate CUDA Graph or megakernel for every batch size. The symbolic Event Tensor is a compact task-graph template.</p> <p>This is the first major reason the abstraction is useful for production inference. Continuous batching creates dynamic shape values all the time. Static CUDA Graph capture handles this by capturing many shape-specialized graphs, which creates warmup overhead and deployment complexity.</p> <h3 id="3-data-dependent-updates-and-triggers">3. Data-Dependent Updates And Triggers</h3> <p>Dynamic shapes are not enough. MoE layers introduce <strong>data-dependent control flow</strong>: the tasks that should run depend on routing tensors computed at runtime.</p> <p>The paper uses MoE as the core example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Token -&gt; TopK router -&gt; token grouping -&gt; expert GroupGEMM -&gt; scatter/output
</code></pre></div></div> <p>For each token, <code class="language-plaintext highlighter-rouge">topk</code> decides which experts it should visit. This means:</p> <ul> <li>A grouping task for token <code class="language-plaintext highlighter-rouge">i</code> may update expert event <code class="language-plaintext highlighter-rouge">E[topk[i, k]]</code>.</li> <li>Each expert event’s wait count depends on how many tokens were routed to it.</li> <li>Each expert event may trigger a runtime-dependent range of GroupGEMM tiles.</li> <li>The range is represented by an <code class="language-plaintext highlighter-rouge">exp_indptr</code> array, similar to CSR sparse matrix indexing.</li> </ul> <p>In text form:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>topk:
  token i -&gt; selected experts

exp_indptr:
  expert e -&gt; GroupGEMM tile range
  tiles for expert e are [exp_indptr[e], exp_indptr[e + 1])
</code></pre></div></div> <p>The novelty is that Event Tensor allows these runtime tensors to participate in event indexing and triggering:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>data-dependent update:
  grouping_tile(token_i) notifies E[topk[i, k]]

data-dependent trigger:
  E[expert_e] triggers GroupGEMM tiles in
  range(exp_indptr[e], exp_indptr[e + 1])
</code></pre></div></div> <p>This is a clean abstraction boundary. The compiler still emits a persistent kernel, but the runtime values determine the actual dependency edges followed by that kernel.</p> <h3 id="4-one-abstraction-two-scheduling-strategies">4. One Abstraction, Two Scheduling Strategies</h3> <p>The same Event Tensor graph can be lowered into two scheduling styles:</p> <table> <thead> <tr> <th>Strategy</th> <th>Best For</th> <th>Mechanism</th> <th>Cost</th> </tr> </thead> <tbody> <tr> <td>Static scheduling</td> <td>Predictable workloads</td> <td>Precompute per-SM task queues, insert wait/notify</td> <td>Low runtime overhead, less adaptive</td> </tr> <tr> <td>Dynamic scheduling</td> <td>Irregular workloads</td> <td>GPU task queue, push tasks when events complete, idle SMs pop work</td> <td>Better load balance, queue overhead</td> </tr> </tbody> </table> <p>This is important because megakernel scheduling is workload-dependent. Dense transformer decode often benefits from static scheduling because tasks are regular and scheduling overhead matters. MoE and communication-heavy kernels may benefit from dynamic scheduling because task times and routing are irregular.</p> <h2 id="core-mental-model">Core Mental Model</h2> <p>Think of the computation as three objects:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task grid:
  A multidimensional set of tile-level tasks.
  Example: GEMM tiles (m_tile, n_tile).

Event tensor:
  A multidimensional set of event counters.
  Example: E[row_block] means all partial sums for that row block are ready.

Coordinate maps:
  Functions from task coordinates to event coordinates.
  Example: producer (i, j) updates event i.
</code></pre></div></div> <p>The general dependency pattern is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>producer task -&gt; event tensor element -&gt; consumer task
</code></pre></div></div> <p>But the dependency edges are not manually listed. They are represented by compact index maps:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Producer grid P(i, j)
  out_edges: P(i, j) -&gt; E(i)

Consumer grid C(i)
  in_edges: E(i) -&gt; C(i)
</code></pre></div></div> <p>This gives the compiler enough information to generate synchronization code without materializing a huge task graph in host memory.</p> <h2 id="example-split-row-sum">Example: Split Row Sum</h2> <p>The paper’s simple example is a split-K row sum.</p> <p>Suppose:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A shape: (n * 32, 128)
C shape: (n * 32)
Goal:
  C[row] = sum over A[row, :]
</code></pre></div></div> <p>Instead of one task per row block, split the reduction into two stages:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>partial_sum(i, j):
  B[i*32 : i*32+32, j] =
    sum A[i*32 : i*32+32, j*32 : j*32+32]

final_sum(i):
  C[i*32 : i*32+32] =
    sum B[i*32 : i*32+32, :]
</code></pre></div></div> <p>The first stage has task grid <code class="language-plaintext highlighter-rouge">(n, 4)</code>. The second stage has task grid <code class="language-plaintext highlighter-rouge">(n,)</code>.</p> <p>Naive kernel-by-kernel execution does this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>run all partial_sum(i, j)
global kernel boundary
run all final_sum(i)
</code></pre></div></div> <p>Event Tensor execution does this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>partial_sum(0, 0) -&gt; E[0]
partial_sum(0, 1) -&gt; E[0]
partial_sum(0, 2) -&gt; E[0]
partial_sum(0, 3) -&gt; E[0]
E[0] complete -&gt; final_sum(0)

partial_sum(1, 0) -&gt; E[1]
...
E[1] complete -&gt; final_sum(1)
</code></pre></div></div> <p>Now <code class="language-plaintext highlighter-rouge">final_sum(0)</code> can start as soon as row block 0 is ready, even if row block 1 is still computing.</p> <p>The event counter implementation is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E[i].counter starts at 4

partial_sum(i, j):
  compute B tile
  atomic_dec(E[i].counter)

final_sum(i):
  wait until E[i].counter == 0
  compute C tile
</code></pre></div></div> <p>This looks small, but this is exactly the dependency pattern that scales to transformer layers: a later tiled operator often only needs a subset of a previous operator’s tiles.</p> <h2 id="event-tensor-ir">Event Tensor IR</h2> <p>A minimal IR for reproducing the abstraction needs these pieces.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">EventTensor</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">,</span> <span class="n">shape</span><span class="p">,</span> <span class="n">wait_count</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">name</span> <span class="o">=</span> <span class="n">name</span>
        <span class="n">self</span><span class="p">.</span><span class="n">shape</span> <span class="o">=</span> <span class="n">shape</span>          <span class="c1"># can contain symbolic variables
</span>        <span class="n">self</span><span class="p">.</span><span class="n">wait_count</span> <span class="o">=</span> <span class="n">wait_count</span>

<span class="k">class</span> <span class="nc">Edge</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">event_tensor</span><span class="p">,</span> <span class="n">coord_map</span><span class="p">,</span> <span class="n">kind</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">event_tensor</span> <span class="o">=</span> <span class="n">event_tensor</span>
        <span class="n">self</span><span class="p">.</span><span class="n">coord_map</span> <span class="o">=</span> <span class="n">coord_map</span>  <span class="c1"># task_coord -&gt; event_coord
</span>        <span class="n">self</span><span class="p">.</span><span class="n">kind</span> <span class="o">=</span> <span class="n">kind</span>            <span class="c1"># "in" or "out"
</span>
<span class="k">class</span> <span class="nc">TaskGrid</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">,</span> <span class="n">tile_shape</span><span class="p">,</span> <span class="n">device_func</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">name</span> <span class="o">=</span> <span class="n">name</span>
        <span class="n">self</span><span class="p">.</span><span class="n">tile_shape</span> <span class="o">=</span> <span class="n">tile_shape</span>
        <span class="n">self</span><span class="p">.</span><span class="n">device_func</span> <span class="o">=</span> <span class="n">device_func</span>
        <span class="n">self</span><span class="p">.</span><span class="n">in_edges</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">self</span><span class="p">.</span><span class="n">out_edges</span> <span class="o">=</span> <span class="p">[]</span>

<span class="k">class</span> <span class="nc">GraphFunc</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">task_grids</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">self</span><span class="p">.</span><span class="n">event_tensors</span> <span class="o">=</span> <span class="p">[]</span>
</code></pre></div></div> <p>For the split row sum:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">n</span> <span class="o">=</span> <span class="nc">SymVar</span><span class="p">(</span><span class="sh">"</span><span class="s">n</span><span class="sh">"</span><span class="p">)</span>
<span class="n">E</span> <span class="o">=</span> <span class="nc">EventTensor</span><span class="p">(</span><span class="sh">"</span><span class="s">E</span><span class="sh">"</span><span class="p">,</span> <span class="n">shape</span><span class="o">=</span><span class="p">(</span><span class="n">n</span><span class="p">,),</span> <span class="n">wait_count</span><span class="o">=</span><span class="mi">4</span><span class="p">)</span>

<span class="n">partial</span> <span class="o">=</span> <span class="nc">TaskGrid</span><span class="p">(</span><span class="sh">"</span><span class="s">partial_sum</span><span class="sh">"</span><span class="p">,</span> <span class="n">tile_shape</span><span class="o">=</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="mi">4</span><span class="p">),</span> <span class="n">device_func</span><span class="o">=</span><span class="n">partial_sum</span><span class="p">)</span>
<span class="n">partial</span><span class="p">.</span><span class="n">out_edges</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Edge</span><span class="p">(</span><span class="n">E</span><span class="p">,</span> <span class="n">coord_map</span><span class="o">=</span><span class="k">lambda</span> <span class="n">i</span><span class="p">,</span> <span class="n">j</span><span class="p">:</span> <span class="p">(</span><span class="n">i</span><span class="p">,),</span> <span class="n">kind</span><span class="o">=</span><span class="sh">"</span><span class="s">out</span><span class="sh">"</span><span class="p">))</span>

<span class="n">final</span> <span class="o">=</span> <span class="nc">TaskGrid</span><span class="p">(</span><span class="sh">"</span><span class="s">final_sum</span><span class="sh">"</span><span class="p">,</span> <span class="n">tile_shape</span><span class="o">=</span><span class="p">(</span><span class="n">n</span><span class="p">,),</span> <span class="n">device_func</span><span class="o">=</span><span class="n">final_sum</span><span class="p">)</span>
<span class="n">final</span><span class="p">.</span><span class="n">in_edges</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Edge</span><span class="p">(</span><span class="n">E</span><span class="p">,</span> <span class="n">coord_map</span><span class="o">=</span><span class="k">lambda</span> <span class="n">i</span><span class="p">:</span> <span class="p">(</span><span class="n">i</span><span class="p">,),</span> <span class="n">kind</span><span class="o">=</span><span class="sh">"</span><span class="s">in</span><span class="sh">"</span><span class="p">))</span>

<span class="n">graph</span> <span class="o">=</span> <span class="nc">GraphFunc</span><span class="p">()</span>
<span class="n">graph</span><span class="p">.</span><span class="n">event_tensors</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">E</span><span class="p">)</span>
<span class="n">graph</span><span class="p">.</span><span class="n">task_grids</span><span class="p">.</span><span class="nf">extend</span><span class="p">([</span><span class="n">partial</span><span class="p">,</span> <span class="n">final</span><span class="p">])</span>
</code></pre></div></div> <p>The compiler must infer two facts:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E[i] producer count = number of partial tasks that map to E[i] = 4
final_sum(i) is ready when E[i] reaches zero
</code></pre></div></div> <p>For static maps like <code class="language-plaintext highlighter-rouge">"ij-&gt;i"</code>, this can be derived statically. For data-dependent maps like <code class="language-plaintext highlighter-rouge">topk[i, k]</code>, initialization must be computed at runtime.</p> <h2 id="runtime-semantics">Runtime Semantics</h2> <p>At runtime, an Event Tensor is lowered to an integer tensor.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E: ETensor(shape=(n,), wait_count=4)

lowers to:
  int E_counter[n]
  initialized to 4 for each i
</code></pre></div></div> <p>The two basic operations are:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">notify</span><span class="p">(</span><span class="n">E</span><span class="p">,</span> <span class="n">idx</span><span class="p">)</span><span class="o">:</span>
  <span class="n">old</span> <span class="o">=</span> <span class="n">atomic_sub</span><span class="p">(</span><span class="o">&amp;</span><span class="n">E_counter</span><span class="p">[</span><span class="n">idx</span><span class="p">],</span> <span class="mi">1</span><span class="p">)</span>
  <span class="k">if</span> <span class="n">old</span> <span class="o">==</span> <span class="mi">1</span><span class="o">:</span>
      <span class="n">trigger</span> <span class="n">consumers</span> <span class="n">of</span> <span class="n">E</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span>

<span class="n">wait</span><span class="p">(</span><span class="n">E</span><span class="p">,</span> <span class="n">idx</span><span class="p">)</span><span class="o">:</span>
  <span class="k">while</span> <span class="n">atomic_load</span><span class="p">(</span><span class="o">&amp;</span><span class="n">E_counter</span><span class="p">[</span><span class="n">idx</span><span class="p">])</span> <span class="o">!=</span> <span class="mi">0</span><span class="o">:</span>
      <span class="n">spin</span>
</code></pre></div></div> <p>For static scheduling, <code class="language-plaintext highlighter-rouge">trigger</code> usually just releases a waiting task because the task is already in some SM’s precomputed queue.</p> <p>For dynamic scheduling, <code class="language-plaintext highlighter-rouge">trigger</code> pushes newly ready consumer tasks into a GPU queue.</p> <p>Important implementation notes:</p> <ul> <li><code class="language-plaintext highlighter-rouge">notify</code> must publish the producer’s writes before the counter reaches zero.</li> <li><code class="language-plaintext highlighter-rouge">wait</code> must acquire visibility of producer writes before reading produced data.</li> <li>A practical CUDA implementation should use release semantics on notify and acquire semantics after wait, or an equivalent combination of atomics and fences.</li> <li>The paper describes the runtime state as integer tensors plus scheduler queues. It does not require a general host-side task graph executor at runtime.</li> </ul> <h2 id="static-scheduling">Static Scheduling</h2> <p>Static scheduling precomputes the task order before launch.</p> <p>The generated megakernel has one persistent loop per SM or worker group:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>host:
  static_schedule = GenerateStaticSchedule(graph)
  copy static_schedule to GPU
  launch persistent_kernel&lt;&lt;&lt;num_sms, ...&gt;&gt;&gt;(static_schedule, ...)

device:
  sm_id = get_sm_id()
  while task = static_schedule[sm_id].next():
      for each input event of task:
          wait(event)
      run task tile
      for each output event of task:
          notify(event)
</code></pre></div></div> <p>The paper’s static transformation can be written as:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">static_schedule_transform</span><span class="p">(</span><span class="n">mod</span><span class="p">,</span> <span class="n">graph</span><span class="p">):</span>
    <span class="n">mod_updated</span> <span class="o">=</span> <span class="nf">copy</span><span class="p">(</span><span class="n">mod</span><span class="p">)</span>
    <span class="n">static_schedule</span> <span class="o">=</span> <span class="nc">GenerateStaticSchedule</span><span class="p">(</span><span class="n">graph</span><span class="p">)</span>
    <span class="n">fused_kernel</span> <span class="o">=</span> <span class="nc">NewPersistentKernel</span><span class="p">()</span>
    <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_buffer</span><span class="p">(</span><span class="n">static_schedule</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">task_grid</span> <span class="ow">in</span> <span class="n">graph</span><span class="p">.</span><span class="n">task_grids</span><span class="p">:</span>
        <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_dispatch_logic</span><span class="p">(</span><span class="n">task_grid</span><span class="p">)</span>

        <span class="k">for</span> <span class="n">event</span> <span class="ow">in</span> <span class="n">task_grid</span><span class="p">.</span><span class="n">in_edges</span><span class="p">:</span>
            <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_wait_logic</span><span class="p">(</span><span class="n">event</span><span class="p">)</span>

        <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_tile_logic</span><span class="p">(</span><span class="n">task_grid</span><span class="p">)</span>

        <span class="k">for</span> <span class="n">event</span> <span class="ow">in</span> <span class="n">task_grid</span><span class="p">.</span><span class="n">out_edges</span><span class="p">:</span>
            <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_notify_logic</span><span class="p">(</span><span class="n">event</span><span class="p">)</span>

    <span class="n">mod_updated</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="n">graph</span><span class="p">,</span> <span class="n">fused_kernel</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">mod_updated</span>
</code></pre></div></div> <p>For regular workloads, static scheduling is appealing because it avoids GPU queue push/pop overhead. The cost is that the schedule may be suboptimal when task durations vary.</p> <p>The paper uses these policies for dynamism under static scheduling:</p> <ul> <li><strong>Shape dynamism</strong>: sample representative shape values; unseen shapes reuse the queue for the next larger sampled shape.</li> <li><strong>Data-dependent dynamism</strong>: conservatively rewrite related notify/wait operations to a single worst-case event such as <code class="language-plaintext highlighter-rouge">E[0]</code>.</li> <li><strong>Queue construction</strong>: use a simple round-robin policy.</li> </ul> <p>That means static scheduling supports dynamism, but conservatively. It is best when the task graph is regular enough that the precomputed order remains good.</p> <h2 id="dynamic-scheduling">Dynamic Scheduling</h2> <p>Dynamic scheduling uses a lightweight GPU task scheduler.</p> <p>The execution model is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ready queue:
  contains tasks whose dependencies are satisfied

worker SM:
  pop ready task
  execute tile
  notify output events
  if an event reaches zero:
      push its consumer tasks
</code></pre></div></div> <p>Text diagram:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>time T1:
  SM0 finishes producer P0
  E[k] goes from 2 to 1
  no consumer is ready yet
  SM0 pops another ready task

time T2:
  SM1 finishes producer P1
  E[k] goes from 1 to 0
  consumers of E[k] are pushed to queue
  an idle SM pops one consumer and runs it
</code></pre></div></div> <p>The paper’s dynamic transformation is:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">dynamic_schedule_transform</span><span class="p">(</span><span class="n">mod</span><span class="p">,</span> <span class="n">graph</span><span class="p">):</span>
    <span class="n">mod_updated</span> <span class="o">=</span> <span class="nf">copy</span><span class="p">(</span><span class="n">mod</span><span class="p">)</span>
    <span class="n">fused_kernel</span> <span class="o">=</span> <span class="nc">NewPersistentKernel</span><span class="p">()</span>
    <span class="n">scheduler</span> <span class="o">=</span> <span class="nc">GPUScheduler</span><span class="p">()</span>

    <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_pop_logic</span><span class="p">(</span><span class="n">scheduler</span><span class="p">.</span><span class="n">pop_tasks</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">task_grid</span> <span class="ow">in</span> <span class="n">graph</span><span class="p">.</span><span class="n">task_grids</span><span class="p">:</span>
        <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_dispatch_logic</span><span class="p">(</span><span class="n">task_grid</span><span class="p">)</span>
        <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_tile_logic</span><span class="p">(</span><span class="n">task_grid</span><span class="p">)</span>

        <span class="k">for</span> <span class="n">event</span> <span class="ow">in</span> <span class="n">task_grid</span><span class="p">.</span><span class="n">out_edges</span><span class="p">:</span>
            <span class="n">fused_kernel</span><span class="p">.</span><span class="nf">add_complete_on_logic</span><span class="p">(</span>
                <span class="n">event</span><span class="p">,</span>
                <span class="n">on_complete</span><span class="o">=</span><span class="n">scheduler</span><span class="p">.</span><span class="n">push_tasks</span><span class="p">,</span>
            <span class="p">)</span>

    <span class="n">mod_updated</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="n">graph</span><span class="p">,</span> <span class="n">fused_kernel</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">mod_updated</span>
</code></pre></div></div> <p>The paper reports a centralized queue in global memory shared across SMs. This is simple but may contend at scale. The appendix also describes an <strong>early push</strong> optimization: push a consumer task when all its producer tasks have been dispatched, not necessarily completed, then keep an extra wait before the consumer’s actual execution. This overlaps scheduler push overhead with producer computation.</p> <p>Inferred implementation skeleton:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">Task</span> <span class="n">t</span> <span class="o">=</span> <span class="n">queue_pop</span><span class="p">(</span><span class="n">global_ready_queue</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">t</span><span class="p">.</span><span class="n">kind</span> <span class="o">==</span> <span class="n">DONE</span><span class="p">)</span> <span class="k">break</span><span class="p">;</span>

    <span class="c1">// The task may have been pushed early.</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">dep</span> <span class="n">in</span> <span class="n">t</span><span class="p">.</span><span class="n">input_events</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">wait</span><span class="p">(</span><span class="n">dep</span><span class="p">.</span><span class="n">event</span><span class="p">,</span> <span class="n">dep</span><span class="p">.</span><span class="n">index</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="n">dispatch_tile</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>

    <span class="k">for</span> <span class="p">(</span><span class="n">out</span> <span class="n">in</span> <span class="n">t</span><span class="p">.</span><span class="n">output_events</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">old</span> <span class="o">=</span> <span class="n">atomic_sub_release</span><span class="p">(</span><span class="o">&amp;</span><span class="n">out</span><span class="p">.</span><span class="n">event</span><span class="p">.</span><span class="n">counter</span><span class="p">[</span><span class="n">out</span><span class="p">.</span><span class="n">index</span><span class="p">],</span> <span class="mi">1</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">old</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">for</span> <span class="p">(</span><span class="n">consumer</span> <span class="n">in</span> <span class="n">consumers</span><span class="p">(</span><span class="n">out</span><span class="p">.</span><span class="n">event</span><span class="p">,</span> <span class="n">out</span><span class="p">.</span><span class="n">index</span><span class="p">))</span> <span class="p">{</span>
                <span class="n">queue_push</span><span class="p">(</span><span class="n">global_ready_queue</span><span class="p">,</span> <span class="n">consumer</span><span class="p">);</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>Dynamic scheduling naturally handles runtime shapes and data-dependent routing because tasks are generated or selected after runtime values are known.</p> <h2 id="data-dependent-moe-in-detail">Data-Dependent MoE In Detail</h2> <p>MoE is the best example because it has both irregular work and fine-grained producer-consumer structure.</p> <p>A simplified MoE layer:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>input tokens
  -&gt; router computes topk experts
  -&gt; grouping reorders tokens by expert
  -&gt; GroupGEMM expert computation
  -&gt; scatter/combine output
</code></pre></div></div> <p>The dynamic dependency challenge:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The compiler does not know at compile time:
  how many tokens go to expert 0
  how many tokens go to expert 1
  which grouping tasks update which expert events
  how many GroupGEMM tiles each expert needs
</code></pre></div></div> <p>Event Tensor representation:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E_expert[e]:
  event for expert e's grouped tokens being ready

topk[token, k]:
  runtime selected expert

exp_count[e]:
  runtime number of tokens routed to expert e

exp_indptr[e]:
  prefix sum of GroupGEMM tiles per expert
</code></pre></div></div> <p>Runtime initialization:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">experts</span><span class="p">:</span>
    <span class="n">E_expert</span><span class="p">[</span><span class="n">e</span><span class="p">].</span><span class="n">counter</span> <span class="o">=</span> <span class="n">exp_count</span><span class="p">[</span><span class="n">e</span><span class="p">]</span>
</code></pre></div></div> <p>Grouping tasks:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">token</span> <span class="ow">in</span> <span class="n">tokens</span><span class="p">:</span>
    <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">top_k</span><span class="p">):</span>
        <span class="n">e</span> <span class="o">=</span> <span class="n">topk</span><span class="p">[</span><span class="n">token</span><span class="p">,</span> <span class="n">k</span><span class="p">]</span>
        <span class="nf">group_token_for_expert</span><span class="p">(</span><span class="n">token</span><span class="p">,</span> <span class="n">e</span><span class="p">)</span>
        <span class="nf">notify</span><span class="p">(</span><span class="n">E_expert</span><span class="p">[</span><span class="n">e</span><span class="p">])</span>
</code></pre></div></div> <p>Triggering GroupGEMM:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">on_expert_ready</span><span class="p">(</span><span class="n">e</span><span class="p">):</span>
    <span class="n">begin</span> <span class="o">=</span> <span class="n">exp_indptr</span><span class="p">[</span><span class="n">e</span><span class="p">]</span>
    <span class="n">end</span> <span class="o">=</span> <span class="n">exp_indptr</span><span class="p">[</span><span class="n">e</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">tile_id</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">begin</span><span class="p">,</span> <span class="n">end</span><span class="p">):</span>
        <span class="nf">push_task</span><span class="p">(</span><span class="n">GroupGEMM</span><span class="p">,</span> <span class="n">tile_id</span><span class="p">)</span>
</code></pre></div></div> <p>This is the core abstraction win. The same Event Tensor can encode:</p> <ul> <li>static edges like <code class="language-plaintext highlighter-rouge">ij -&gt; i</code>;</li> <li>symbolic-shape event grids like <code class="language-plaintext highlighter-rouge">(B, H)</code>;</li> <li>data-dependent updates like <code class="language-plaintext highlighter-rouge">i -&gt; topk[i, :]</code>;</li> <li>data-dependent triggers like <code class="language-plaintext highlighter-rouge">e -&gt; range(exp_indptr[e], exp_indptr[e + 1])</code>.</li> </ul> <p>Without this abstraction, every MoE megakernel needs custom scheduling code.</p> <h2 id="lowering-to-a-minimal-runtime">Lowering To A Minimal Runtime</h2> <p>The paper emphasizes that ETC does not need a heavy task-graph runtime.</p> <p>Runtime state:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. integer tensors for Event Tensor counters
2. task queue data structures for dynamic scheduling
3. precomputed execution queues for static scheduling
</code></pre></div></div> <p>The dependency graph is compiled into control flow. There is no generic runtime walking a fully materialized graph of millions of nodes.</p> <p>This matters because tile-level LLM task graphs can be huge. If every tile and every edge were separately materialized, graph management overhead could dominate.</p> <p>The compiled-in model:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Event Tensor IR:
  compact symbolic dependencies

compiler lowering:
  wait/notify/push/pop inserted into persistent kernel

runtime:
  counters and queues only
</code></pre></div></div> <h2 id="end-to-end-compilation-flow">End-To-End Compilation Flow</h2> <p>The paper’s system is the Event Tensor Compiler, or ETC. It is implemented as compiler passes on top of Apache TVM. The abstraction itself is intended to be compiler-agnostic and could be integrated into stacks such as Triton or CuteDSL.</p> <p>The pipeline:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Start from a computational graph with tiled operators.
   Device functions are already partitioned into CTA-level tasks.

2. Add Event Tensors and in_edges/out_edges.
   These encode fine-grained dependencies between task grids.

3. Run graph-level optimizations.
   This includes standard graph optimization and memory planning.

4. Run tile-level optimizations.
   This decides low-level instruction mapping, pipelining, and operator details.

5. Choose a scheduling transformation.
   Static: precompute per-SM queues.
   Dynamic: insert scheduler push/pop logic.

6. Emit a persistent GPU kernel.
   The fused device function dispatches all tile kinds inside one kernel.

7. Add weight-prefetching pass.
   If the task's weights are known before input activation arrives, prefetch them while waiting.

8. For static scheduling, materialize the per-SM task order.
</code></pre></div></div> <p>The important assumption is step 1: ETC starts from tiled operators. It is not yet automatically discovering every possible tile partition from a high-level PyTorch graph. The conclusion says future work could generate Event Tensor task graphs from standard computational graphs.</p> <h2 id="how-to-replicate-the-system-from-scratch">How To Replicate The System From Scratch</h2> <p>This section is not all explicitly specified by the paper. It is a practical reconstruction plan based on the paper’s abstraction and pseudocode.</p> <h3 id="step-1-build-a-tiny-event-tensor-ir">Step 1: Build A Tiny Event Tensor IR</h3> <p>Implement:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">SymVar</span>
<span class="n">Tensor</span>
<span class="n">EventTensor</span>
<span class="n">TaskGrid</span>
<span class="n">EdgeMap</span>
<span class="n">GraphFunc</span>
</code></pre></div></div> <p>Required APIs:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">graph</span><span class="p">.</span><span class="nf">add_event</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">shape</span><span class="p">,</span> <span class="n">wait_count</span><span class="p">)</span>
<span class="n">graph</span><span class="p">.</span><span class="nf">call_device</span><span class="p">(</span><span class="n">fn</span><span class="p">,</span> <span class="n">tile_num</span><span class="p">,</span> <span class="n">args</span><span class="p">,</span> <span class="n">in_edges</span><span class="p">,</span> <span class="n">out_edges</span><span class="p">)</span>
<span class="nf">edge_map</span><span class="p">(</span><span class="n">task_coord</span><span class="p">,</span> <span class="n">runtime_tensors</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">event_coord_or_range</span>
</code></pre></div></div> <p>Represent edge maps in two forms:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>static affine/einsum maps:
  "ij-&gt;i"
  "bh-&gt;bh"
  "bh-&gt;u"

general lambda maps:
  lambda i, k: topk[i, k]
  lambda e: range(exp_indptr[e], exp_indptr[e + 1])
</code></pre></div></div> <p>For a first prototype, do not implement a full parser. Store Python callbacks or a small structured map object.</p> <h3 id="step-2-implement-counter-initialization">Step 2: Implement Counter Initialization</h3> <p>For each Event Tensor element, compute the number of producers that must notify it.</p> <p>Static case:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">event_index</span> <span class="ow">in</span> <span class="n">E</span><span class="p">.</span><span class="n">shape</span><span class="p">:</span>
    <span class="n">E</span><span class="p">.</span><span class="n">counter</span><span class="p">[</span><span class="n">event_index</span><span class="p">]</span> <span class="o">=</span> <span class="nf">count_producers_mapping_to</span><span class="p">(</span><span class="n">event_index</span><span class="p">)</span>
</code></pre></div></div> <p>For <code class="language-plaintext highlighter-rouge">"ij-&gt;i"</code> with tile shape <code class="language-plaintext highlighter-rouge">(n, 4)</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">E</span><span class="p">.</span><span class="n">counter</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="mi">4</span>
</code></pre></div></div> <p>Dynamic case:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">E</span><span class="p">.</span><span class="n">counter</span><span class="p">[</span><span class="n">e</span><span class="p">]</span> <span class="o">=</span> <span class="n">runtime_exp_count</span><span class="p">[</span><span class="n">e</span><span class="p">]</span>
</code></pre></div></div> <p>For MoE, this count can be computed from <code class="language-plaintext highlighter-rouge">topk</code>.</p> <h3 id="step-3-implement-notify-and-wait-intrinsics">Step 3: Implement Notify And Wait Intrinsics</h3> <p>Define compiler intrinsics:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">event_notify</span><span class="p">(</span><span class="n">E</span><span class="p">,</span> <span class="n">index</span><span class="p">)</span>
<span class="nf">event_wait</span><span class="p">(</span><span class="n">E</span><span class="p">,</span> <span class="n">index</span><span class="p">)</span>
<span class="nf">event_trigger</span><span class="p">(</span><span class="n">E</span><span class="p">,</span> <span class="n">index</span><span class="p">)</span>
</code></pre></div></div> <p>Lower them to CUDA-like code:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__device__</span> <span class="kt">void</span> <span class="nf">event_notify</span><span class="p">(</span><span class="kt">int</span><span class="o">*</span> <span class="n">counter</span><span class="p">,</span> <span class="kt">int</span> <span class="n">idx</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">old</span> <span class="o">=</span> <span class="n">atomicSubRelease</span><span class="p">(</span><span class="o">&amp;</span><span class="n">counter</span><span class="p">[</span><span class="n">idx</span><span class="p">],</span> <span class="mi">1</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">old</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// event complete</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="n">__device__</span> <span class="kt">void</span> <span class="nf">event_wait</span><span class="p">(</span><span class="kt">int</span><span class="o">*</span> <span class="n">counter</span><span class="p">,</span> <span class="kt">int</span> <span class="n">idx</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">atomicLoadAcquire</span><span class="p">(</span><span class="o">&amp;</span><span class="n">counter</span><span class="p">[</span><span class="n">idx</span><span class="p">])</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">__nanosleep</span><span class="p">(</span><span class="mi">8</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>If release/acquire atomics are not available in your prototype stack, use conservative fences:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__threadfence</span><span class="p">();</span>
<span class="n">atomicSub</span><span class="p">(</span><span class="o">&amp;</span><span class="n">counter</span><span class="p">[</span><span class="n">idx</span><span class="p">],</span> <span class="mi">1</span><span class="p">);</span>

<span class="k">while</span> <span class="p">(</span><span class="n">atomicAdd</span><span class="p">(</span><span class="o">&amp;</span><span class="n">counter</span><span class="p">[</span><span class="n">idx</span><span class="p">],</span> <span class="mi">0</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{}</span>
<span class="n">__threadfence</span><span class="p">();</span>
</code></pre></div></div> <p>This is slower but useful for correctness-first prototyping.</p> <h3 id="step-4-implement-a-static-scheduler">Step 4: Implement A Static Scheduler</h3> <p>For every task grid, enumerate concrete tasks for the selected shape.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tasks</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">task_grid</span> <span class="ow">in</span> <span class="n">graph</span><span class="p">.</span><span class="n">task_grids</span><span class="p">:</span>
    <span class="k">for</span> <span class="n">coord</span> <span class="ow">in</span> <span class="nf">product</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">dim</span><span class="p">)</span> <span class="k">for</span> <span class="n">dim</span> <span class="ow">in</span> <span class="nf">materialize</span><span class="p">(</span><span class="n">task_grid</span><span class="p">.</span><span class="n">tile_shape</span><span class="p">)):</span>
        <span class="n">tasks</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">TaskInstance</span><span class="p">(</span><span class="n">task_grid</span><span class="p">,</span> <span class="n">coord</span><span class="p">))</span>
</code></pre></div></div> <p>Use round-robin per-SM queues:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">queues</span> <span class="o">=</span> <span class="p">[[]</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_sms</span><span class="p">)]</span>
<span class="k">for</span> <span class="n">k</span><span class="p">,</span> <span class="n">task</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">tasks</span><span class="p">):</span>
    <span class="n">queues</span><span class="p">[</span><span class="n">k</span> <span class="o">%</span> <span class="n">num_sms</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="n">task</span><span class="p">)</span>
</code></pre></div></div> <p>Emit one persistent kernel:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="p">(</span><span class="n">cursor</span> <span class="o">&lt;</span> <span class="n">queue_len</span><span class="p">[</span><span class="n">sm_id</span><span class="p">])</span> <span class="p">{</span>
    <span class="n">Task</span> <span class="n">t</span> <span class="o">=</span> <span class="n">queue</span><span class="p">[</span><span class="n">sm_id</span><span class="p">][</span><span class="n">cursor</span><span class="o">++</span><span class="p">];</span>

    <span class="k">for</span> <span class="n">each</span> <span class="n">input</span> <span class="n">edge</span><span class="o">:</span>
        <span class="n">idx</span> <span class="o">=</span> <span class="n">eval_edge_map</span><span class="p">(</span><span class="n">edge</span><span class="p">,</span> <span class="n">t</span><span class="p">.</span><span class="n">coord</span><span class="p">);</span>
        <span class="n">event_wait</span><span class="p">(</span><span class="n">edge</span><span class="p">.</span><span class="n">E</span><span class="p">,</span> <span class="n">idx</span><span class="p">);</span>

    <span class="n">dispatch_task</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>

    <span class="k">for</span> <span class="n">each</span> <span class="n">output</span> <span class="n">edge</span><span class="o">:</span>
        <span class="n">idx</span> <span class="o">=</span> <span class="n">eval_edge_map</span><span class="p">(</span><span class="n">edge</span><span class="p">,</span> <span class="n">t</span><span class="p">.</span><span class="n">coord</span><span class="p">);</span>
        <span class="n">event_notify</span><span class="p">(</span><span class="n">edge</span><span class="p">.</span><span class="n">E</span><span class="p">,</span> <span class="n">idx</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>This already replicates the paper’s basic static scheduling idea.</p> <h3 id="step-5-implement-a-dynamic-scheduler">Step 5: Implement A Dynamic Scheduler</h3> <p>Start with a simple global MPMC queue in GPU global memory:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">Queue</span> <span class="p">{</span>
    <span class="n">Task</span><span class="o">*</span> <span class="n">buffer</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">head</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">tail</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div> <p>A simple prototype can use atomic increments:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">bool</span> <span class="nf">push</span><span class="p">(</span><span class="n">Queue</span><span class="o">*</span> <span class="n">q</span><span class="p">,</span> <span class="n">Task</span> <span class="n">t</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">pos</span> <span class="o">=</span> <span class="n">atomicAdd</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="o">-&gt;</span><span class="n">tail</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
    <span class="n">q</span><span class="o">-&gt;</span><span class="n">buffer</span><span class="p">[</span><span class="n">pos</span><span class="p">]</span> <span class="o">=</span> <span class="n">t</span><span class="p">;</span>
    <span class="k">return</span> <span class="nb">true</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">bool</span> <span class="nf">pop</span><span class="p">(</span><span class="n">Queue</span><span class="o">*</span> <span class="n">q</span><span class="p">,</span> <span class="n">Task</span><span class="o">*</span> <span class="n">out</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">pos</span> <span class="o">=</span> <span class="n">atomicAdd</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="o">-&gt;</span><span class="n">head</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">pos</span> <span class="o">&gt;=</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">tail</span><span class="p">)</span> <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
    <span class="o">*</span><span class="n">out</span> <span class="o">=</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">buffer</span><span class="p">[</span><span class="n">pos</span><span class="p">];</span>
    <span class="k">return</span> <span class="nb">true</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>This is not production-grade because <code class="language-plaintext highlighter-rouge">tail</code> visibility and empty-queue races need careful handling. For a robust version:</p> <ul> <li>use fixed-capacity ring buffers with sequence numbers;</li> <li>or one queue per SM/CTA plus work stealing;</li> <li>or a centralized queue with CAS-protected head/tail and memory fences.</li> </ul> <p>For functional replication, the first version is enough if tasks are seeded conservatively and queue capacity is large.</p> <p>Dynamic kernel skeleton:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">done</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">Task</span> <span class="n">t</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">pop</span><span class="p">(</span><span class="n">global_queue</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">t</span><span class="p">))</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">all_tasks_done</span><span class="p">())</span> <span class="k">break</span><span class="p">;</span>
        <span class="n">backoff</span><span class="p">();</span>
        <span class="k">continue</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="k">for</span> <span class="n">each</span> <span class="n">input</span> <span class="n">edge</span><span class="o">:</span>
        <span class="n">event_wait</span><span class="p">(</span><span class="n">edge</span><span class="p">.</span><span class="n">E</span><span class="p">,</span> <span class="n">eval_edge_map</span><span class="p">(</span><span class="n">edge</span><span class="p">,</span> <span class="n">t</span><span class="p">.</span><span class="n">coord</span><span class="p">));</span>

    <span class="n">dispatch_task</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>

    <span class="k">for</span> <span class="n">each</span> <span class="n">output</span> <span class="n">edge</span><span class="o">:</span>
        <span class="n">idx</span> <span class="o">=</span> <span class="n">eval_edge_map</span><span class="p">(</span><span class="n">edge</span><span class="p">,</span> <span class="n">t</span><span class="p">.</span><span class="n">coord</span><span class="p">);</span>
        <span class="n">old</span> <span class="o">=</span> <span class="n">atomicSub</span><span class="p">(</span><span class="o">&amp;</span><span class="n">edge</span><span class="p">.</span><span class="n">E</span><span class="p">.</span><span class="n">counter</span><span class="p">[</span><span class="n">idx</span><span class="p">],</span> <span class="mi">1</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">old</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">push_consumers</span><span class="p">(</span><span class="n">edge</span><span class="p">.</span><span class="n">E</span><span class="p">,</span> <span class="n">idx</span><span class="p">);</span>
        <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">push_consumers(E, idx)</code> needs an inverse mapping from event coordinates to consumer tasks. For static maps, precompute this inverse relation. For dynamic maps, compute it using runtime tensors.</p> <h3 id="step-6-implement-dispatch">Step 6: Implement Dispatch</h3> <p>A persistent kernel needs to dispatch among task kinds:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">switch</span> <span class="p">(</span><span class="n">task</span><span class="p">.</span><span class="n">kind</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">case</span> <span class="n">TASK_Q_NORM</span><span class="p">:</span>
        <span class="n">q_norm_tile</span><span class="p">(</span><span class="n">task</span><span class="p">.</span><span class="n">coord</span><span class="p">,</span> <span class="n">args</span><span class="p">...);</span>
        <span class="k">break</span><span class="p">;</span>
    <span class="k">case</span> <span class="n">TASK_ROPE</span><span class="p">:</span>
        <span class="n">rope_tile</span><span class="p">(</span><span class="n">task</span><span class="p">.</span><span class="n">coord</span><span class="p">,</span> <span class="n">args</span><span class="p">...);</span>
        <span class="k">break</span><span class="p">;</span>
    <span class="k">case</span> <span class="n">TASK_GEMM</span><span class="p">:</span>
        <span class="n">gemm_tile</span><span class="p">(</span><span class="n">task</span><span class="p">.</span><span class="n">coord</span><span class="p">,</span> <span class="n">args</span><span class="p">...);</span>
        <span class="k">break</span><span class="p">;</span>
    <span class="k">case</span> <span class="n">TASK_REDUCE_SCATTER</span><span class="p">:</span>
        <span class="n">reduce_scatter_tile</span><span class="p">(</span><span class="n">task</span><span class="p">.</span><span class="n">coord</span><span class="p">,</span> <span class="n">args</span><span class="p">...);</span>
        <span class="k">break</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>This is where operator kernels live. Event Tensor does not replace the need for good tile kernels. It coordinates them.</p> <h3 id="step-7-implement-shape-dynamism">Step 7: Implement Shape Dynamism</h3> <p>At compile time:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">compile</span> <span class="n">persistent</span> <span class="n">kernel</span> <span class="k">with</span> <span class="n">symbolic</span> <span class="n">dimensions</span>
</code></pre></div></div> <p>At runtime:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">shape_env</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">B</span><span class="sh">"</span><span class="p">:</span> <span class="n">actual_batch</span><span class="p">,</span> <span class="sh">"</span><span class="s">n</span><span class="sh">"</span><span class="p">:</span> <span class="n">actual_num_blocks</span><span class="p">}</span>
<span class="n">materialize</span> <span class="n">event</span> <span class="n">counters</span> <span class="n">using</span> <span class="n">shape_env</span>
<span class="n">materialize</span> <span class="n">static</span> <span class="n">queue</span> <span class="k">for</span> <span class="nb">next</span> <span class="n">larger</span> <span class="n">sampled</span> <span class="n">shape</span><span class="p">,</span> <span class="ow">or</span> <span class="n">dynamic</span> <span class="n">ready</span> <span class="n">tasks</span>
<span class="n">launch</span> <span class="n">one</span> <span class="n">persistent</span> <span class="n">kernel</span>
</code></pre></div></div> <p>For static scheduling, sample shape buckets:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">sampled_B</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">128</span><span class="p">]</span>
<span class="n">bucket</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">sampled_B</span> <span class="k">if</span> <span class="n">x</span> <span class="o">&gt;=</span> <span class="n">actual_B</span><span class="p">)</span>
<span class="n">use</span> <span class="n">schedule</span><span class="p">[</span><span class="n">bucket</span><span class="p">]</span>
<span class="n">guard</span> <span class="n">tasks</span> <span class="n">whose</span> <span class="n">coordinates</span> <span class="n">exceed</span> <span class="n">actual_B</span>
</code></pre></div></div> <p>For dynamic scheduling, seed the queue only with tasks valid under actual runtime shape.</p> <h3 id="step-8-implement-moe-dynamic-dependencies">Step 8: Implement MoE Dynamic Dependencies</h3> <p>Minimum MoE prototype:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">topk</span> <span class="o">=</span> <span class="nf">router</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>                  <span class="c1"># shape [num_tokens, top_k]
</span><span class="n">exp_count</span> <span class="o">=</span> <span class="nf">histogram</span><span class="p">(</span><span class="n">topk</span><span class="p">)</span>            <span class="c1"># shape [num_experts]
</span><span class="n">exp_indptr</span> <span class="o">=</span> <span class="nf">prefix_sum</span><span class="p">(</span><span class="nf">tile_count</span><span class="p">(</span><span class="n">exp_count</span><span class="p">))</span>
</code></pre></div></div> <p>Event setup:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">E_grouped</span> <span class="o">=</span> <span class="nc">EventTensor</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">E_grouped</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">shape</span><span class="o">=</span><span class="p">(</span><span class="n">num_experts</span><span class="p">,),</span>
    <span class="n">wait_count</span><span class="o">=</span><span class="k">lambda</span> <span class="n">e</span><span class="p">:</span> <span class="n">exp_count</span><span class="p">[</span><span class="n">e</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <p>Tasks:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>route/group task:
  coordinate: token_id
  output event: E_grouped[topk[token_id, k]]

groupgemm task:
  coordinate: tile_id
  input event: E_grouped[expert_of_tile(tile_id)]
</code></pre></div></div> <p>Trigger:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">consumers_of_E_grouped</span><span class="p">(</span><span class="n">e</span><span class="p">):</span>
    <span class="k">return</span> <span class="p">[</span>
        <span class="nc">Task</span><span class="p">(</span><span class="n">kind</span><span class="o">=</span><span class="n">GROUPGEMM</span><span class="p">,</span> <span class="n">coord</span><span class="o">=</span><span class="n">tile</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">tile</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">exp_indptr</span><span class="p">[</span><span class="n">e</span><span class="p">],</span> <span class="n">exp_indptr</span><span class="p">[</span><span class="n">e</span> <span class="o">+</span> <span class="mi">1</span><span class="p">])</span>
    <span class="p">]</span>
</code></pre></div></div> <p>Correctness checks:</p> <ul> <li>every routed token is grouped exactly once per selected expert;</li> <li><code class="language-plaintext highlighter-rouge">sum(exp_count) == num_tokens * top_k</code>;</li> <li>every GroupGEMM tile belongs to exactly one expert;</li> <li>scatter uses the inverse permutation from grouping.</li> </ul> <h3 id="step-9-add-weight-prefetching">Step 9: Add Weight Prefetching</h3> <p>The paper says ETC generates weight-prefetching functions based on user annotations. The idea:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>If a task is known before its input activation is ready,
the worker can prefetch the task's weights while waiting on input events.
</code></pre></div></div> <p>Implementation pattern:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Task</span> <span class="n">t</span> <span class="o">=</span> <span class="n">get_next_task</span><span class="p">();</span>

<span class="n">prefetch_weights</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>

<span class="k">for</span> <span class="n">dep</span> <span class="n">in</span> <span class="n">t</span><span class="p">.</span><span class="n">input_events</span><span class="o">:</span>
    <span class="n">event_wait</span><span class="p">(</span><span class="n">dep</span><span class="p">);</span>

<span class="n">run_tile</span><span class="p">(</span><span class="n">t</span><span class="p">);</span>
</code></pre></div></div> <p>This is especially useful when the schedule knows a GEMM tile’s weight block before the activation tile is produced.</p> <h2 id="minimal-correctness-tests">Minimal Correctness Tests</h2> <p>An LLM agent trying to reproduce the system should start with these tests.</p> <h3 id="test-1-split-row-sum">Test 1: Split Row Sum</h3> <p>Goal:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>partial_sum(i, j) -&gt; E[i] -&gt; final_sum(i)
</code></pre></div></div> <p>Checks:</p> <ul> <li>Output equals a reference row sum.</li> <li><code class="language-plaintext highlighter-rouge">final_sum(i)</code> can run before <code class="language-plaintext highlighter-rouge">partial_sum(i+1, *)</code> completes.</li> <li>No <code class="language-plaintext highlighter-rouge">final_sum(i)</code> runs before all four partial sums for <code class="language-plaintext highlighter-rouge">i</code> complete.</li> </ul> <h3 id="test-2-static-gemm--reduce-scatter-mock">Test 2: Static GEMM + Reduce-Scatter Mock</h3> <p>Use fake tile functions first:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MM tile sleeps/spins for compute time
RS tile sleeps/spins for communication time
</code></pre></div></div> <p>Check:</p> <ul> <li>RS tile starts as soon as its dependent MM tiles finish.</li> <li>MM and RS tasks overlap on different SMs.</li> <li>Static queue preserves correctness.</li> </ul> <p>Then replace fake tiles with real GEMM and communication.</p> <h3 id="test-3-dynamic-queue-load-balancing">Test 3: Dynamic Queue Load Balancing</h3> <p>Create tasks with variable runtime:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>some experts get many tokens
some experts get few tokens
</code></pre></div></div> <p>Check:</p> <ul> <li>dynamic scheduler reduces idle time compared with static schedule;</li> <li>queue operations do not lose or duplicate tasks;</li> <li>all tasks complete exactly once.</li> </ul> <h3 id="test-4-moe-routing">Test 4: MoE Routing</h3> <p>Use a small MoE:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>num_tokens = 8
num_experts = 4
top_k = 2
</code></pre></div></div> <p>Check:</p> <ul> <li><code class="language-plaintext highlighter-rouge">exp_count</code> matches <code class="language-plaintext highlighter-rouge">topk</code>;</li> <li>event counters initialize from <code class="language-plaintext highlighter-rouge">exp_count</code>;</li> <li>expert GroupGEMM tasks triggered from <code class="language-plaintext highlighter-rouge">exp_indptr</code>;</li> <li>final output matches a normal multi-kernel MoE reference.</li> </ul> <h3 id="test-5-dynamic-shape-buckets">Test 5: Dynamic Shape Buckets</h3> <p>For static scheduling:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>compile buckets B = [1, 2, 4, 8]
run actual B = 3 using bucket 4
guard invalid tasks
</code></pre></div></div> <p>Check:</p> <ul> <li>output matches reference;</li> <li>tasks for invalid coordinates do not touch memory;</li> <li>no deadlock occurs from counters expecting invalid producers.</li> </ul> <p>This last point is subtle. If a bucket schedule contains more tasks than the actual shape, counter initialization must match the actual shape, or invalid producer tasks must still perform dummy notifies. The cleaner design is to guard both task execution and counter counts from the same runtime shape.</p> <h2 id="evaluation-summary">Evaluation Summary</h2> <p>The paper evaluates ETC on a server with:</p> <ul> <li>8 NVIDIA B200 GPUs connected by NVLink;</li> <li>Ubuntu 24.04;</li> <li>PyTorch 2.8.0;</li> <li>CUDA 13.0;</li> <li>NVIDIA driver 580.82.07.</li> </ul> <p>The baselines include deep learning compilers, specialized libraries, and serving systems. The paper notes that existing megakernel frameworks mainly target single-batch inference and cannot be fairly compared on dynamic-shape or data-dependent workloads.</p> <h3 id="fused-communication-and-computation">Fused Communication And Computation</h3> <p>Workloads:</p> <ul> <li>GEMM + Reduce-Scatter;</li> <li>All-Gather + GEMM;</li> <li>tensor parallel size 8;</li> <li>8192 tokens;</li> <li>MLP configurations derived from Qwen3, LLaMA, Gemma, GPT-3, and related model sizes.</li> </ul> <p>Baselines:</p> <ul> <li>cuBLAS + NCCL sequential execution;</li> <li>TP-Async;</li> <li>Triton Distributed v0.0.2-rc;</li> <li>cuBLASMp.</li> </ul> <p>Scheduling choices:</p> <ul> <li>GEMM + Reduce-Scatter uses dynamic scheduling because communication latency can be unpredictable.</li> <li>All-Gather + GEMM uses static scheduling because ring all-gather has predictable data arrival and DMA does not consume SM compute in the same way.</li> </ul> <p>Headline result:</p> <ul> <li>ETC achieves up to 1.40x speedup over the cuBLAS + NCCL baseline for both workloads.</li> </ul> <p>The claimed reason is fine-grained overlap. Event Tensor lets the compiler break monolithic operations into tile dependencies, so compute and communication resources remain busy instead of waiting at kernel boundaries.</p> <h3 id="moe-layer">MoE Layer</h3> <p>Workload:</p> <ul> <li>complete MoE layer from Qwen3-30B-A3B;</li> <li>128 experts;</li> <li>top-k 8;</li> <li>variable token counts;</li> <li>single B200.</li> </ul> <p>Baselines:</p> <ul> <li>Triton 3.4.0 MoE implementation;</li> <li>FlashInfer 0.2.14.post1.</li> </ul> <p>Scheduling:</p> <ul> <li>ETC uses dynamic scheduling because routing creates irregular task durations and load imbalance.</li> </ul> <p>Headline result:</p> <ul> <li>up to 1.23x speedup over the best baseline at 1024 tokens.</li> </ul> <p>The important part is not just the number. The MoE experiment tests the core data-dependent Event Tensor claim: the whole MoE dataflow can be fused into one megakernel even though routing decisions are runtime values.</p> <h3 id="end-to-end-low-batch-serving">End-To-End Low-Batch Serving</h3> <p>Workloads:</p> <ul> <li>Qwen3-30B-A3B MoE model;</li> <li>Qwen3-32B dense model;</li> <li>Qwen3-32B with tensor parallelism TP=4.</li> </ul> <p>Benchmark setup:</p> <ul> <li>decoding stage;</li> <li>synthetic dataset;</li> <li>prefill length 512;</li> <li>generate 100 output tokens;</li> <li>batch sizes 1 to 128;</li> <li>metric is time per output token, TPOT.</li> </ul> <p>Baselines:</p> <ul> <li>vLLM v0.11.0rc2;</li> <li>SGLang v0.5.3rc0;</li> <li>both use CUDA Graph and torch.compile-style optimizations.</li> </ul> <p>Headline results:</p> <ul> <li>Qwen3-30B-A3B: 1.48x speedup over vLLM and 1.20x over SGLang at batch size 1.</li> <li>Qwen3-32B single GPU: up to 1.15x over vLLM at batch size 1, and up to 1.09x over SGLang at batch size 64.</li> <li>Qwen3-32B TP=4: ETC matches vLLM with speedups from 0.99x to 1.06x; SGLang is faster in some cases due to its optimized CPU scheduler.</li> </ul> <p>The paper says ETC’s compiled megakernels cover the full decoding pipeline: Attention, RoPE, KV-cache, Norm, MLP, and MoE, not just GEMM.</p> <p>The specific optimizations enabled by removing kernel boundaries:</p> <ul> <li>Q’s Norm + RoPE can run concurrently with K’s Norm + RoPE + CacheAppend.</li> <li>GroupGEMM stages in MoE can be pipelined.</li> <li>GEMM stages in MLP can be pipelined.</li> <li>Weight prefetching can begin before input activations are ready.</li> <li>Wave quantization is reduced because SMs are assigned work more smoothly across fused operators.</li> </ul> <h3 id="warmup-overhead">Warmup Overhead</h3> <p>The paper measures engine warmup time for Qwen3-32B, including engine initialization, model loading, JIT compilation, and CUDA Graph capture.</p> <table> <thead> <tr> <th>Method</th> <th style="text-align: right">Warmup Time</th> <th style="text-align: right">Number Of JIT Graph Captures</th> </tr> </thead> <tbody> <tr> <td>SGLang JIT</td> <td style="text-align: right">583 s</td> <td style="text-align: right">51</td> </tr> <tr> <td>vLLM JIT</td> <td style="text-align: right">123 s</td> <td style="text-align: right">67</td> </tr> <tr> <td>ETC AOT</td> <td style="text-align: right">35 s</td> <td style="text-align: right">0</td> </tr> </tbody> </table> <p>ETC also has an offline compilation time of 107 s for Qwen3-32B, but that is not repeated at serving startup. The reason this works is symbolic-shape Event Tensor support: one precompiled shape-generic megakernel can cover dynamic runtime shapes.</p> <h3 id="static-vs-dynamic-scheduling">Static vs Dynamic Scheduling</h3> <p>The paper compares static and dynamic scheduling against an unfused megakernel baseline. The unfused baseline uses the same operator code, but inserts a global synchronization-style event between stages. Therefore the speedups isolate the value of fine-grained Event Tensor dependencies rather than better operator kernels.</p> <p>MoE layer, relative to unfused megakernel:</p> <table> <thead> <tr> <th style="text-align: right">Tokens</th> <th style="text-align: right">Static</th> <th style="text-align: right">Dynamic</th> </tr> </thead> <tbody> <tr> <td style="text-align: right">1</td> <td style="text-align: right">1.03</td> <td style="text-align: right">0.95</td> </tr> <tr> <td style="text-align: right">128</td> <td style="text-align: right">1.02</td> <td style="text-align: right">1.06</td> </tr> <tr> <td style="text-align: right">1024</td> <td style="text-align: right">1.04</td> <td style="text-align: right">1.08</td> </tr> <tr> <td style="text-align: right">4096</td> <td style="text-align: right">1.02</td> <td style="text-align: right">1.03</td> </tr> </tbody> </table> <p>Qwen3-32B TP=4, relative to unfused megakernel:</p> <table> <thead> <tr> <th style="text-align: right">Batch Size</th> <th style="text-align: right">Static</th> <th style="text-align: right">Dynamic</th> </tr> </thead> <tbody> <tr> <td style="text-align: right">1</td> <td style="text-align: right">1.09</td> <td style="text-align: right">0.83</td> </tr> <tr> <td style="text-align: right">16</td> <td style="text-align: right">1.06</td> <td style="text-align: right">0.82</td> </tr> <tr> <td style="text-align: right">32</td> <td style="text-align: right">1.07</td> <td style="text-align: right">0.85</td> </tr> <tr> <td style="text-align: right">128</td> <td style="text-align: right">1.06</td> <td style="text-align: right">0.89</td> </tr> </tbody> </table> <p>Interpretation:</p> <ul> <li>Dynamic scheduling helps irregular MoE once token count is large enough to amortize scheduler overhead.</li> <li>Static scheduling wins on regular dense distributed decode because dynamic queue overhead is too high, especially for remote task queues.</li> <li>Supporting both strategies in one compiler framework is therefore useful.</li> </ul> <h2 id="what-is-actually-new">What Is Actually New?</h2> <p>I think the novelty is easy to misunderstand, so here is the sharp version.</p> <p>Not new by itself:</p> <ul> <li>persistent kernels;</li> <li>semaphore counters;</li> <li>spin-wait synchronization;</li> <li>GPU task queues;</li> <li>static per-SM schedules;</li> <li>dynamic work queues;</li> <li>CUDA Graph alternatives;</li> <li>operator fusion.</li> </ul> <p>New contribution:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Represent fine-grained task synchronization as symbolic-shaped tensors in compiler IR,
then lower that representation into static or dynamic persistent megakernel schedules,
including data-dependent event updates and triggers.
</code></pre></div></div> <p>The Event Tensor is an abstraction that makes existing mechanisms composable and compiler-generatable.</p> <p>The difference is similar to the difference between:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>manual CUDA shared-memory programming
</code></pre></div></div> <p>and</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a compiler IR that explicitly represents memory scopes and layouts
</code></pre></div></div> <p>The low-level mechanisms may already exist, but the IR decides whether a compiler can reason about them systematically.</p> <h2 id="limitations-and-open-questions">Limitations And Open Questions</h2> <h3 id="the-compiler-starts-from-tiled-operators">The Compiler Starts From Tiled Operators</h3> <p>ETC assumes operators are already partitioned into CTA-level tasks, either user-specified through a DSL or provided as compiler builtins. Automatically generating optimal Event Tensor graphs from arbitrary model graphs is future work.</p> <p>This means a full reproduction still needs a good tile-kernel layer:</p> <ul> <li>GEMM tiles;</li> <li>attention tiles;</li> <li>RoPE and Norm tiles;</li> <li>GroupGEMM tiles;</li> <li>communication tiles.</li> </ul> <p>Event Tensor coordinates them, but does not remove the need to implement them.</p> <h3 id="static-scheduling-is-conservative-for-dynamism">Static Scheduling Is Conservative For Dynamism</h3> <p>The paper’s static scheduling support for data-dependent dynamism can fall back to conservative synchronization such as rewriting dependencies to <code class="language-plaintext highlighter-rouge">E[0]</code>. This preserves correctness but can lose parallelism.</p> <p>This is probably why dynamic scheduling is important for MoE.</p> <h3 id="dynamic-scheduling-uses-a-centralized-queue">Dynamic Scheduling Uses A Centralized Queue</h3> <p>The paper says the implementation uses a centralized global-memory queue for simplicity. This may become a bottleneck at larger scale or with very small tasks.</p> <p>Potential future improvements:</p> <ul> <li>per-SM queues;</li> <li>per-cluster queues;</li> <li>work stealing;</li> <li>hierarchical schedulers;</li> <li>specialized queue layouts for predictable producer-consumer patterns.</li> </ul> <h3 id="memory-ordering-details-are-crucial">Memory Ordering Details Are Crucial</h3> <p>The paper describes notify/wait with integer counters and atomics, but a robust reproduction must be careful about memory consistency.</p> <p>A producer must make its output data visible before the event reaches zero. A consumer must not read produced data before acquiring the completed event.</p> <p>This is easy to get wrong in CUDA, especially if different CTAs or SMs communicate through global memory.</p> <h3 id="evaluation-is-strong-but-hardware-specific">Evaluation Is Strong But Hardware-Specific</h3> <p>The main experiments use B200 GPUs. The abstraction is not B200-specific, but performance trade-offs are hardware-dependent:</p> <ul> <li>atomic cost;</li> <li>queue contention;</li> <li>copy engine behavior;</li> <li>tensor core shape support;</li> <li>NVLink and multimem behavior;</li> <li>SM count and occupancy constraints.</li> </ul> <p>Replication on H100 or A100 should preserve the idea, but not necessarily the same speedups.</p> <h3 id="operator-quality-still-matters">Operator Quality Still Matters</h3> <p>The paper notes some cases where ETC trails the best baseline due to engineering factors, such as compiler-generated GEMM tiles being less tuned than cuBLAS and CPU-side overhead in the serving engine.</p> <p>This is important. A megakernel compiler can expose more overlap, but poor tile kernels can erase the benefit.</p> <h2 id="my-take">My Take</h2> <p>This paper is interesting because it identifies a clean compiler abstraction for a messy systems problem.</p> <p>Manual megakernels are powerful but painful. CUDA Graphs are practical but too coarse. Event Tensor sits between them:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fine-grained enough:
  tile-level dependencies, task overlap, dynamic MoE routing

structured enough:
  tensor-shaped IR, symbolic dimensions, compiler transformations

low-level enough:
  lowers to counters, queues, persistent kernels
</code></pre></div></div> <p>The abstraction also fits a bigger trend: LLM serving wants less CPU involvement and more device-side autonomy. MPK-style systems and other megakernel efforts show that one persistent kernel can remove launch overhead. Event Tensor asks the next compiler question: how do we make this programmable and dynamic enough for real serving workloads?</p> <p>For me, the most valuable idea is not “use a counter.” It is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Make synchronization tensor-shaped.
</code></pre></div></div> <p>Once dependencies are shaped like tensors, symbolic shape compilation, dynamic indexing, and task graph transformations become natural compiler problems instead of bespoke CUDA engineering.</p> <h2 id="checklist-for-an-llm-agent-reimplementation">Checklist For An LLM Agent Reimplementation</h2> <p>If an LLM agent had to replicate a small version from this note only, it should implement in this order:</p> <ol> <li>Implement <code class="language-plaintext highlighter-rouge">EventTensor</code>, <code class="language-plaintext highlighter-rouge">TaskGrid</code>, <code class="language-plaintext highlighter-rouge">EdgeMap</code>, and <code class="language-plaintext highlighter-rouge">GraphFunc</code>.</li> <li>Support static edge maps like <code class="language-plaintext highlighter-rouge">"ij-&gt;i"</code> using simple coordinate projection.</li> <li>Lower Event Tensors to integer counters.</li> <li>Implement <code class="language-plaintext highlighter-rouge">notify</code> and <code class="language-plaintext highlighter-rouge">wait</code> with conservative atomics and fences.</li> <li>Build a static round-robin per-SM task queue.</li> <li>Emit a persistent kernel that loops through the static queue.</li> <li>Validate on split row sum.</li> <li>Add a dynamic global ready queue.</li> <li>Push consumer tasks when event counters reach zero.</li> <li>Validate on variable-duration fake tasks.</li> <li>Add runtime edge maps using tensors such as <code class="language-plaintext highlighter-rouge">topk</code> and <code class="language-plaintext highlighter-rouge">exp_indptr</code>.</li> <li>Validate on a toy MoE layer.</li> <li>Add static shape buckets and runtime guards.</li> <li>Add weight prefetch hooks before event waits.</li> <li>Replace fake tile functions with real GEMM, communication, attention, and MoE kernels.</li> <li>Compare against kernel-by-kernel and CUDA Graph baselines.</li> <li>Measure both raw GPU time and end-to-end serving TPOT.</li> <li>Add ablations against an unfused megakernel using identical tile code.</li> </ol> <p>The core thing to preserve is this invariant:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Every consumer task must wait for exactly the Event Tensor elements
that cover the producer tasks whose data it reads.
</code></pre></div></div> <p>Everything else, static queues, dynamic queues, prefetching, and shape buckets, is an optimization around that invariant.</p>]]></content><author><name></name></author><category term="paper-reading"/><category term="paper-reading"/><category term="mlsys"/><category term="gpu"/><category term="compiler"/><category term="megakernel"/><category term="llm-inference"/><summary type="html"><![CDATA[Detailed notes on Event Tensor, a compiler abstraction for dynamic GPU megakernels with symbolic-shape and data-dependent task dependencies.]]></summary></entry><entry><title type="html">Inside Mirage (3) - Megakernel Persistent Runtime</title><link href="https://uv-xiao.github.io/blog/2025/mirage-mpk-runtime/" rel="alternate" type="text/html" title="Inside Mirage (3) - Megakernel Persistent Runtime"/><published>2025-12-27T00:00:00+00:00</published><updated>2025-12-27T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/mirage-mpk-runtime</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/mirage-mpk-runtime/"><![CDATA[<p>This post continues our exploration of <a href="https://github.com/mirage-project/mirage">Mirage</a>, diving into the <strong>Megakernel Persistent Kernel (MPK)</strong> runtime that powers high-performance LLM inference. If the <a href="/blog/2025/mirage-transpiler-cuda/">previous post on the transpiler</a> showed you <em>what</em> code gets generated, this post shows you <em>how</em> that code executes.</p> <blockquote> <p><strong>Related reading:</strong> Check out my <a href="/blog/2025/mirage-mpk-paper/">MPK paper summary</a> for the high-level ideas behind this architecture.</p> </blockquote> <hr/> <h2 id="1-the-megakernel-execution-model">1. The Megakernel Execution Model</h2> <p>Traditional CUDA programming launches separate kernels for each operation. Mirage’s megakernel takes a radically different approach: <strong>launch once, run forever</strong>.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Traditional Model:
+--------+     +--------+     +--------+
|Kernel 1|-&gt;CPU|Kernel 2|-&gt;CPU|Kernel 3|-&gt; ...
+--------+     +--------+     +--------+
   5-10us latency each launch

Megakernel Model:
+-----------------------------------------+
|        Mega-Kernel (Persistent)         |
|   Runs until all tasks complete         |
+-----------------------------------------+
   Single launch, amortized overhead
</code></pre></div></div> <p>The megakernel contains all the fused operations from the transpiler, plus a scheduling runtime that manages their execution.</p> <hr/> <h2 id="2-architecture-overview">2. Architecture Overview</h2> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+-----------------------------------------------------------------------+
|                          Host (Python API)                            |
|  +---------------+  +---------------+  +-----------------------+      |
|  | Kernel Graph  |  | Threadblock   |  | Task Graph Compiler   |      |
|  | (KGraph)      |--|Graph (TBGraph)|--| Fusion &amp; Scheduling   |      |
|  +---------------+  +---------------+  +-----------------------+      |
+-----------------------------------------------------------------------+
                               |
                               v Launch once
+-----------------------------------------------------------------------+
|                        GPU Megakernel                                 |
|                                                                       |
|  +----------+ +----------+ +----------+    +---------------------+    |
|  | Worker 0 | | Worker 1 | | Worker 2 |    | Scheduler SMs       |    |
|  |    SM    | |    SM    | |    SM    |    | +-----+ +-----+     |    |
|  |  +----+  | |  +----+  | |  +----+  |&lt;---| |Sch0 | |Sch1 |     |    |
|  |  |Task|  | |  |Task|  | |  |Task|  |    | +--+--+ +--+--+     |    |
|  |  |Que |  | |  |Que |  | |  |Que |  |    |    |       |        |    |
|  |  +----+  | |  +----+  | |  +----+  |    |  Event    Event     |    |
|  +----------+ +----------+ +----------+    |  Queues   Queues    |    |
|       |             |             |        +---------------------+    |
|       +-------------+-------------+----------------------+            |
|                    Event Counter Array                                |
+-----------------------------------------------------------------------+
</code></pre></div></div> <p>The GPU is partitioned into:</p> <ul> <li><strong>Workers</strong>: Most SMs, executing tensor operations</li> <li><strong>Schedulers</strong>: A few SMs dedicated to task distribution</li> </ul> <p>This separation keeps the workers focused on compute while schedulers handle coordination.</p> <hr/> <h2 id="3-grid-layout-and-role-assignment">3. Grid Layout and Role Assignment</h2> <p>The kernel grid is organized to separate workers from schedulers:</p> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">mirage/include/mirage/persistent_kernel/persistent_kernel.cuh:440-456</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">void</span> <span class="nf">persistent_checker</span><span class="p">(</span><span class="n">RuntimeConfig</span> <span class="n">config</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="k">const</span> <span class="n">num_schedulers</span> <span class="o">=</span>
      <span class="n">config</span><span class="p">.</span><span class="n">num_local_schedulers</span> <span class="o">+</span> <span class="n">config</span><span class="p">.</span><span class="n">num_remote_schedulers</span><span class="p">;</span>
  <span class="kt">int</span> <span class="k">const</span> <span class="n">num_schedulers_per_sm</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">min</span><span class="p">((</span><span class="kt">int</span><span class="p">)</span><span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">/</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">4</span><span class="p">);</span>

  <span class="c1">// Grid layout: [workers | schedulers]</span>
  <span class="n">assert</span><span class="p">(</span><span class="n">gridDim</span><span class="p">.</span><span class="n">x</span> <span class="o">==</span>
         <span class="n">config</span><span class="p">.</span><span class="n">num_workers</span> <span class="o">+</span> <span class="n">num_schedulers</span> <span class="o">/</span> <span class="n">num_schedulers_per_sm</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gridDim.x = num_worker_blocks + num_scheduler_blocks

Block Types:
[0, num_workers-1]              : Worker blocks (1 worker per SM)
[num_workers, gridDim.x-1]      : Scheduler blocks (up to 4 schedulers per SM)
</code></pre></div></div> <p>Each scheduler runs in a single warp (32 threads), allowing up to 4 schedulers per SM to maximize scheduler throughput without wasting too many SMs.</p> <hr/> <h2 id="4-worker-main-loop">4. Worker Main Loop</h2> <p>Workers are the computational workhorses. Here’s the execution flow:</p> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">mirage/include/mirage/persistent_kernel/persistent_kernel.cuh:458-582</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">void</span> <span class="nf">execute_worker</span><span class="p">(</span><span class="n">RuntimeConfig</span> <span class="n">config</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// Task buffering in shared memory</span>
  <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">TASK_DESCS_BUFFER_LENGTH</span> <span class="o">=</span> <span class="mi">16</span><span class="p">;</span>
  <span class="n">__shared__</span> <span class="n">TaskDesc</span> <span class="n">task_descs</span><span class="p">[</span><span class="n">TASK_DESCS_BUFFER_LENGTH</span><span class="p">];</span>
  <span class="n">__shared__</span> <span class="n">TaskId</span> <span class="n">task_ids</span><span class="p">[</span><span class="n">TASK_DESCS_BUFFER_LENGTH</span><span class="p">];</span>
  <span class="n">__shared__</span> <span class="n">TaskId</span> <span class="o">*</span><span class="n">worker_queues</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>  <span class="c1">// Local + remote queue</span>

  <span class="kt">int</span> <span class="k">const</span> <span class="n">worker_id</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>

  <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// PHASE 1: Fetch tasks from queue (batch load up to 16)</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">queue_pos</span> <span class="o">==</span> <span class="n">queue_len</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// Round-robin poll across local and remote queues</span>
      <span class="c1">// Use cp.async for efficient task descriptor loading</span>
      <span class="p">...</span>
    <span class="p">}</span>

    <span class="c1">// PHASE 2: Wait for dependencies (event counters)</span>
    <span class="n">TaskDesc</span> <span class="o">*</span><span class="n">task_desc</span> <span class="o">=</span> <span class="n">task_descs</span> <span class="o">+</span> <span class="n">queue_pos</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">task_desc</span><span class="o">-&gt;</span><span class="n">num_wait_local_events</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// Spin-wait with acquire semantics</span>
      <span class="k">while</span> <span class="p">(</span><span class="n">atomicAdd</span><span class="p">(</span><span class="o">&amp;</span><span class="n">counters</span><span class="p">[</span><span class="n">event_idx</span><span class="p">],</span> <span class="mi">0</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">threshold</span><span class="p">)</span> <span class="p">{}</span>
      <span class="n">__threadfence</span><span class="p">();</span>
    <span class="p">}</span>

    <span class="c1">// PHASE 3: Execute task</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">task_desc</span><span class="o">-&gt;</span><span class="n">task_type</span> <span class="o">==</span> <span class="n">TASK_TERMINATE</span><span class="p">)</span> <span class="p">{</span>
      <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">_execute_task</span><span class="p">(</span><span class="n">task_desc</span><span class="p">,</span> <span class="n">config</span><span class="p">);</span>

    <span class="c1">// PHASE 4: Trigger downstream events</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">task_desc</span><span class="o">-&gt;</span><span class="n">num_trigger_local_events</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">atom_add_release_gpu_u64</span><span class="p">(</span><span class="o">&amp;</span><span class="n">counters</span><span class="p">[</span><span class="n">event_idx</span><span class="p">],</span> <span class="n">delta</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="n">queue_pos</span> <span class="o">+=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="key-design-points">Key Design Points</h3> <p><strong>Batch task loading</strong>: Workers fetch up to 16 tasks at once using <code class="language-plaintext highlighter-rouge">cp.async</code>, hiding memory latency by overlapping descriptor fetches with computation.</p> <p><strong>Double-buffered queues</strong>: Each worker polls two queues (local and remote) to support multi-GPU task dispatch. The round-robin polling prevents starvation.</p> <p><strong>Event-driven synchronization</strong>: Instead of global barriers, tasks wait on specific event counters and trigger events on completion. This enables fine-grained parallelism.</p> <hr/> <h2 id="5-scheduler-main-loop">5. Scheduler Main Loop</h2> <p>Schedulers manage the work distribution. They’re more complex than workers because they handle multiple event types and coordinate task launches.</p> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">mirage/include/mirage/persistent_kernel/persistent_kernel.cuh:737-981</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">void</span> <span class="nf">execute_scheduler</span><span class="p">(</span><span class="n">RuntimeConfig</span> <span class="n">config</span><span class="p">,</span> <span class="kt">int</span> <span class="n">offset</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="k">const</span> <span class="n">num_schedulers</span> <span class="o">=</span>
      <span class="n">config</span><span class="p">.</span><span class="n">num_local_schedulers</span> <span class="o">+</span> <span class="n">config</span><span class="p">.</span><span class="n">num_remote_schedulers</span><span class="p">;</span>
  <span class="kt">int</span> <span class="k">const</span> <span class="n">num_schedulers_per_sm</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">min</span><span class="p">((</span><span class="kt">int</span><span class="p">)</span><span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">/</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">4</span><span class="p">);</span>
  <span class="kt">int</span> <span class="k">const</span> <span class="n">warp_id</span> <span class="o">=</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">/</span> <span class="mi">32</span><span class="p">;</span>

  <span class="c1">// Only first 4 warps run schedulers (one scheduler per warp)</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">%</span> <span class="mi">32</span> <span class="o">==</span> <span class="mi">0</span> <span class="o">&amp;&amp;</span> <span class="n">warp_id</span> <span class="o">&lt;</span> <span class="n">num_schedulers_per_sm</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="k">const</span> <span class="n">sched_id</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">num_schedulers_per_sm</span> <span class="o">+</span> <span class="n">warp_id</span> <span class="o">+</span> <span class="n">offset</span><span class="p">;</span>

    <span class="c1">// Each scheduler manages a subset of workers</span>
    <span class="n">EventId</span> <span class="o">*</span><span class="n">sched_queues</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>       <span class="c1">// Local + global queue</span>
    <span class="kt">int</span> <span class="n">sched_queue_ids</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="kt">long</span> <span class="kt">int</span> <span class="n">my_first_worker</span><span class="p">,</span> <span class="n">my_last_worker</span><span class="p">;</span>

    <span class="c1">// Local schedulers also process events from the global queue</span>
    <span class="n">sched_queues</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="n">config</span><span class="p">.</span><span class="n">sched_queues</span><span class="p">[</span><span class="n">sched_id</span><span class="p">];</span>
    <span class="n">sched_queue_ids</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="n">sched_id</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">sched_id</span> <span class="o">&lt;</span> <span class="n">config</span><span class="p">.</span><span class="n">num_local_schedulers</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">sched_queues</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="n">config</span><span class="p">.</span><span class="n">sched_queues</span><span class="p">[</span><span class="n">num_schedulers</span><span class="p">];</span>  <span class="c1">// Global queue</span>
      <span class="n">sched_queue_ids</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="n">num_schedulers</span><span class="p">;</span>
      <span class="n">get_first_last_ids</span><span class="p">(</span><span class="n">config</span><span class="p">.</span><span class="n">num_workers</span><span class="p">,</span> <span class="n">config</span><span class="p">.</span><span class="n">num_local_schedulers</span><span class="p">,</span>
                         <span class="n">sched_id</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">my_first_worker</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">my_last_worker</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="kt">size_t</span> <span class="n">cur_event_pos</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">};</span>
    <span class="kt">size_t</span> <span class="n">last_event_pos</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">};</span>
    <span class="kt">size_t</span> <span class="n">worker_queue_next_free_task_pos</span><span class="p">[</span><span class="n">MAX_WORKER_PER_SCHEDULER</span><span class="p">];</span>
    <span class="kt">int</span> <span class="n">next_worker</span> <span class="o">=</span> <span class="n">my_first_worker</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">queue_idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="kt">size_t</span> <span class="n">iteration_num</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

    <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// PHASE 1: Poll event queues (round-robin across local and global)</span>
      <span class="k">while</span> <span class="p">(</span><span class="n">cur_event_pos</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">]</span> <span class="o">==</span> <span class="n">last_event_pos</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">])</span> <span class="p">{</span>
        <span class="n">last_event_pos</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">ld_acquire_gpu_u64</span><span class="p">(</span>
            <span class="o">&amp;</span><span class="n">config</span><span class="p">.</span><span class="n">sched_queue_last_ready_event_id</span><span class="p">[</span><span class="n">sched_queue_ids</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">]]);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">cur_event_pos</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">]</span> <span class="o">&lt;</span> <span class="n">last_event_pos</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">])</span> <span class="p">{</span>
          <span class="k">break</span><span class="p">;</span>
        <span class="p">}</span>
        <span class="n">queue_idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">queue_idx</span> <span class="o">==</span> <span class="n">num_sched_queues</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">?</span> <span class="mi">0</span> <span class="o">:</span> <span class="n">queue_idx</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
        <span class="n">__nanosleep</span><span class="p">(</span><span class="mi">10</span><span class="p">);</span>  <span class="c1">// Avoid overwhelming I/O</span>
      <span class="p">}</span>

      <span class="c1">// PHASE 2: Process event</span>
      <span class="n">EventId</span> <span class="n">event_id</span> <span class="o">=</span> <span class="n">ld_relaxed_gpu_u64</span><span class="p">(</span>
          <span class="o">&amp;</span><span class="n">sched_queues</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">][</span><span class="n">cur_event_pos</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">]</span> <span class="o">%</span> <span class="n">config</span><span class="p">.</span><span class="n">per_sched_queue_len</span><span class="p">]);</span>
      <span class="n">EventDesc</span> <span class="n">e</span> <span class="o">=</span> <span class="n">config</span><span class="p">.</span><span class="n">all_events</span><span class="p">[</span><span class="n">event_id</span><span class="p">];</span>

      <span class="c1">// Handle termination</span>
      <span class="k">if</span> <span class="p">(</span><span class="n">is_termination_event</span><span class="p">(</span><span class="n">event_id</span><span class="p">,</span> <span class="n">e</span><span class="p">))</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">my_first_worker</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">my_last_worker</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
          <span class="c1">// Send TASK_TERMINATE to all workers</span>
          <span class="kt">size_t</span> <span class="n">last_task_id</span> <span class="o">=</span> <span class="n">worker_queue_next_free_task_pos</span><span class="p">[</span><span class="n">i</span> <span class="o">-</span> <span class="n">my_first_worker</span><span class="p">]</span><span class="o">++</span><span class="p">;</span>
          <span class="n">st_relaxed_gpu_u64</span><span class="p">(</span><span class="o">&amp;</span><span class="n">config</span><span class="p">.</span><span class="n">worker_queues</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">last_task_id</span> <span class="o">%</span> <span class="n">config</span><span class="p">.</span><span class="n">per_worker_queue_len</span><span class="p">],</span> <span class="mi">0</span><span class="p">);</span>
          <span class="n">atom_add_release_gpu_u64</span><span class="p">(</span><span class="o">&amp;</span><span class="n">config</span><span class="p">.</span><span class="n">worker_queue_last_ready_task_id</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="mi">1</span><span class="p">);</span>
        <span class="p">}</span>
        <span class="k">return</span><span class="p">;</span>
      <span class="p">}</span>

      <span class="c1">// Handle end-of-task-graph (batch boundary for LLM inference)</span>
      <span class="k">if</span> <span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">event_type</span> <span class="o">==</span> <span class="n">EVENT_END_OF_TASK_GRAPH</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">prepare_next_batch</span><span class="p">(</span><span class="n">config</span><span class="p">))</span> <span class="p">{</span>
          <span class="n">terminate_schedulers</span><span class="p">(</span><span class="n">config</span><span class="p">);</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
          <span class="c1">// Launch begin_task_graph for next iteration</span>
          <span class="kt">size_t</span> <span class="n">last_task_id</span> <span class="o">=</span> <span class="n">worker_queue_next_free_task_pos</span><span class="p">[</span><span class="n">next_worker</span> <span class="o">-</span> <span class="n">my_first_worker</span><span class="p">]</span><span class="o">++</span><span class="p">;</span>
          <span class="n">st_relaxed_gpu_u64</span><span class="p">(</span>
              <span class="o">&amp;</span><span class="n">config</span><span class="p">.</span><span class="n">worker_queues</span><span class="p">[</span><span class="n">next_worker</span><span class="p">][</span><span class="n">last_task_id</span> <span class="o">%</span> <span class="n">config</span><span class="p">.</span><span class="n">per_worker_queue_len</span><span class="p">],</span>
              <span class="n">compute_task_id</span><span class="p">(</span><span class="n">iteration_num</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span> <span class="cm">/*begin_task_graph*/</span><span class="p">));</span>
          <span class="n">atom_add_release_gpu_u64</span><span class="p">(</span><span class="o">&amp;</span><span class="n">config</span><span class="p">.</span><span class="n">worker_queue_last_ready_task_id</span><span class="p">[</span><span class="n">next_worker</span><span class="p">],</span> <span class="mi">1</span><span class="p">);</span>
          <span class="n">next_worker</span> <span class="o">=</span> <span class="p">(</span><span class="n">next_worker</span> <span class="o">==</span> <span class="n">my_last_worker</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">?</span> <span class="n">my_first_worker</span> <span class="o">:</span> <span class="n">next_worker</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
        <span class="p">}</span>
      <span class="p">}</span>
      <span class="c1">// Handle massive task launches (split across all local schedulers)</span>
      <span class="k">else</span> <span class="nf">if</span> <span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">event_type</span> <span class="o">==</span> <span class="n">EVENT_LAUNCH_MASSIVE_TASKS</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Each scheduler gets a portion of tasks</span>
        <span class="n">TaskId</span> <span class="n">my_first_task</span><span class="p">,</span> <span class="n">my_last_task</span><span class="p">;</span>
        <span class="n">get_first_last_ids</span><span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">last_task_id</span> <span class="o">-</span> <span class="n">e</span><span class="p">.</span><span class="n">first_task_id</span><span class="p">,</span>
                           <span class="n">config</span><span class="p">.</span><span class="n">num_local_schedulers</span><span class="p">,</span> <span class="n">sched_id</span><span class="p">,</span>
                           <span class="o">&amp;</span><span class="n">my_first_task</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">my_last_task</span><span class="p">);</span>
        <span class="n">my_first_task</span> <span class="o">+=</span> <span class="n">e</span><span class="p">.</span><span class="n">first_task_id</span><span class="p">;</span>
        <span class="n">my_last_task</span> <span class="o">+=</span> <span class="n">e</span><span class="p">.</span><span class="n">first_task_id</span><span class="p">;</span>

        <span class="c1">// Round-robin distribute tasks to workers</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">my_first_task</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">my_last_task</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
          <span class="kt">size_t</span> <span class="n">last_task_id</span> <span class="o">=</span> <span class="n">worker_queue_next_free_task_pos</span><span class="p">[</span><span class="n">next_worker</span> <span class="o">-</span> <span class="n">my_first_worker</span><span class="p">]</span><span class="o">++</span><span class="p">;</span>
          <span class="n">st_relaxed_gpu_u64</span><span class="p">(</span>
              <span class="o">&amp;</span><span class="n">config</span><span class="p">.</span><span class="n">worker_queues</span><span class="p">[</span><span class="n">next_worker</span><span class="p">][</span><span class="n">last_task_id</span> <span class="o">%</span> <span class="n">config</span><span class="p">.</span><span class="n">per_worker_queue_len</span><span class="p">],</span>
              <span class="n">compute_task_id</span><span class="p">(</span><span class="n">iteration_num</span><span class="p">,</span> <span class="n">i</span><span class="p">));</span>
          <span class="n">atom_add_release_gpu_u64</span><span class="p">(</span><span class="o">&amp;</span><span class="n">config</span><span class="p">.</span><span class="n">worker_queue_last_ready_task_id</span><span class="p">[</span><span class="n">next_worker</span><span class="p">],</span> <span class="mi">1</span><span class="p">);</span>
          <span class="n">next_worker</span> <span class="o">=</span> <span class="p">(</span><span class="n">next_worker</span> <span class="o">==</span> <span class="n">my_last_worker</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">?</span> <span class="n">my_first_worker</span> <span class="o">:</span> <span class="n">next_worker</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
        <span class="p">}</span>
      <span class="p">}</span>

      <span class="n">cur_event_pos</span><span class="p">[</span><span class="n">queue_idx</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="scheduler-execution-flow">Scheduler Execution Flow</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+-----------------------------------------------------------------------+
|                   Scheduler Main Loop                                 |
|                                                                       |
|  +------------------------------------------------------------------+ |
|  |  1. Poll Event Queues (round-robin local + global)               | |
|  |     +- ld_acquire_gpu_u64(&amp;sched_queue_last_ready_event_id)      | |
|  +------------------------------------------------------------------+ |
|                              |                                        |
|                              v                                        |
|  +------------------------------------------------------------------+ |
|  |  2. Fetch Event from Queue                                       | |
|  |     +- ld_relaxed_gpu_u64(&amp;sched_queues[queue_idx][pos])         | |
|  +------------------------------------------------------------------+ |
|                              |                                        |
|                              v                                        |
|  +------------------------------------------------------------------+ |
|  |  3. Process Event by Type                                        | |
|  |                                                                  | |
|  |     EVENT_TERMINATE                                              | |
|  |       +- Send TASK_TERMINATE to all workers -&gt; return            | |
|  |                                                                  | |
|  |     EVENT_END_OF_TASK_GRAPH                                      | |
|  |       +- prepare_next_batch() -&gt; launch begin_task_graph         | |
|  |                                                                  | |
|  |     EVENT_LAUNCH_MASSIVE_TASKS                                   | |
|  |       +- Split tasks across schedulers -&gt; round-robin to workers | |
|  +------------------------------------------------------------------+ |
|                              |                                        |
|                              v                                        |
|  +------------------------------------------------------------------+ |
|  |  4. Dispatch Tasks to Workers (round-robin)                      | |
|  |     +- st_relaxed_gpu_u64(&amp;worker_queues[worker_id][pos], task)  | |
|  |     +- atom_add_release_gpu_u64(&amp;last_ready_task_id[w], 1)       | |
|  +------------------------------------------------------------------+ |
+-----------------------------------------------------------------------+
</code></pre></div></div> <h3 id="event-types">Event Types</h3> <table> <thead> <tr> <th>Event Type</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">EVENT_TERMINATE</code></td> <td>Signal all workers to exit</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">EVENT_END_OF_TASK_GRAPH</code></td> <td>Batch boundary - prepare next iteration</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">EVENT_LAUNCH_MASSIVE_TASKS</code></td> <td>Bulk task dispatch across workers</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">EVENT_LAUNCH_DEPENDENT_TASKS</code></td> <td>Tasks depending on previous iteration</td> </tr> </tbody> </table> <hr/> <h2 id="6-event-driven-synchronization">6. Event-Driven Synchronization</h2> <p>The event system is the heart of MPK’s coordination mechanism.</p> <h3 id="event-counter-model">Event Counter Model</h3> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">mirage/include/mirage/persistent_kernel/persistent_kernel.cuh:157-159</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Initialize all event counters to zero</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">config</span><span class="p">.</span><span class="n">num_events</span><span class="p">;</span> <span class="n">i</span> <span class="o">+=</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">all_event_counters</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
<span class="n">__syncthreads</span><span class="p">();</span>
</code></pre></div></div> <h3 id="event-triggering">Event Triggering</h3> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">mirage/include/mirage/persistent_kernel/persistent_kernel.cuh:397-410</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// After task completion, trigger dependent events</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">task_desc</span><span class="o">-&gt;</span><span class="n">num_trigger_local_events</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">EventDesc</span> <span class="n">event_desc</span> <span class="o">=</span> <span class="n">task_desc</span><span class="o">-&gt;</span><span class="n">trigger_local_events</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
  <span class="kt">uint64_t</span> <span class="o">*</span><span class="n">event_counter_ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">all_event_counters</span><span class="p">[</span><span class="n">event_desc</span><span class="p">.</span><span class="n">event_idx</span><span class="p">];</span>

  <span class="c1">// Release semantics: ensure tile data visible before counter increment</span>
  <span class="n">atom_add_release_gpu_u64</span><span class="p">(</span><span class="n">event_counter_ptr</span><span class="p">,</span> <span class="p">(</span><span class="kt">uint64_t</span><span class="p">)</span><span class="n">event_desc</span><span class="p">.</span><span class="n">trigger_delta</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="event-id-encoding">Event ID Encoding</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 64-bit EventId: nvshmem_tag(16) | owner_gpu(16) | event_idx(32)</span>
<span class="n">EVENT_NVSHMEM_TAG</span> <span class="o">=</span> <span class="mh">0x1e00000000000000</span><span class="p">;</span>

<span class="n">__device__</span> <span class="kt">bool</span> <span class="nf">is_nvshmem_event</span><span class="p">(</span><span class="n">EventId</span> <span class="n">id</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">id</span> <span class="o">&amp;</span> <span class="n">EVENT_NVSHMEM_TAG</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>The encoding allows distinguishing local events from cross-GPU NVSHMEM events, enabling efficient routing.</p> <hr/> <h2 id="7-cross-gpu-communication">7. Cross-GPU Communication</h2> <p>For multi-GPU inference, MPK leverages <strong>NVSHMEM symmetric heap</strong> for zero-copy communication.</p> <h3 id="architecture">Architecture</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPU 0                                        GPU 1
+--------------------------------------+    +--------------------------------------+
|  Workers                             |    |  Workers                             |
|  +--------------------------------+  |    |  +--------------------------------+  |
|  | Worker 0                       |  |    |  | Worker 0                       |  |
|  |  - Local Queue [0]             |  |    |  |  - Local Queue [0]             |  |
|  |  - Remote Queue [num_workers]  |  |    |  |  - Remote Queue [num_workers]  |  |
|  +--------------------------------+  |    |  +--------------------------------+  |
|                                      |    |                                      |
|  TASK_NVSHMEM_COPY transfers data ---+----+--&gt; Data + signal arrive together    |
|  via nvshmem_putmem_signal()         |    |                                      |
+--------------------------------------+    +--------------------------------------+
                   |                                       |
                   +---------------+-----------------------+
                                   |
                         NVSHMEM Symmetric Heap
            (queues, event counters, data - same address on all GPUs)
</code></pre></div></div> <h3 id="symmetric-heap-allocation">Symmetric Heap Allocation</h3> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">persistent_kernel.cuh:1005-1012</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span> <span class="o">&lt;</span><span class="k">typename</span> <span class="nc">DT</span><span class="p">&gt;</span>
<span class="n">DT</span> <span class="o">*</span><span class="nf">gpu_malloc</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">size</span><span class="p">)</span> <span class="p">{</span>
<span class="cp">#ifdef USE_NVSHMEM
</span>  <span class="n">dst_ptr</span> <span class="o">=</span> <span class="n">nvshmem_malloc</span><span class="p">(</span><span class="n">size</span><span class="p">);</span>  <span class="c1">// Symmetric heap - same address on all GPUs</span>
<span class="cp">#else
</span>  <span class="n">cudaMalloc</span><span class="p">(</span><span class="o">&amp;</span><span class="n">dst_ptr</span><span class="p">,</span> <span class="n">size</span><span class="p">);</span>
<span class="cp">#endif
</span>  <span class="k">return</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">DT</span> <span class="o">*&gt;</span><span class="p">(</span><span class="n">dst_ptr</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="cross-gpu-data-transfer">Cross-GPU Data Transfer</h3> <p>The <code class="language-plaintext highlighter-rouge">TASK_NVSHMEM_COPY</code> task uses <code class="language-plaintext highlighter-rouge">nvshmem_putmem_signal</code> to <strong>atomically</strong> transfer data AND signal completion:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// TASK_NVSHMEM_COPY internally does:</span>
<span class="n">nvshmem_putmem_signal</span><span class="p">(</span>
    <span class="n">remote_data_ptr</span><span class="p">,</span>           <span class="c1">// Destination on remote GPU (symmetric address)</span>
    <span class="n">local_data_ptr</span><span class="p">,</span>            <span class="c1">// Source on local GPU</span>
    <span class="n">size_in_bytes</span><span class="p">,</span>
    <span class="o">&amp;</span><span class="n">remote_event_counter</span><span class="p">,</span>     <span class="c1">// Signal location on remote GPU</span>
    <span class="n">signal_value</span><span class="p">,</span>              <span class="c1">// Value to add</span>
    <span class="n">NVSHMEM_SIGNAL_ADD</span><span class="p">,</span>
    <span class="n">target_pe</span>                  <span class="c1">// Target GPU rank</span>
<span class="p">);</span>
<span class="c1">// This atomically: (1) copies data, (2) signals remote event counter</span>
</code></pre></div></div> <h3 id="nvshmem-event-waiting">NVSHMEM Event Waiting</h3> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">persistent_kernel.cuh:596-610</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#ifdef MIRAGE_USE_NVSHMEM
</span><span class="c1">// Wait for remote tile data</span>
<span class="n">nvshmem_signal_wait_until</span><span class="p">(</span>
    <span class="o">&amp;</span><span class="n">all_event_counters</span><span class="p">[</span><span class="n">event_desc</span><span class="p">.</span><span class="n">event_idx</span><span class="p">],</span>
    <span class="n">NVSHMEM_CMP_GE</span><span class="p">,</span>
    <span class="p">(</span><span class="kt">uint64_t</span><span class="p">)</span><span class="n">event_desc</span><span class="p">.</span><span class="n">wait_threshold</span><span class="p">);</span>
<span class="cp">#endif
</span></code></pre></div></div> <h3 id="cross-gpu-communication-flow">Cross-GPU Communication Flow</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPU 0 Worker                          GPU 1 Worker
     |                                      |
     | Execute TASK_NVSHMEM_COPY            | Waiting on nvshmem_signal_wait_until()
     |     |                                |         |
     |     +-&gt; nvshmem_putmem_signal() -----+--------&gt;|
     |     |   (data + signal atomically)   |         |
     |     |                                |    Event counter incremented
     |                                      |         |
     |                                      |    &lt;----+ Wait satisfied
     |                                      |
     |                                      | Execute dependent task
</code></pre></div></div> <hr/> <h2 id="8-task-implementation">8. Task Implementation</h2> <p>Megakernel tasks are <strong>hand-written CUDA templates</strong>, not generated code. The transpiler only extracts shape parameters to instantiate these templates.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Two Separate Systems in Mirage:

+-----------------------------------------------------------------------+
|  TBGraph / STensor (Transpiler Path)                                  |
|  - Used for STANDALONE kernel code generation                         |
|  - NOT used by megakernel runtime                                     |
+-----------------------------------------------------------------------+
                             |
                             | Extract shapes only
                             v
+-----------------------------------------------------------------------+
|  Megakernel Tasks (Runtime Path)                                      |
|  - Hand-written CUDA templates in tasks/ampere/*.cuh, hopper/*.cuh    |
|  - TaskRegister instantiates templates with extracted dimensions      |
|  - Executed by worker SMs at runtime                                  |
+-----------------------------------------------------------------------+
</code></pre></div></div> <h3 id="example-linear-layer">Example: Linear Layer</h3> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">mirage/include/mirage/persistent_kernel/tasks/ampere/linear.cuh</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Hand-written CUDA template - NOT generated code</span>
<span class="k">template</span> <span class="o">&lt;</span><span class="k">typename</span> <span class="nc">T</span><span class="p">,</span>
          <span class="kt">int</span> <span class="n">BATCH_SIZE</span><span class="p">,</span>
          <span class="kt">int</span> <span class="n">OUTPUT_SIZE</span><span class="p">,</span>
          <span class="kt">int</span> <span class="n">REDUCTION_SIZE</span><span class="p">,</span>
          <span class="kt">int</span> <span class="n">O_STRIDE</span> <span class="o">=</span> <span class="n">OUTPUT_SIZE</span><span class="p">,</span>
          <span class="kt">int</span> <span class="n">PIPE_MAX</span> <span class="o">=</span> <span class="mi">3</span><span class="p">&gt;</span>
<span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">void</span> <span class="nf">linear_kernel</span><span class="p">(</span><span class="kt">void</span> <span class="k">const</span> <span class="o">*</span><span class="n">input_ptr</span><span class="p">,</span>
                                              <span class="kt">void</span> <span class="k">const</span> <span class="o">*</span><span class="n">weight_ptr</span><span class="p">,</span>
                                              <span class="kt">void</span> <span class="k">const</span> <span class="o">*</span><span class="n">residual_ptr</span><span class="p">,</span>
                                              <span class="kt">void</span> <span class="o">*</span><span class="n">output_ptr</span><span class="p">,</span>
                                              <span class="kt">int</span> <span class="n">num_active_tokens</span><span class="p">,</span>
                                              <span class="kt">bool</span> <span class="n">residual</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// Manual tiling, shared memory management, MMA operations</span>
  <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">TILE_SIZE</span> <span class="o">=</span> <span class="mi">128</span><span class="p">;</span>
  <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">FORLOOP_RANGE</span> <span class="o">=</span> <span class="n">REDUCTION_SIZE</span> <span class="o">/</span> <span class="n">TILE_SIZE</span><span class="p">;</span>
  <span class="c1">// ... hundreds of lines of hand-optimized CUDA</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="taskregister-template-instantiation">TaskRegister: Template Instantiation</h3> <p><strong>File</strong>: <code class="language-plaintext highlighter-rouge">mirage/src/kernel/task_register.cc</code></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">TaskRegister</span><span class="o">::</span><span class="n">register_rmsnorm_task</span><span class="p">(</span><span class="n">threadblock</span><span class="o">::</span><span class="n">Graph</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">bgraph</span><span class="p">,</span>
                                        <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">params</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// Extract dimensions from TBGraph (used as specification)</span>
  <span class="kt">int</span> <span class="n">batch_size</span> <span class="o">=</span> <span class="n">output_ops</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">-&gt;</span><span class="n">output_tensors</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">dim</span><span class="p">[</span><span class="mi">0</span><span class="p">];</span>
  <span class="kt">int</span> <span class="n">hidden_dim</span> <span class="o">=</span> <span class="n">output_ops</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">-&gt;</span><span class="n">output_tensors</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">dim</span><span class="p">[</span><span class="mi">1</span><span class="p">];</span>

  <span class="c1">// Generate function CALL to hand-written template (NOT generated code)</span>
  <span class="n">mirage</span><span class="o">::</span><span class="n">transpiler</span><span class="o">::</span><span class="n">CodeKeeper</span> <span class="n">code</span><span class="p">;</span>
  <span class="n">code</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"kernel::rms_norm_impl&lt;bfloat16, $, $&gt;("</span><span class="p">,</span> <span class="n">batch_size</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">);</span>
  <span class="n">code</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"    task_desc-&gt;input_ptrs[0],"</span><span class="p">);</span>
  <span class="n">code</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"    task_desc-&gt;input_ptrs[1],"</span><span class="p">);</span>
  <span class="n">code</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"    task_desc-&gt;output_ptrs[0],"</span><span class="p">);</span>
  <span class="n">code</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"    1e-6f);"</span><span class="p">);</span>
  <span class="k">return</span> <span class="n">register_task_variant</span><span class="p">(</span><span class="n">TASK_RMS_NORM</span><span class="p">,</span> <span class="n">code</span><span class="p">.</span><span class="n">to_string</span><span class="p">());</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="available-task-types">Available Task Types</h3> <table> <thead> <tr> <th>Task Type</th> <th>Implementation File</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">TASK_LINEAR</code></td> <td><code class="language-plaintext highlighter-rouge">tasks/ampere/linear.cuh</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TASK_RMS_NORM</code></td> <td><code class="language-plaintext highlighter-rouge">tasks/ampere/rmsnorm.cuh</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TASK_SILU_MUL</code></td> <td><code class="language-plaintext highlighter-rouge">tasks/ampere/silu_mul.cuh</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TASK_PAGED_ATTENTION_*</code></td> <td><code class="language-plaintext highlighter-rouge">tasks/ampere/multitoken_paged_attention*.cuh</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TASK_LINEAR_HOPPER</code></td> <td><code class="language-plaintext highlighter-rouge">tasks/hopper/linear_hopper.cuh</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TASK_NVSHMEM_COPY</code></td> <td>Cross-GPU data transfer</td> </tr> </tbody> </table> <h3 id="architecture-specific-implementations">Architecture-Specific Implementations</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>persistent_kernel/tasks/
+-- ampere/      # SM80: CP.ASYNC-based loading
+-- hopper/      # SM90: TMA-based loading
+-- blackwell/   # SM100: New tensor core operations
</code></pre></div></div> <hr/> <h2 id="9-worker-scheduler-communication">9. Worker-Scheduler Communication</h2> <p>The communication flow between workers and schedulers forms a closed loop:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+-----------------------------------------------------------------------+
|              Worker-Scheduler Communication Architecture              |
|                                                                       |
|  Schedulers                              Workers                      |
|  +------------+                         +------------+                |
|  | Scheduler 0|-------------------------| Worker 0  |                |
|  |            |                    +----| Worker 1  |                |
|  +------------+                    |    | Worker 2  |                |
|                                    |    +------------+                |
|  +------------+                    |                                  |
|  | Scheduler 1|--------------------+----| Worker 3  |                |
|  |            |              +-----+----| Worker 4  |                |
|  +------------+              |     |    | Worker 5  |                |
|                              |     |    +------------+                |
|         ^                    |     |          |                       |
|         |                    |     |          |                       |
|    Event Queues         Worker Queues   Event Counters                |
|   (sched_queues)       (worker_queues)  (all_event_counters)          |
|                              |     |          |                       |
|         |                    |     |          v                       |
|    +----+----+          +----+-----+----+                             |
|    | Workers |          |  Schedulers   |                             |
|    | trigger |----------|  dispatch     |                             |
|    | events  |          |  tasks        |                             |
|    +---------+          +---------------+                             |
+-----------------------------------------------------------------------+

Communication Flow:
1. Worker completes task → increments event counter
2. Event counter reaches threshold → event placed in scheduler queue
3. Scheduler processes event → creates TaskId(iteration, task_idx)
4. Scheduler places task in worker queue → worker fetches and executes
</code></pre></div></div> <hr/> <h2 id="10-performance-characteristics">10. Performance Characteristics</h2> <table> <thead> <tr> <th>Aspect</th> <th>Traditional Kernels</th> <th>MPK Runtime</th> </tr> </thead> <tbody> <tr> <td>Kernel launch overhead</td> <td>5-10μs per kernel</td> <td>Amortized (single launch)</td> </tr> <tr> <td>Inter-layer pipelining</td> <td>Not possible</td> <td>Enabled</td> </tr> <tr> <td>Compute/Comm overlap</td> <td>Manual</td> <td>Automatic via events</td> </tr> <tr> <td>GPU utilization</td> <td>Variable (gaps)</td> <td>High (continuous)</td> </tr> <tr> <td>Scheduling</td> <td>Host-side</td> <td>Device-side</td> </tr> </tbody> </table> <p>The MPK runtime shines in scenarios with:</p> <ul> <li><strong>Small batch sizes</strong>: Launch overhead dominates traditional approaches</li> <li><strong>Multi-GPU setups</strong>: NVSHMEM overlap maximizes bandwidth utilization</li> <li><strong>Deep models</strong>: More layers = more opportunities for pipelining</li> </ul> <hr/> <h2 id="11-key-takeaways">11. Key Takeaways</h2> <ol> <li><strong>Persistent execution</strong> eliminates kernel launch overhead by keeping one kernel running</li> <li><strong>Worker-scheduler separation</strong> allows dedicated coordination without impacting compute</li> <li><strong>Event counters</strong> provide lock-free, fine-grained synchronization</li> <li><strong>NVSHMEM integration</strong> enables efficient cross-GPU communication with atomic signals</li> <li><strong>Hand-written task templates</strong> ensure peak performance for critical operations</li> <li><strong>Architecture-specific paths</strong> leverage SM-specific features (TMA, etc.)</li> </ol> <p>The MPK runtime is a fascinating example of bringing operating system concepts (cooperative scheduling, event-driven I/O) into GPU programming. It shows that the traditional host-device model isn’t the only way to program GPUs - sometimes the GPU can be its own scheduler.</p> <hr/> <h2 id="references">References</h2> <ul> <li><a href="https://github.com/mirage-project/mirage">Mirage GitHub Repository</a></li> <li><a href="https://arxiv.org/abs/2512.22219">MPK Paper (arXiv:2512.22219)</a></li> <li><a href="https://docs.nvidia.com/nvshmem/">NVSHMEM Documentation</a></li> <li><a href="https://github.com/NVIDIA/cutlass/blob/main/media/docs/cute/00_quickstart.md">CuTe Documentation</a></li> </ul> <blockquote> <p><strong>Previous:</strong> <a href="/blog/2025/mirage-transpiler-cuda/">Inside Mirage (2) - Transpiler from MuGraph to CUDA</a></p> </blockquote>]]></content><author><name></name></author><category term="mlsys"/><category term="mirage"/><category term="mlsys"/><category term="gpu"/><category term="cuda"/><summary type="html"><![CDATA[Deep dive into Mirage's MPK runtime - worker-scheduler model, event-driven synchronization, and multi-GPU support]]></summary></entry><entry><title type="html">Paper Reading - Mirage Persistent Kernel (MPK)</title><link href="https://uv-xiao.github.io/blog/2025/mirage-mpk-paper/" rel="alternate" type="text/html" title="Paper Reading - Mirage Persistent Kernel (MPK)"/><published>2025-12-26T00:00:00+00:00</published><updated>2025-12-26T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/mirage-mpk-paper</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/mirage-mpk-paper/"><![CDATA[<p>This post summarizes the key ideas from <a href="https://arxiv.org/abs/2512.22219"><strong>Mirage Persistent Kernel: A Compiler and Runtime for Mega-Kernelizing Tensor Programs</strong></a> (arXiv:2512.22219). If you’re interested in how modern LLM inference can push GPU utilization to the limit, this paper offers some genuinely clever solutions.</p> <hr/> <h2 id="the-problem-death-by-a-thousand-kernel-launches">The Problem: Death by a Thousand Kernel Launches</h2> <p>Modern deep learning workloads, especially LLM inference, involve a parade of small tensor operations: layer norms, projections, attention, MLPs. Each operation typically launches a separate CUDA kernel. The result?</p> <ul> <li><strong>Kernel launch overhead</strong>: Each launch costs 5-10 microseconds. That adds up fast.</li> <li><strong>Memory bandwidth waste</strong>: Data gets written to global memory, then read back for the next operation.</li> <li><strong>GPU sitting idle</strong>: Between kernel launches, the GPU twiddles its thumbs waiting for the host.</li> <li><strong>No cross-layer optimization</strong>: Each kernel is optimized in isolation.</li> </ul> <p>The standard solution of “fuse more operations” has limits. At some point, you need a fundamentally different execution model.</p> <h2 id="the-mpk-solution-one-kernel-to-rule-them-all">The MPK Solution: One Kernel to Rule Them All</h2> <p><strong>Mirage Persistent Kernel (MPK)</strong> takes the fusion idea to its logical extreme: fuse the <em>entire inference</em> into a single, persistent mega-kernel that stays resident on the GPU.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Traditional:  [Kernel 1] -&gt; CPU -&gt; [Kernel 2] -&gt; CPU -&gt; [Kernel 3] -&gt; ...
MPK:          [=================== Mega-Kernel ===================]
                              ^ runs until done
</code></pre></div></div> <p>The key insight is that kernel launch overhead isn’t inherent to GPU computation - it’s an artifact of the host-device programming model. By keeping the kernel alive and scheduling work <em>on the GPU</em>, MPK sidesteps this bottleneck entirely.</p> <hr/> <h2 id="system-architecture">System Architecture</h2> <p>MPK introduces a <strong>worker-scheduler model</strong> that lives entirely on the GPU:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+--------------------------------------------------------------+
|                  Mega-Kernel (Persistent)                    |
|  +--------------------------------------------------------+  |
|  |                  Schedulers                            |  |
|  |   (Dedicated warps that manage work distribution)      |  |
|  +------------------------+-------------------------------+  |
|                           |                                  |
|                           v                                  |
|  +--------------------------------------------------------+  |
|  |                  Task Queue                            |  |
|  |   [Task 0] [Task 1] [Task 2] [Task 3] ...              |  |
|  +------------------------+-------------------------------+  |
|                           |                                  |
|                           v                                  |
|  +--------------------------------------------------------+  |
|  |                   Workers                              |  |
|  |  [SM 0] [SM 1] [SM 2] ... (execute actual compute)     |  |
|  +--------------------------------------------------------+  |
+--------------------------------------------------------------+
</code></pre></div></div> <h3 id="workers-and-schedulers">Workers and Schedulers</h3> <p><strong>Workers</strong> (most SMs) execute the actual tensor computations. Each worker:</p> <ol> <li>Polls the task queue for ready work</li> <li>Executes the task (a fused operation from Mirage’s transpiler)</li> <li>Signals completion via event counters</li> <li>Repeats until termination</li> </ol> <p><strong>Schedulers</strong> (a few dedicated warps) manage task distribution:</p> <ol> <li>Monitor event queues for completed dependencies</li> <li>Distribute newly-ready tasks to workers in round-robin fashion</li> <li>Handle cross-layer and cross-iteration boundaries</li> <li>Coordinate multi-GPU communication</li> </ol> <p>This separation keeps the workers focused on compute while schedulers handle the coordination overhead.</p> <h3 id="event-driven-synchronization">Event-Driven Synchronization</h3> <p>Instead of expensive <code class="language-plaintext highlighter-rouge">__syncthreads()</code> or atomic barriers, MPK uses <strong>event counters</strong> for fine-grained synchronization:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Worker completion: increment counter with release semantics</span>
<span class="n">atom_add_release_gpu_u64</span><span class="p">(</span><span class="o">&amp;</span><span class="n">event_counters</span><span class="p">[</span><span class="n">event_id</span><span class="p">],</span> <span class="n">delta</span><span class="p">);</span>

<span class="c1">// Dependency wait: spin until counter reaches threshold</span>
<span class="k">while</span> <span class="p">(</span><span class="n">atomicAdd</span><span class="p">(</span><span class="o">&amp;</span><span class="n">event_counters</span><span class="p">[</span><span class="n">dep_event</span><span class="p">],</span> <span class="mi">0</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">threshold</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">__nanosleep</span><span class="p">(</span><span class="mi">10</span><span class="p">);</span>
<span class="p">}</span>
<span class="n">__threadfence</span><span class="p">();</span>  <span class="c1">// Acquire semantics</span>
</code></pre></div></div> <p>This enables:</p> <ul> <li><strong>Lock-free</strong> task queue operations</li> <li><strong>Fine-grained</strong> inter-task dependencies</li> <li><strong>Minimal overhead</strong> compared to traditional synchronization</li> </ul> <hr/> <h2 id="key-technical-contributions">Key Technical Contributions</h2> <h3 id="1-task-graph-compilation">1. Task Graph Compilation</h3> <p>Rather than CUDA graphs (kernel granularity), MPK compiles at <strong>sub-kernel granularity</strong>. Each task in the task graph represents a fused operation from Mirage’s transpiler:</p> <table> <thead> <tr> <th>Traditional CUDA Graph</th> <th>MPK Task Graph</th> </tr> </thead> <tbody> <tr> <td>Kernel launch = 1 node</td> <td>Fused op = 1 task</td> </tr> <tr> <td>Host-side scheduling</td> <td>Device-side scheduling</td> </tr> <tr> <td>Static dependencies</td> <td>Dynamic event-driven</td> </tr> </tbody> </table> <h3 id="2-inter-layer-pipelining">2. Inter-Layer Pipelining</h3> <p>With device-side scheduling, MPK enables <strong>inter-layer pipelining</strong>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Time -&gt;
Token 0: [RMSNorm][Attn][FFN]
Token 1:    [RMSNorm][Attn][FFN]
Token 2:       [RMSNorm][Attn][FFN]
         +-- GPU continuously utilized --+
</code></pre></div></div> <p>Different tokens flow through different layers simultaneously, maximizing SM utilization.</p> <h3 id="3-computationcommunication-overlap">3. Computation/Communication Overlap</h3> <p>For multi-GPU inference, MPK overlaps compute with NVSHMEM-based communication:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Data transfer task uses nvshmem_putmem_signal</span>
<span class="n">nvshmem_putmem_signal</span><span class="p">(</span>
    <span class="n">remote_ptr</span><span class="p">,</span>        <span class="c1">// Destination on peer GPU</span>
    <span class="n">local_ptr</span><span class="p">,</span>         <span class="c1">// Source data</span>
    <span class="n">size</span><span class="p">,</span>              <span class="c1">// Bytes to transfer</span>
    <span class="o">&amp;</span><span class="n">remote_event</span><span class="p">,</span>     <span class="c1">// Signal on completion</span>
    <span class="n">signal_value</span><span class="p">,</span>
    <span class="n">NVSHMEM_SIGNAL_ADD</span><span class="p">,</span>
    <span class="n">target_pe</span>          <span class="c1">// Peer GPU rank</span>
<span class="p">);</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">putmem_signal</code> atomically transfers data AND signals the remote GPU, enabling tight overlap.</p> <h3 id="4-double-buffered-task-queues">4. Double-Buffered Task Queues</h3> <p>Workers poll from two queues (local + remote) to support cross-GPU task dispatch:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">worker_queues</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="n">local_queue</span><span class="p">[</span><span class="n">worker_id</span><span class="p">];</span>
<span class="k">if</span> <span class="p">(</span><span class="n">num_gpus</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">worker_queues</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="n">remote_queue</span><span class="p">[</span><span class="n">worker_id</span><span class="p">];</span>
<span class="p">}</span>
<span class="c1">// Round-robin poll both queues</span>
</code></pre></div></div> <hr/> <h2 id="performance-results">Performance Results</h2> <p>The paper reports impressive gains over traditional approaches:</p> <table> <thead> <tr> <th>Metric</th> <th>Improvement</th> </tr> </thead> <tbody> <tr> <td>Kernel launch overhead</td> <td>~95% reduction</td> </tr> <tr> <td>Memory bandwidth</td> <td>40-60% reduction</td> </tr> <tr> <td>End-to-end speedup</td> <td>1.2-2.0x (model dependent)</td> </tr> </tbody> </table> <p>The gains are most pronounced for:</p> <ul> <li><strong>Small batch sizes</strong>: Launch overhead dominates</li> <li><strong>Multi-GPU setups</strong>: Communication/compute overlap pays off</li> <li><strong>Deep models</strong>: More layers = more fusion opportunities</li> </ul> <hr/> <h2 id="comparison-with-alternatives">Comparison with Alternatives</h2> <table> <thead> <tr> <th>Approach</th> <th>Scheduling</th> <th>Fusion</th> <th>Multi-GPU</th> </tr> </thead> <tbody> <tr> <td><strong>MPK</strong></td> <td>Device-side, event-driven</td> <td>Automatic (mega-kernel)</td> <td>NVSHMEM events</td> </tr> <tr> <td>CUDA Graphs</td> <td>Host-side, static</td> <td>None</td> <td>Manual</td> </tr> <tr> <td>Triton</td> <td>Host-side</td> <td>Manual</td> <td>Manual</td> </tr> <tr> <td>FlashAttention</td> <td>N/A</td> <td>Single-op</td> <td>N/A</td> </tr> </tbody> </table> <p>MPK’s key differentiation is the <strong>fully on-device execution model</strong> that eliminates host-GPU round trips during inference.</p> <hr/> <h2 id="limitations-and-future-directions">Limitations and Future Directions</h2> <p><strong>Current limitations:</strong></p> <ul> <li>Fusion effectiveness varies by operation mix</li> <li>Control flow within fused kernels is limited</li> <li>Tested up to 4 GPUs (scaling to 8+ is future work)</li> <li>Compile-time task graph (no dynamic shapes)</li> </ul> <p><strong>Interesting future directions:</strong></p> <ul> <li>Adaptive fusion based on runtime profiling</li> <li>Support for more complex control flow</li> <li>Integration with continuous batching for variable-length sequences</li> </ul> <hr/> <h2 id="my-take">My Take</h2> <p>MPK represents a significant step toward treating the GPU as a <strong>truly autonomous compute device</strong> rather than a host-controlled accelerator. The worker-scheduler model is elegant - it’s essentially implementing a cooperative multitasking runtime inside a single CUDA kernel.</p> <p>The event-driven synchronization using atomic counters is particularly clever. It achieves the semantics of condition variables without the overhead, leveraging the fact that GPU memory accesses have predictable latency.</p> <p>For practitioners, the key takeaway is: <strong>kernel launch overhead matters more than you think</strong>, especially as models get faster and batches get smaller. MPK shows one path toward eliminating this overhead entirely.</p> <hr/> <h2 id="references">References</h2> <ul> <li><a href="https://arxiv.org/abs/2512.22219">Paper on arXiv</a></li> <li><a href="https://github.com/mirage-project/mirage">Mirage GitHub Repository</a></li> <li><a href="https://docs.nvidia.com/nvshmem/">NVSHMEM Documentation</a></li> </ul> <blockquote> <p><strong>Next:</strong> For a deep dive into Mirage’s code and implementation details, check out my <a href="/blog/tag/mirage/">Inside Mirage series</a>.</p> </blockquote>]]></content><author><name></name></author><category term="paper-reading"/><category term="paper-reading"/><category term="mlsys"/><category term="gpu"/><category term="cuda"/><summary type="html"><![CDATA[Notes on the Mirage MPK paper - a compiler and runtime for mega-kernelizing tensor programs]]></summary></entry><entry><title type="html">Inside Mirage (2) - Transpiler from MuGraph to CUDA</title><link href="https://uv-xiao.github.io/blog/2025/mirage-transpiler-cuda/" rel="alternate" type="text/html" title="Inside Mirage (2) - Transpiler from MuGraph to CUDA"/><published>2025-11-25T00:00:00+00:00</published><updated>2025-11-25T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/mirage-transpiler-cuda</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/mirage-transpiler-cuda/"><![CDATA[<p>In this blog, we dive deep into <strong>Mirage’s Transpiler</strong>, the component responsible for translating optimized MuGraph representations into efficient CUDA code. The transpiler bridges the gap between the high-level graph representation and low-level GPU execution.</p> <blockquote> <p>This chapter is based on <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00/docs/transpiler/transpiler.md"><code class="language-plaintext highlighter-rouge">docs/transpiler/transpiler.md</code></a>. It is very nice and detailed. I recommend reading it first to get the algorithm insights.</p> </blockquote> <blockquote> <p>For Runtime knowledge, I learned from <a href="https://www.zhihu.com/collection/694786845">Zhihu</a> and <a href="https://mp.weixin.qq.com/s/sVjLJMHGpeSuLFaKOmBeow">Weixin</a>.</p> </blockquote> <blockquote> <p><strong>Note:</strong> This post follows the hands-on <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00">Mirage Tutorial (uv_tutorial_00 branch)</a>.</p> </blockquote> <hr/> <h2 id="1-overview">1. Overview</h2> <p>The transpiler takes a kernel graph (potentially containing custom threadblock graphs) and generates optimized CUDA code. This is both an <strong>algorithmic challenge</strong> (deciding tensor layouts, memory allocation, scheduling) and an <strong>engineering challenge</strong> (maintaining correctness while maximizing performance).</p> <h3 id="11-architecture">1.1 Architecture</h3> <p>The transpiler consists of two major components:</p> <ol> <li><strong>The Transpiler</strong> (<code class="language-plaintext highlighter-rouge">src/transpiler/</code>): Translates MuGraph into CUDA code</li> <li><strong>The Runtime</strong> (<code class="language-plaintext highlighter-rouge">include/mirage/transpiler/runtime/</code>): Header-only library providing optimized kernels for the generated code</li> </ol> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+---------------------+
|   Kernel Graph      |
|   (MuGraph)         |
+---------+-----------+
          |
          v
+---------------------+
|   Transpiler        |
|  +---------------+  |
|  | Fusion        |  |
|  | Resolution    |  |
|  +-------+-------+  |
|          v          |
|  +---------------+  |
|  | Layout        |  |
|  | Resolution    |  |
|  +-------+-------+  |
|          v          |
|  +---------------+  |
|  | TB Graph      |  |
|  | Scheduling    |  |
|  +-------+-------+  |
|          v          |
|  +---------------+  |
|  | Swizzle       |  |
|  | Planning      |  |
|  +-------+-------+  |
|          v          |
|  +---------------+  |
|  | Memory        |  |
|  | Planning      |  |
|  +-------+-------+  |
|          v          |
|  +---------------+  |
|  | Code          |  |
|  | Generation    |  |
|  +---------------+  |
+---------+-----------+
          |
          v
+---------------------+
|   Generated CUDA    |
|   + Runtime Headers |
+---------------------+
</code></pre></div></div> <h3 id="12-source-code-structure">1.2 Source Code Structure</h3> <p>The transpiler source code is organized as follows:</p> <table> <thead> <tr> <th>File</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">transpile.cc</code></td> <td>Main entry point, graph preprocessing</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">resolve_tb_fusion.cc</code></td> <td>Threadblock-level operator fusion</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">resolve_tensor_layout.cc</code></td> <td>Layout resolution using Z3 solver</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">sched_tb_graph.cc</code></td> <td>Threadblock graph scheduling</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">plan_tb_swizzle.cc</code></td> <td>Swizzle planning to avoid bank conflicts</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">plan_dtensor_memory.cc</code></td> <td>Global memory planning for DTensors</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">plan_stensor_memory.cc</code></td> <td>Shared memory planning for STensors</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">transpiler_kn.cc</code></td> <td>Kernel-level code generation</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">transpiler_tb.cc</code></td> <td>Threadblock-level code generation</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">transpiler_tb_hopper.cc</code></td> <td>Hopper-specific code generation</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">transpiler_tb_blackwell.cc</code></td> <td>Blackwell-specific code generation</td> </tr> </tbody> </table> <h3 id="13-transpiler-pipeline">1.3 Transpiler Pipeline</h3> <p>The transpilation process follows these steps (from <code class="language-plaintext highlighter-rouge">transpiler.h</code>):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TranspileResult</span> <span class="nf">generate_code</span><span class="p">()</span> <span class="p">{</span>
  <span class="k">this</span><span class="o">-&gt;</span><span class="n">resolve_distributed_config</span><span class="p">();</span>  <span class="c1">// Multi-GPU configuration</span>
  <span class="k">this</span><span class="o">-&gt;</span><span class="n">resolve_dtensor_meta</span><span class="p">();</span>        <span class="c1">// Gather tensor metadata</span>
  <span class="k">this</span><span class="o">-&gt;</span><span class="n">resolve_tb_fusion</span><span class="p">();</span>           <span class="c1">// Plan operator fusion</span>
  <span class="k">this</span><span class="o">-&gt;</span><span class="n">resolve_tensor_layout</span><span class="p">();</span>       <span class="c1">// Decide tensor layouts</span>
  <span class="k">this</span><span class="o">-&gt;</span><span class="n">plan_dtensor_memory</span><span class="p">();</span>         <span class="c1">// Allocate global memory</span>
  <span class="k">return</span> <span class="k">this</span><span class="o">-&gt;</span><span class="n">transpile_ugraph</span><span class="p">();</span>     <span class="c1">// Generate CUDA code</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="2-threadblock-level-data-fusion">2. Threadblock-Level Data Fusion</h2> <p>The first step is deciding which operators can be fused together to reduce memory traffic.</p> <h3 id="21-the-problem">2.1 The Problem</h3> <p>In threadblock-level code, operators read from shared memory, compute, and write back. Fusion avoids intermediate memory operations:</p> <p><strong>Without Fusion:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Matmul: Read A, B → Compute → Write C to SMEM
Exp:    Read C from SMEM → Compute → Write D to SMEM
</code></pre></div></div> <p><strong>With Fusion:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Matmul+Exp: Read A, B → Compute Matmul → Apply Exp in registers → Write D to SMEM
</code></pre></div></div> <h3 id="22-fusion-rules">2.2 Fusion Rules</h3> <p>Currently, Mirage fuses <strong>unary elementwise operators</strong> with their predecessor when:</p> <ol> <li>The predecessor has exactly one consumer</li> <li>The predecessor is not an input op with <code class="language-plaintext highlighter-rouge">forloop_dim = -1</code></li> <li>The predecessor is not a forloop accumulator (the loop body vs. outside the loop)</li> <li>The predecessor is not a reduction max op (which has two outputs)</li> </ol> <p><strong>Code Reference</strong> (<code class="language-plaintext highlighter-rouge">src/transpiler/resolve_tb_fusion.cc:39-65</code>):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Currently we only fuse elementwise unary operators and</span>
<span class="c1">// forloop_accum_no_red with the previous operator</span>
<span class="k">for</span> <span class="p">(</span><span class="n">tb</span><span class="o">::</span><span class="n">TBOperator</span> <span class="o">*</span><span class="k">const</span> <span class="n">op</span> <span class="o">:</span> <span class="n">tb_graph</span><span class="p">.</span><span class="n">operators</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if</span> <span class="p">((</span><span class="n">type</span><span class="o">::</span><span class="n">is_threadblock_element_unary</span><span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">op_type</span><span class="p">))</span> <span class="o">||</span>
      <span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">==</span> <span class="n">type</span><span class="o">::</span><span class="n">TB_FORLOOP_ACCUM_NO_RED_OP</span><span class="p">))</span> <span class="p">{</span>
    <span class="n">tb</span><span class="o">::</span><span class="n">STensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">input0</span> <span class="o">=</span> <span class="n">op</span><span class="o">-&gt;</span><span class="n">input_tensors</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span>
    <span class="n">tb</span><span class="o">::</span><span class="n">TBOperator</span> <span class="o">*</span><span class="n">prev_op</span> <span class="o">=</span> <span class="n">input0</span><span class="p">.</span><span class="n">owner_op</span><span class="p">;</span>
    <span class="c1">// Check various conditions...</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">num_consumers</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="n">input0</span><span class="p">.</span><span class="n">guid</span><span class="p">)</span> <span class="o">==</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span>
        <span class="n">prev_op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">!=</span> <span class="n">type</span><span class="o">::</span><span class="n">TB_FORLOOP_ACCUM_NO_RED_OP</span> <span class="o">&amp;&amp;</span>
        <span class="n">prev_op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">!=</span> <span class="n">type</span><span class="o">::</span><span class="n">TB_INPUT_OP</span> <span class="o">&amp;&amp;</span>
        <span class="cm">/* ... other conditions ... */</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">is_fused_with_prev</span><span class="p">[</span><span class="n">op</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
      <span class="n">is_fused_with_next</span><span class="p">[</span><span class="n">prev_op</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="23-fusion-chains">2.3 Fusion Chains</h3> <p>After determining fusion relationships, operators are grouped into <strong>chains</strong>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Construct `fusion_chain`</span>
<span class="k">for</span> <span class="p">(</span><span class="n">tb</span><span class="o">::</span><span class="n">TBOperator</span> <span class="o">*</span><span class="k">const</span> <span class="n">last_op</span> <span class="o">:</span> <span class="n">tb_graph</span><span class="p">.</span><span class="n">operators</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">is_fused_with_next</span><span class="p">[</span><span class="n">last_op</span><span class="p">])</span> <span class="k">continue</span><span class="p">;</span>
  <span class="c1">// Now last_op is the tail of a fusion chain</span>
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">tb</span><span class="o">::</span><span class="n">TBOperator</span> <span class="k">const</span> <span class="o">*&gt;</span> <span class="n">fused_ops</span><span class="p">;</span>
  <span class="n">tb</span><span class="o">::</span><span class="n">TBOperator</span> <span class="o">*</span><span class="n">cur_op</span> <span class="o">=</span> <span class="n">last_op</span><span class="p">;</span>
  <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">fused_ops</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">cur_op</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">is_fused_with_prev</span><span class="p">[</span><span class="n">cur_op</span><span class="p">])</span> <span class="p">{</span>
      <span class="n">cur_op</span> <span class="o">=</span> <span class="n">cur_op</span><span class="o">-&gt;</span><span class="n">input_tensors</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="mi">0</span><span class="p">).</span><span class="n">owner_op</span><span class="p">;</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
      <span class="k">break</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="n">std</span><span class="o">::</span><span class="n">reverse</span><span class="p">(</span><span class="n">fused_ops</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">fused_ops</span><span class="p">.</span><span class="n">end</span><span class="p">());</span>
  <span class="n">fusion_chain</span><span class="p">[</span><span class="n">leading_op</span><span class="p">]</span> <span class="o">=</span> <span class="n">fused_ops</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>Example Chain:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Matmul → Exp → Square → Store
  ↑
Leading Op    Fused Ops →
</code></pre></div></div> <h2 id="3-layout-resolution">3. Layout Resolution</h2> <p>Tensor layout (the stride of each dimension) critically affects performance. The transpiler uses the <strong>Z3 SMT solver</strong> to find optimal layouts.</p> <h3 id="31-problem-formulation">3.1 Problem Formulation</h3> <p>For each tensor dimension, we create boolean variables:</p> <ul> <li><code class="language-plaintext highlighter-rouge">di_x_y</code>: Is dimension y of DTensor x the innermost dimension?</li> <li><code class="language-plaintext highlighter-rouge">si_x_y</code>: Is dimension y of STensor x the innermost dimension?</li> <li><code class="language-plaintext highlighter-rouge">sw_x_y</code>: Is dimension y of STensor x swizzled?</li> </ul> <h3 id="32-constraints">3.2 Constraints</h3> <p><strong>Hard constraints</strong> (must be satisfied):</p> <ol> <li>Each tensor has exactly one innermost dimension</li> <li>Each STensor has at most one swizzled dimension</li> <li>The innermost dimension cannot be swizzled</li> <li>Matmul inputs/outputs must have innermost dim in last two dimensions</li> <li>Input tensors must match the user-provided stride layout</li> </ol> <p><strong>Code Reference</strong> (<code class="language-plaintext highlighter-rouge">src/transpiler/resolve_tensor_layout.cc:179-216</code>):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="p">(</span><span class="n">kn</span><span class="o">::</span><span class="n">DTensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">dtensor</span> <span class="o">:</span> <span class="n">all_dtensors</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// Every DTensor can only have 1 innermost dim</span>
  <span class="n">z3</span><span class="o">::</span><span class="n">expr_vector</span> <span class="n">innermost_exprs</span><span class="p">(</span><span class="n">ctx</span><span class="p">);</span>
  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">num_dims</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">innermost_exprs</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">d_is_innermost</span><span class="p">[</span><span class="n">dtensor</span><span class="p">.</span><span class="n">guid</span><span class="p">][</span><span class="n">i</span><span class="p">]);</span>
  <span class="p">}</span>
  <span class="n">opt</span><span class="p">.</span><span class="n">add</span><span class="p">(</span><span class="n">z3</span><span class="o">::</span><span class="n">atmost</span><span class="p">(</span><span class="n">innermost_exprs</span><span class="p">,</span> <span class="mi">1</span><span class="p">));</span>
  <span class="n">opt</span><span class="p">.</span><span class="n">add</span><span class="p">(</span><span class="n">z3</span><span class="o">::</span><span class="n">atleast</span><span class="p">(</span><span class="n">innermost_exprs</span><span class="p">,</span> <span class="mi">1</span><span class="p">));</span>
<span class="p">}</span>

<span class="c1">// For matmul, innermost must be in last two dims</span>
<span class="k">for</span> <span class="p">(</span><span class="n">kn</span><span class="o">::</span><span class="n">DTensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">tensor</span> <span class="o">:</span> <span class="p">{</span><span class="n">lhs</span><span class="p">,</span> <span class="n">rhs</span><span class="p">,</span> <span class="n">output</span><span class="p">})</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">num_dims</span> <span class="o">=</span> <span class="n">tensor</span><span class="p">.</span><span class="n">num_dims</span><span class="p">;</span>
  <span class="n">opt</span><span class="p">.</span><span class="n">add</span><span class="p">(</span><span class="n">d_is_innermost</span><span class="p">[</span><span class="n">tensor</span><span class="p">.</span><span class="n">guid</span><span class="p">][</span><span class="n">num_dims</span> <span class="o">-</span> <span class="mi">1</span><span class="p">]</span> <span class="o">||</span>
          <span class="n">d_is_innermost</span><span class="p">[</span><span class="n">tensor</span><span class="p">.</span><span class="n">guid</span><span class="p">][</span><span class="n">num_dims</span> <span class="o">-</span> <span class="mi">2</span><span class="p">]);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="33-cost-model">3.3 Cost Model</h3> <p>The optimizer minimizes a cost function:</p> <table> <thead> <tr> <th>Scenario</th> <th>Cost</th> </tr> </thead> <tbody> <tr> <td>Input without wide copy</td> <td>4000</td> </tr> <tr> <td>Output without wide copy</td> <td>4000</td> </tr> <tr> <td>Matmul without ldmatrix</td> <td>10000</td> </tr> <tr> <td>Input without cp.async</td> <td>20000</td> </tr> <tr> <td>Swizzling a dimension</td> <td>1000</td> </tr> <tr> <td>Shared memory usage</td> <td>1 per byte</td> </tr> </tbody> </table> <p><strong>Code Reference</strong> (<code class="language-plaintext highlighter-rouge">src/transpiler/resolve_tensor_layout.cc:51-76</code>):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">namespace</span> <span class="n">cost</span> <span class="p">{</span>
<span class="n">cost_t</span> <span class="n">KN_REDUCTION_INNERMOST_EQ_REDUC_DIM</span> <span class="o">=</span> <span class="mi">4000</span><span class="p">;</span>
<span class="n">cost_t</span> <span class="n">TB_INPUT_NO_WIDE_COPY</span> <span class="o">=</span> <span class="mi">4000</span><span class="p">;</span>
<span class="n">cost_t</span> <span class="n">TB_OUTPUT_NO_WIDE_COPY</span> <span class="o">=</span> <span class="mi">4000</span><span class="p">;</span>
<span class="n">cost_t</span> <span class="n">TB_MATMUL_NO_LDMATRIX</span> <span class="o">=</span> <span class="mi">10000</span><span class="p">;</span>
<span class="n">cost_t</span> <span class="n">TB_INPUT_NO_CP_ASYNC</span> <span class="o">=</span> <span class="mi">20000</span><span class="p">;</span>
<span class="n">cost_t</span> <span class="n">SWIZZLE_DIM</span> <span class="o">=</span> <span class="mi">1000</span><span class="p">;</span>
<span class="n">cost_t</span> <span class="n">SMEM_FACTOR</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="34-stride-calculation">3.4 Stride Calculation</h3> <p>After determining innermost dimensions, strides are calculated:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">calc_tensor_strides</span><span class="p">(</span><span class="kt">size_t</span> <span class="o">*</span><span class="n">strides</span><span class="p">,</span> <span class="kt">size_t</span> <span class="o">&amp;</span><span class="n">num_phy_elems</span><span class="p">,</span>
                         <span class="kt">int</span> <span class="n">num_dims</span><span class="p">,</span> <span class="kt">int</span> <span class="k">const</span> <span class="n">dims</span><span class="p">[],</span>
                         <span class="kt">int</span> <span class="n">innermost_dim</span><span class="p">,</span> <span class="kt">int</span> <span class="n">datatype_size</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// Order: [innermost, N-1, N-2, ..., innermost+1, innermost-1, ..., 0]</span>
  <span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">dim_order</span> <span class="o">=</span> <span class="p">{</span><span class="n">innermost_dim</span><span class="p">};</span>
  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">num_dims</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;=</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">!=</span> <span class="n">innermost_dim</span><span class="p">)</span> <span class="n">dim_order</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>
  <span class="p">}</span>

  <span class="kt">size_t</span> <span class="n">alignment</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">max</span><span class="p">(</span><span class="mi">16</span> <span class="o">/</span> <span class="n">datatype_size</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
  <span class="kt">size_t</span> <span class="n">cur_stride</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">dim_idx</span> <span class="o">:</span> <span class="n">dim_order</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">strides</span><span class="p">[</span><span class="n">dim_idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">cur_stride</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">dims</span><span class="p">[</span><span class="n">dim_idx</span><span class="p">]</span> <span class="o">!=</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// Pad first non-1 dimension to 16-byte alignment</span>
      <span class="n">cur_stride</span> <span class="o">*=</span> <span class="n">round_to_multiple</span><span class="p">((</span><span class="kt">size_t</span><span class="p">)</span><span class="n">dims</span><span class="p">[</span><span class="n">dim_idx</span><span class="p">],</span> <span class="n">alignment</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="n">num_phy_elems</span> <span class="o">=</span> <span class="n">cur_stride</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>Example:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Shape: [4, 8, 16], innermost_dim = 2, dtype = half (2 bytes)
Order: [2, 1, 0]
Strides: [128, 16, 1]  (dim 2 padded: 16 → 16, dim 1: 16*8=128)
</code></pre></div></div> <h2 id="4-threadblock-graph-scheduling">4. Threadblock Graph Scheduling</h2> <p>The scheduler determines the order of operator execution within a threadblock.</p> <h3 id="41-objectives">4.1 Objectives</h3> <ol> <li><strong>Minimize synchronizations</strong> (<code class="language-plaintext highlighter-rouge">__syncthreads()</code>)</li> <li><strong>Minimize peak shared memory usage</strong></li> </ol> <p>These can conflict - the scheduler prioritizes fewer synchronizations.</p> <h3 id="42-algorithm-modified-topological-sort">4.2 Algorithm: Modified Topological Sort</h3> <p>Each operator is assigned a <strong>depth</strong> (longest path from any input):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Calculate depth using dynamic programming</span>
<span class="k">for</span> <span class="p">(</span><span class="n">tb</span><span class="o">::</span><span class="n">TBOperator</span> <span class="o">*</span><span class="k">const</span> <span class="n">op</span> <span class="o">:</span> <span class="n">tb_graph</span><span class="p">.</span><span class="n">operators</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">==</span> <span class="n">type</span><span class="o">::</span><span class="n">TB_INPUT_OP</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">op2chaining_meta</span><span class="p">[</span><span class="n">op</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">0</span><span class="p">,</span> <span class="n">next_chain_idx</span><span class="o">++</span><span class="p">,</span> <span class="mi">0</span><span class="p">};</span>
  <span class="p">}</span>
<span class="p">}</span>
<span class="k">for</span> <span class="p">(</span><span class="n">tb</span><span class="o">::</span><span class="n">TBOperator</span> <span class="o">*</span><span class="k">const</span> <span class="n">op</span> <span class="o">:</span> <span class="n">tb_graph</span><span class="p">.</span><span class="n">operators</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">==</span> <span class="n">type</span><span class="o">::</span><span class="n">TB_INPUT_OP</span><span class="p">)</span> <span class="k">continue</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">is_fused_with_prev</span><span class="p">[</span><span class="n">op</span><span class="p">])</span> <span class="p">{</span>
    <span class="c1">// Same depth as predecessor</span>
    <span class="n">OpChainingMeta</span> <span class="n">prev_meta</span> <span class="o">=</span> <span class="n">op2chaining_meta</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="n">prev_op</span><span class="p">);</span>
    <span class="n">op2chaining_meta</span><span class="p">[</span><span class="n">op</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="n">prev_meta</span><span class="p">.</span><span class="n">level</span><span class="p">,</span> <span class="n">prev_meta</span><span class="p">.</span><span class="n">fuse_chain_idx</span><span class="p">,</span>
                            <span class="n">prev_meta</span><span class="p">.</span><span class="n">pos_in_fuse_chain</span> <span class="o">+</span> <span class="mi">1</span><span class="p">};</span>
  <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="c1">// max(input depths) + 1</span>
    <span class="kt">int</span> <span class="n">res</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">tb</span><span class="o">::</span><span class="n">STensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">input</span> <span class="o">:</span> <span class="n">op</span><span class="o">-&gt;</span><span class="n">input_tensors</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">res</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">max</span><span class="p">(</span><span class="n">res</span><span class="p">,</span> <span class="n">op2chaining_meta</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="n">input</span><span class="p">.</span><span class="n">owner_op</span><span class="p">).</span><span class="n">level</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="n">op2chaining_meta</span><span class="p">[</span><span class="n">op</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="n">res</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">next_chain_idx</span><span class="o">++</span><span class="p">,</span> <span class="mi">0</span><span class="p">};</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="43-schedule-structure">4.3 Schedule Structure</h3> <p>The schedule has three phases:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">TBSched</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">TBSchedNode</span><span class="o">&gt;</span> <span class="n">pre_loop_nodes</span><span class="p">;</span>   <span class="c1">// Before the forloop</span>
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">TBSchedNode</span><span class="o">&gt;</span> <span class="n">loop_nodes</span><span class="p">;</span>       <span class="c1">// Inside the forloop</span>
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">TBSchedNode</span><span class="o">&gt;</span> <span class="n">post_loop_nodes</span><span class="p">;</span>  <span class="c1">// After the forloop</span>
<span class="p">};</span>
</code></pre></div></div> <p><strong>Synchronization Rule:</strong> A <code class="language-plaintext highlighter-rouge">__syncthreads()</code> is inserted when the depth changes.</p> <h3 id="44-metadata-refinement">4.4 Metadata Refinement</h3> <p>The scheduler also decides per-operator metadata:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">TBSchedOpMeta</span> <span class="p">{</span>
  <span class="kt">bool</span> <span class="n">is_chunked_input</span><span class="p">;</span>    <span class="c1">// Use 128-bit wide copies?</span>
  <span class="kt">bool</span> <span class="n">is_pipelined_input</span><span class="p">;</span>  <span class="c1">// Use async cp.async?</span>
  <span class="kt">bool</span> <span class="n">is_chunked_output</span><span class="p">;</span>   <span class="c1">// Use 128-bit wide writes?</span>
  <span class="kt">bool</span> <span class="n">is_accum_in_reg</span><span class="p">;</span>     <span class="c1">// Keep accumulator in registers?</span>
  <span class="c1">// ...</span>
<span class="p">};</span>
</code></pre></div></div> <p><strong>Register Accumulator Heuristic</strong> (<code class="language-plaintext highlighter-rouge">src/transpiler/sched_tb_graph.cc:236-253</code>):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Allow accumulators to take up to 192 registers per thread</span>
<span class="c1">// (Each thread has 255 32-bit registers max)</span>
<span class="k">if</span> <span class="p">(</span><span class="n">per_thread_accum_numel_tot</span> <span class="o">+</span> <span class="n">per_thr_accum_numel</span> <span class="o">&lt;=</span> <span class="mi">192</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">op_meta</span><span class="p">.</span><span class="n">is_accum_in_reg</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
  <span class="n">per_thread_accum_numel_tot</span> <span class="o">+=</span> <span class="n">per_thr_accum_numel</span><span class="p">;</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
  <span class="n">op_meta</span><span class="p">.</span><span class="n">is_accum_in_reg</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="5-swizzle-planning">5. Swizzle Planning</h2> <p>Swizzling reorders shared memory layout to avoid <strong>bank conflicts</strong>.</p> <h3 id="51-the-problem">5.1 The Problem</h3> <p>Shared memory has 32 banks. When threads in a warp access the same bank, accesses are serialized. The <code class="language-plaintext highlighter-rouge">ldmatrix</code> instruction loads 16x16 tiles - without swizzling, threads may conflict.</p> <h3 id="52-xor-based-swizzling">5.2 XOR-Based Swizzling</h3> <p>Used when the innermost dimension size is a power of 2:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>new_addr = old_addr XOR row
</code></pre></div></div> <p><strong>Example (8x8 tensor, 8 banks):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Original:              Swizzled:
Bank: 0 1 2 3 4 5 6 7  Bank: 0 1 2 3 4 5 6 7
Row 0: 0 1 2 3 4 5 6 7  Row 0: 0 1 2 3 4 5 6 7
Row 1: 0 1 2 3 4 5 6 7  Row 1: 1 0 3 2 5 4 7 6  (XOR with 1)
Row 2: 0 1 2 3 4 5 6 7  Row 2: 2 3 0 1 6 7 4 5  (XOR with 2)
...
</code></pre></div></div> <p><strong>Parameters:</strong> CuTe’s <code class="language-plaintext highlighter-rouge">Swizzle&lt;B, M, S&gt;</code> with:</p> <ul> <li>B = log2(num_chunks_in_128B)</li> <li>M = log2(chunk_size_in_elements)</li> <li>S = log2(num_chunks_in_inner_dim)</li> </ul> <h3 id="53-shift-based-swizzling">5.3 Shift-Based Swizzling</h3> <p>Used when innermost dimension is not a power of 2:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>new_addr = old_addr + row * shift
</code></pre></div></div> <p>The shift is chosen so that <code class="language-plaintext highlighter-rouge">gcd(new_stride, num_banks) == 1</code>.</p> <p><strong>Code Reference</strong> (<code class="language-plaintext highlighter-rouge">src/transpiler/plan_tb_swizzle.cc:141-172</code>):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">is_power_of_2</span><span class="p">(</span><span class="n">num_chunks_in_inner_dim</span><span class="p">))</span> <span class="p">{</span>
  <span class="c1">// XOR-based swizzling</span>
  <span class="n">meta</span><span class="p">.</span><span class="n">is_xor_swizzled</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
  <span class="n">meta</span><span class="p">.</span><span class="n">xor_swizzle_b</span> <span class="o">=</span> <span class="n">log2</span><span class="p">(</span><span class="n">num_chunks_in_128B</span><span class="p">);</span>
  <span class="n">meta</span><span class="p">.</span><span class="n">xor_swizzle_m</span> <span class="o">=</span> <span class="n">log2</span><span class="p">(</span><span class="n">chunk_size_num_elems</span><span class="p">);</span>
  <span class="n">meta</span><span class="p">.</span><span class="n">xor_swizzle_s</span> <span class="o">=</span> <span class="n">log2</span><span class="p">(</span><span class="n">num_chunks_in_inner_dim</span><span class="p">);</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
  <span class="c1">// Shift-based swizzling</span>
  <span class="kt">int</span> <span class="n">new_num_chunks</span> <span class="o">=</span> <span class="n">num_chunks_in_inner_dim</span><span class="p">;</span>
  <span class="k">while</span> <span class="p">(</span><span class="n">gcd</span><span class="p">(</span><span class="n">new_num_chunks</span><span class="p">,</span> <span class="n">num_chunks_in_128B</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">new_num_chunks</span><span class="o">++</span><span class="p">;</span>
  <span class="p">}</span>
  <span class="c1">// Update strides to reflect padding</span>
  <span class="n">meta</span><span class="p">.</span><span class="n">strides</span><span class="p">[</span><span class="n">meta</span><span class="p">.</span><span class="n">innermost_dim</span><span class="p">]</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">size_t</span> <span class="n">cur_stride</span> <span class="o">=</span> <span class="n">new_num_chunks</span> <span class="o">*</span> <span class="n">chunk_size_num_elems</span><span class="p">;</span>
  <span class="c1">// ... update other strides</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="6-memory-planning">6. Memory Planning</h2> <h3 id="61-global-memory-dtensors">6.1 Global Memory (DTensors)</h3> <p>DTensor memory is allocated sequentially with 128-byte alignment:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MemoryPlanner</span> <span class="p">{</span>
  <span class="kt">size_t</span> <span class="n">cur_addr</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

  <span class="kt">size_t</span> <span class="n">allocate</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">size</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">size_t</span> <span class="n">addr</span> <span class="o">=</span> <span class="n">cur_addr</span><span class="p">;</span>
    <span class="n">cur_addr</span> <span class="o">+=</span> <span class="n">size</span><span class="p">;</span>
    <span class="n">cur_addr</span> <span class="o">=</span> <span class="n">round_to_multiple</span><span class="p">(</span><span class="n">cur_addr</span><span class="p">,</span> <span class="mi">128</span><span class="p">);</span>  <span class="c1">// 128B alignment</span>
    <span class="k">return</span> <span class="n">addr</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">};</span>
</code></pre></div></div> <p>Only <strong>intermediate tensors</strong> (not inputs/outputs) are allocated from the buffer.</p> <h3 id="62-shared-memory-stensors">6.2 Shared Memory (STensors)</h3> <p>STensor memory planning is modeled as <strong>Dynamic Storage Allocation (DSA)</strong>:</p> <p><strong>Problem:</strong> Given tensors with (size, alloc_time, free_time), minimize peak memory.</p> <p><strong>Geometric View:</strong> Pack axis-parallel rectangles (height = size, width = lifetime) without overlap.</p> <h3 id="63-tensor-lifecycles">6.3 Tensor Lifecycles</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Pre-loop inputs: allocated at pre_loop time, freed at 2T or post_loop usage</span>
<span class="c1">// Loop intermediates: allocated/freed within each iteration</span>
<span class="c1">// Accumulators: allocated before loop, freed after post_loop usage</span>
<span class="c1">// Post-loop intermediates: allocated/freed within post_loop phase</span>
</code></pre></div></div> <p>Timeline encoding:</p> <ul> <li>Pre-loop: time 0 to T</li> <li>Loop: time T to 2T</li> <li>Post-loop: time 2T to 3T</li> </ul> <h3 id="64-allocation-algorithms">6.4 Allocation Algorithms</h3> <p>Three heuristics are tried; the best result is used:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">AbstractMemoryPlanner</span><span class="o">&gt;&gt;</span> <span class="n">planners</span> <span class="o">=</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">FirstFitMemoryPlanner</span><span class="o">&gt;</span><span class="p">(),</span>
  <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">BestFitMemoryPlanner</span><span class="o">&gt;</span><span class="p">(),</span>
  <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">WorseFitMemoryPlanner</span><span class="o">&gt;</span><span class="p">()</span>
<span class="p">};</span>
</code></pre></div></div> <p><strong>First-Fit:</strong> Choose the first free block that fits <strong>Best-Fit:</strong> Choose the smallest free block that fits <strong>Worse-Fit:</strong> Choose the largest free block that fits</p> <p><strong>Code Reference</strong> (<code class="language-plaintext highlighter-rouge">src/transpiler/plan_stensor_memory.cc:184-213</code>):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">FirstFitMemoryPlanner</span> <span class="o">:</span> <span class="k">public</span> <span class="n">OnlineAllocMemoryPlannerBase</span> <span class="p">{</span>
  <span class="n">Range</span> <span class="n">select_range</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">size</span><span class="p">)</span> <span class="k">override</span> <span class="p">{</span>
    <span class="k">auto</span> <span class="n">it</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">find_if</span><span class="p">(</span><span class="n">free_ranges</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">free_ranges</span><span class="p">.</span><span class="n">end</span><span class="p">(),</span>
      <span class="p">[</span><span class="o">&amp;</span><span class="p">](</span><span class="n">Range</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">range</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">range</span><span class="p">.</span><span class="n">second</span> <span class="o">-</span> <span class="n">range</span><span class="p">.</span><span class="n">first</span> <span class="o">&gt;=</span> <span class="n">size</span><span class="p">;</span>
      <span class="p">});</span>
    <span class="k">return</span> <span class="p">{</span><span class="n">it</span><span class="o">-&gt;</span><span class="n">first</span><span class="p">,</span> <span class="n">it</span><span class="o">-&gt;</span><span class="n">first</span> <span class="o">+</span> <span class="n">size</span><span class="p">};</span>
  <span class="p">}</span>
<span class="p">};</span>
</code></pre></div></div> <h2 id="7-code-generation">7. Code Generation</h2> <h3 id="71-kernel-level-code">7.1 Kernel-Level Code</h3> <p>For each kernel-level operator, the transpiler generates appropriate code:</p> <p><strong>Matmul:</strong> Calls cuBLAS via the runtime</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">exec</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"kn::gemm&lt;$&gt;($,$,$, $,$,$, ...);"</span><span class="p">,</span>
       <span class="n">compute_type</span><span class="p">,</span> <span class="n">out0_ptr</span><span class="p">,</span> <span class="n">in0_ptr</span><span class="p">,</span> <span class="n">in1_ptr</span><span class="p">,</span> <span class="n">m</span><span class="p">,</span> <span class="n">n</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="p">...);</span>
</code></pre></div></div> <p><strong>Elementwise:</strong> Uses CuTe layouts</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">exec</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"using kernel = kn::ElementUnaryKernel&lt;$, "</span>
       <span class="s">"kn::ElementUnaryOpType::$, $, $&gt;;"</span><span class="p">,</span>
       <span class="n">datatype</span><span class="p">,</span> <span class="n">op_type</span><span class="p">,</span> <span class="n">in_layout</span><span class="p">,</span> <span class="n">out_layout</span><span class="p">);</span>
<span class="n">exec</span><span class="p">.</span><span class="n">e</span><span class="p">(</span><span class="s">"kernel::run($, $);"</span><span class="p">,</span> <span class="n">out_ptr</span><span class="p">,</span> <span class="n">in_ptr</span><span class="p">);</span>
</code></pre></div></div> <p><strong>Custom Op:</strong> Generates a complete CUDA kernel</p> <h3 id="72-custom-kernel-structure">7.2 Custom Kernel Structure</h3> <p>For <code class="language-plaintext highlighter-rouge">KN_CUSTOMIZED_OP</code>, a complete kernel is generated:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">custom_kernel_0</span><span class="p">(</span><span class="n">half</span><span class="o">*</span> <span class="n">output</span><span class="p">,</span> <span class="n">half</span> <span class="k">const</span><span class="o">*</span> <span class="n">input</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">thread_idx</span> <span class="o">=</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
  <span class="k">extern</span> <span class="n">__shared__</span> <span class="kt">char</span> <span class="n">buf</span><span class="p">[];</span>

  <span class="c1">// Define STensor pointers</span>
  <span class="n">half</span> <span class="o">*</span><span class="n">stensor0_ptr</span> <span class="o">=</span> <span class="p">(</span><span class="n">half</span><span class="o">*</span><span class="p">)(</span><span class="n">buf</span> <span class="o">+</span> <span class="mi">0</span><span class="p">);</span>
  <span class="n">half</span> <span class="o">*</span><span class="n">stensor1_ptr</span> <span class="o">=</span> <span class="p">(</span><span class="n">half</span><span class="o">*</span><span class="p">)(</span><span class="n">buf</span> <span class="o">+</span> <span class="mi">1024</span><span class="p">);</span>

  <span class="c1">// Pre-loop: Load static inputs</span>
  <span class="n">STensor0InputAtom</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">stensor0_ptr</span><span class="p">,</span> <span class="n">dtensor_tile_ptr</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
  <span class="n">__syncthreads</span><span class="p">();</span>

  <span class="c1">// Main loop</span>
  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">for_idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">for_idx</span> <span class="o">&lt;</span> <span class="n">FORLOOP_RANGE</span><span class="p">;</span> <span class="n">for_idx</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Load inputs for this iteration</span>
    <span class="n">STensor1InputAtom</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">stensor1_ptr</span><span class="p">,</span> <span class="n">dtensor_tile_ptr</span> <span class="o">+</span> <span class="n">offset</span><span class="o">*</span><span class="n">for_idx</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
    <span class="n">__syncthreads</span><span class="p">();</span>

    <span class="c1">// Compute</span>
    <span class="n">Matmul0Kernel</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">matmul_accum</span><span class="p">,</span> <span class="n">stensor0_ptr</span><span class="p">,</span> <span class="n">stensor1_ptr</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
    <span class="c1">// ... more operators</span>
  <span class="p">}</span>
  <span class="n">__syncthreads</span><span class="p">();</span>

  <span class="c1">// Post-loop: reductions, outputs</span>
  <span class="n">OutputAtom</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">dtensor_output</span><span class="p">,</span> <span class="n">stensor_result</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="73-epilogue-fusion">7.3 Epilogue Fusion</h3> <p>Fused operators are implemented as <strong>epilogues</strong>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">string</span> <span class="nf">transpile_fusion_epilogue</span><span class="p">(</span><span class="n">chain</span><span class="p">,</span> <span class="n">dtype</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">string</span> <span class="n">res</span> <span class="o">=</span> <span class="s">"tb::EpilogueStore&lt;half&gt;"</span><span class="p">;</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">op</span> <span class="n">in</span> <span class="n">chain</span><span class="p">[</span><span class="mi">1</span><span class="o">:</span><span class="p">])</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">==</span> <span class="n">TB_EXP_OP</span><span class="p">)</span>
      <span class="n">res</span> <span class="o">=</span> <span class="n">fmt</span><span class="p">(</span><span class="s">"tb::EpilogueExp&lt;half, $&gt;"</span><span class="p">,</span> <span class="n">res</span><span class="p">);</span>
    <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">==</span> <span class="n">TB_FORLOOP_ACCUM_NO_RED_OP</span><span class="p">)</span>
      <span class="n">res</span> <span class="o">=</span> <span class="s">"tb::EpilogueStoreAccum&lt;half&gt;"</span><span class="p">;</span>
    <span class="c1">// ...</span>
  <span class="p">}</span>
  <span class="k">return</span> <span class="n">res</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>Example chain <code class="language-plaintext highlighter-rouge">Matmul → Exp → Store</code>:</strong></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">using</span> <span class="n">Epilogue</span> <span class="o">=</span> <span class="n">tb</span><span class="o">::</span><span class="n">EpilogueExp</span><span class="o">&lt;</span><span class="n">half</span><span class="p">,</span> <span class="n">tb</span><span class="o">::</span><span class="n">EpilogueStore</span><span class="o">&lt;</span><span class="n">half</span><span class="o">&gt;&gt;</span><span class="p">;</span>
</code></pre></div></div> <h3 id="74-architecture-specific-code">7.4 Architecture-Specific Code</h3> <p><strong>Hopper (SM90):</strong></p> <ul> <li>TMA (Tensor Memory Accelerator) for async copies</li> <li>Warp group specialization</li> <li><code class="language-plaintext highlighter-rouge">SM90_TMA_LOAD</code> copy atoms</li> </ul> <p><strong>Blackwell (SM100):</strong></p> <ul> <li>2-SM MMA instructions</li> <li>Cluster-level synchronization</li> <li>TMEM allocation</li> </ul> <h2 id="8-running-example-rmsnorm--linear">8. Running Example: RMSNorm + Linear</h2> <p>Let’s trace through transpiling our running example:</p> <h3 id="81-input-graph">8.1 Input Graph</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># From c1_architecture
</span><span class="n">X</span> <span class="o">=</span> <span class="n">graph</span><span class="p">.</span><span class="nf">new_input</span><span class="p">([</span><span class="n">batch</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="n">hidden</span><span class="p">])</span>
<span class="n">W_norm</span> <span class="o">=</span> <span class="n">graph</span><span class="p">.</span><span class="nf">new_input</span><span class="p">([</span><span class="n">hidden</span><span class="p">])</span>
<span class="n">W_linear</span> <span class="o">=</span> <span class="n">graph</span><span class="p">.</span><span class="nf">new_input</span><span class="p">([</span><span class="n">hidden</span><span class="p">,</span> <span class="n">output</span><span class="p">])</span>

<span class="c1"># Custom op with threadblock graph:
# 1. Load X tile
# 2. Square X
# 3. Reduce sum
# 4. Sqrt (get RMS)
# 5. Divide X by RMS
# 6. Matmul with W_linear
# 7. Store result
</span></code></pre></div></div> <h3 id="82-fusion-analysis">8.2 Fusion Analysis</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TB_INPUT (X) → TB_SQUARE → TB_REDUCTION → TB_SQRT → TB_DIV → TB_MATMUL → TB_OUTPUT
     ↑              ↑
Not fusable    Can fuse with TB_INPUT? No (TB_INPUT has forloop_dim)
</code></pre></div></div> <p>Result: Minimal fusion (individual operators)</p> <h3 id="83-layout-resolution">8.3 Layout Resolution</h3> <p>Z3 constraints:</p> <ul> <li>Input X: innermost_dim from user (e.g., last dim)</li> <li>All matmul operands: innermost in last two dims</li> <li>STensors: prefer same innermost as DTensors for wide copy</li> </ul> <h3 id="84-scheduling">8.4 Scheduling</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pre_loop_nodes: [TB_INPUT(W_norm)]  # Static weight
loop_nodes: [TB_INPUT(X), TB_SQUARE, TB_REDUCTION, TB_SQRT,
             TB_DIV, TB_MATMUL, TB_FORLOOP_ACCUM]
post_loop_nodes: [TB_OUTPUT]
</code></pre></div></div> <p>Synchronizations inserted between depth changes.</p> <h3 id="85-generated-code-simplified">8.5 Generated Code (Simplified)</h3> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">custom_kernel_rmsnorm_linear</span><span class="p">(...)</span> <span class="p">{</span>
  <span class="k">extern</span> <span class="n">__shared__</span> <span class="kt">char</span> <span class="n">buf</span><span class="p">[];</span>

  <span class="c1">// Load weight (pre-loop)</span>
  <span class="n">WNormInputAtom</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">wnorm_ptr</span><span class="p">,</span> <span class="n">wnorm_tile</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
  <span class="n">__syncthreads</span><span class="p">();</span>

  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">for_idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">for_idx</span> <span class="o">&lt;</span> <span class="n">K_TILES</span><span class="p">;</span> <span class="n">for_idx</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Load X tile</span>
    <span class="n">XInputAtom</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">x_ptr</span><span class="p">,</span> <span class="n">x_tile</span> <span class="o">+</span> <span class="n">for_idx</span> <span class="o">*</span> <span class="n">TILE_K</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
    <span class="n">__syncthreads</span><span class="p">();</span>

    <span class="c1">// RMSNorm computation</span>
    <span class="n">SquareKernel</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">sq_ptr</span><span class="p">,</span> <span class="n">x_ptr</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
    <span class="n">__syncthreads</span><span class="p">();</span>
    <span class="n">ReductionKernel</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">sum_ptr</span><span class="p">,</span> <span class="n">sq_ptr</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
    <span class="n">__syncthreads</span><span class="p">();</span>
    <span class="n">SqrtKernel</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">rms_ptr</span><span class="p">,</span> <span class="n">sum_ptr</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
    <span class="n">DivKernel</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">norm_ptr</span><span class="p">,</span> <span class="n">x_ptr</span><span class="p">,</span> <span class="n">rms_ptr</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
    <span class="n">__syncthreads</span><span class="p">();</span>

    <span class="c1">// Matmul with accumulation</span>
    <span class="n">MatmulKernel</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">accum</span><span class="p">,</span> <span class="n">norm_ptr</span><span class="p">,</span> <span class="n">wlinear_ptr</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
  <span class="p">}</span>
  <span class="n">__syncthreads</span><span class="p">();</span>

  <span class="c1">// Output</span>
  <span class="n">OutputAtom</span><span class="o">::</span><span class="n">run</span><span class="p">(</span><span class="n">output_tile</span><span class="p">,</span> <span class="n">accum</span><span class="p">,</span> <span class="n">thread_idx</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="9-python-examples">9. Python Examples</h2> <p>To see the transpiler in action, run the example scripts:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Inspect layout resolution</span>
python3 tutorial/c2_transpiler_cuda/layout_resolution_example.py

<span class="c"># Visualize scheduling</span>
python3 tutorial/c2_transpiler_cuda/scheduling_example.py

<span class="c"># See generated code</span>
python3 tutorial/c2_transpiler_cuda/codegen_example.py
</code></pre></div></div> <h2 id="10-key-takeaways">10. Key Takeaways</h2> <ol> <li><strong>Fusion</strong> reduces memory traffic but is currently limited to unary operators</li> <li><strong>Layout resolution</strong> uses Z3 to balance multiple competing objectives</li> <li><strong>Scheduling</strong> minimizes synchronizations using depth-based topological sort</li> <li><strong>Swizzling</strong> avoids bank conflicts with XOR or shift-based methods</li> <li><strong>Memory planning</strong> uses best-of-three heuristics for shared memory</li> <li><strong>Code generation</strong> leverages CuTe for portable tensor operations</li> </ol> <p>The transpiler is the bridge between Mirage’s graph-level optimizations and efficient GPU execution. Understanding these algorithms helps when:</p> <ul> <li>Adding new operators</li> <li>Debugging performance issues</li> <li>Extending support for new architectures</li> </ul> <blockquote> <p><strong>Next Up:</strong> The next post in this series will cover Mirage’s <strong>Persistent Kernel (MPK) Runtime</strong> - the mega-kernel architecture that enables high-performance LLM inference with event-driven scheduling and multi-GPU support.</p> </blockquote>]]></content><author><name></name></author><summary type="html"><![CDATA[Deep dive into Mirage's transpiler - the journey from MuGraph to CUDA through fusion resolution, layout planning, scheduling, and code generation]]></summary></entry><entry><title type="html">Inside Mirage (1) - Superoptimizing Tensor Programs via MuGraph and Search</title><link href="https://uv-xiao.github.io/blog/2025/mirage-ir-search/" rel="alternate" type="text/html" title="Inside Mirage (1) - Superoptimizing Tensor Programs via MuGraph and Search"/><published>2025-11-24T00:00:00+00:00</published><updated>2025-11-24T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/mirage-ir-search</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/mirage-ir-search/"><![CDATA[<p>In the world of Large Language Models (LLMs), theoretical FLOPs often take a backseat to heterogenous processing units and memory hierarchies. <strong>Mirage</strong> is a tensor algebra superoptimizer. Instead of relying on static, hand-written fusion rules, Mirage <strong>searches</strong> the space of potential GPU kernels to automatically discover highly optimized, fused implementations that efficiently exploit GPU resources and memory hierarchies.</p> <p>In this post, we will tear down the architecture of Mirage. We will set up a development environment (even without a GPU), explore the <strong>MuGraph</strong> intermediate representation, and dissect the <strong>Search Algorithms</strong> that drive the optimization.</p> <blockquote> <p><strong>Note:</strong> This post follows the hands-on <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00">Mirage Tutorial (uv_tutorial_00 branch)</a>.</p> </blockquote> <hr/> <h2 id="chapter-0-environment-setup">Chapter 0: Environment Setup</h2> <p>This chapter only provides the Docker-based installation. The advantage is that it can be run without local GPUs!</p> <p>You can also adopt alternatives for installation, according to <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00/INSTALL.md">INSTALL.md</a>.</p> <h3 id="0-prerequisite">0. Prerequisite</h3> <p>Clone the mirage repo recursively.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone <span class="nt">--recursive</span> https://www.github.com/mirage-project/mirage
<span class="c"># Or maybe you can want to use my fork for tutorial purpose: </span>
<span class="c"># git clone --recursive -b uv_tutorial_00 git@github.com:arch-of-shadow/mirage.git</span>
<span class="nb">cd </span>mirage
</code></pre></div></div> <p>If you have <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00/INSTALL.md">qualified environment</a> on your machine and don’t need a Docker, just run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install</span> <span class="nt">-e</span> <span class="nb">.</span> <span class="nt">-v</span> 
</code></pre></div></div> <h3 id="1-docker">1. Docker</h3> <p>Run the script:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./docker/run_docker_no_gpu.sh mirage_img mirage_build
</code></pre></div></div> <p>where <code class="language-plaintext highlighter-rouge">mirage_img</code> is the name of the docker image, and <code class="language-plaintext highlighter-rouge">mirage_build</code> is the name of the docker container.</p> <h3 id="2-build-inside-the-container">2. Build inside the container</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./docker/install_latest.sh
</code></pre></div></div> <h3 id="3-check-installation">3. Check installation</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-c</span> <span class="s1">'import mirage'</span>
</code></pre></div></div> <hr/> <h2 id="chapter-1-architecture-and-internals">Chapter 1: Architecture and Internals</h2> <p>In this chapter, we will dive deep into the architecture of the Mirage project. We will explore the <strong>MuGraph</strong> intermediate representation, the <strong>Search Algorithms</strong> that drive optimization, and the overall <strong>Compiler Pipeline</strong>. We will use a concrete example—optimizing an <strong>RMSNorm followed by a Linear layer</strong>—to illustrate these concepts.</p> <h3 id="1-the-running-example-rmsnorm--linear">1. The Running Example: RMSNorm + Linear</h3> <p>To understand how Mirage works, let’s consider a common pattern in Large Language Models (LLMs): an RMSNorm layer followed by a Linear layer (Matrix Multiplication).</p> <p>Mathematically, this looks like: \(Y = \text{RMSNorm}(X) \times W\)</p> <p>where, RMSNorm is:</p> \[y_i = \frac{ x_i * g_i }{ \sqrt{\frac{1}{n} \sum_{i=1}^{n}{x_i^2}} }\] <p>In a standard deep learning framework (like PyTorch), this is executed as two separate kernels:</p> <ol> <li><strong>Kernel 1</strong>: Reads $X$, computes RMSNorm, writes intermediate result to global memory.</li> <li><strong>Kernel 2</strong>: Reads intermediate result, performs Matrix Multiplication with $W$, writes $Y$.</li> </ol> <p><strong>Mirage’s Goal</strong>: Automatically discover a fused kernel that is faster. For example, it might discover that we can perform the division of RMSNorm <em>after</em> the matrix multiplication accumulation in some cases, or simply fuse them to reduce memory traffic.</p> <h3 id="2-core-components-mugraph">2. Core Components: MuGraph</h3> <p>Mirage uses a multi-level graph representation called <strong>MuGraph</strong> to specify the execution of a tensor program on GPUs. Unlike standard computation graphs (like in PyTorch or TensorFlow) that only represent data dependencies, MuGraph explicitly represents things together: 1. operator graph at hierarchies; 2. loop schedule; 3. memory allocation.</p> <p><a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00/docs/source/mugraph.rst">Multi-Level Graph Representation</a> gives a very clear description of MuGraph. Below we introduce some related source code to help developers.</p> <h4 id="21-kernel-graph-miragekernelgraph">2.1 Kernel Graph (<code class="language-plaintext highlighter-rouge">mirage::kernel::Graph</code>)</h4> <p>The top level is the <strong>Kernel Graph</strong>. It represents computations at the grid level.</p> <ul> <li><strong>Nodes</strong>: <code class="language-plaintext highlighter-rouge">KNOperator</code> (Kernel Operators). These represent operations running on the entire grid.</li> <li><strong>Edges</strong>: <code class="language-plaintext highlighter-rouge">DTensor</code> (Device Tensors). These represent data stored in <strong>Global Memory</strong> (DRAM).</li> </ul> <p><strong>Why is it needed?</strong> It defines the boundaries of GPU kernels and manages global memory allocation.</p> <p><strong>Code Snippet (<code class="language-plaintext highlighter-rouge">include/mirage/kernel/graph.h</code>)</strong>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Graph</span> <span class="p">{</span>
<span class="nl">public:</span>
  <span class="c1">// ...</span>
  <span class="c1">// Create a new input tensor (resides in Global Memory)</span>
  <span class="n">DTensor</span> <span class="n">new_input</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">dims</span><span class="p">,</span> <span class="p">...);</span>

  <span class="c1">// Create a standard operator (e.g., Matmul)</span>
  <span class="n">DTensor</span> <span class="n">matmul</span><span class="p">(</span><span class="n">DTensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">A</span><span class="p">,</span> <span class="n">DTensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">B</span><span class="p">);</span>

  <span class="c1">// Create a "Customized" operator defined by a Thread Block Graph</span>
  <span class="c1">// This is where kernel fusion happens!</span>
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">DTensor</span><span class="o">&gt;</span> <span class="n">customized</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">DTensor</span><span class="o">&gt;</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">inputs</span><span class="p">,</span>
                                  <span class="n">mirage</span><span class="o">::</span><span class="n">threadblock</span><span class="o">::</span><span class="n">Graph</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">_graph</span><span class="p">);</span>
  
  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">mirage</span><span class="o">::</span><span class="n">kernel</span><span class="o">::</span><span class="n">KNOperator</span> <span class="o">*&gt;</span> <span class="n">operators</span><span class="p">;</span>
  <span class="c1">// ...</span>
<span class="p">};</span>
</code></pre></div></div> <h4 id="22-thread-block-graph-miragethreadblockgraph">2.2 Thread Block Graph (<code class="language-plaintext highlighter-rouge">mirage::threadblock::Graph</code>)</h4> <p>The second level is the <strong>Thread Block Graph</strong>. It represents computations within a single CUDA thread block.</p> <ul> <li><strong>Nodes</strong>: <code class="language-plaintext highlighter-rouge">TBOperator</code> (Thread Block Operators). These represent operations like loading data, computing matmul on a tile, or reducing values.</li> <li><strong>Edges</strong>: <code class="language-plaintext highlighter-rouge">STensor</code> (Shared Memory Tensors). These represent data stored in <strong>Shared Memory</strong> (SRAM).</li> </ul> <p><strong>Why is it needed?</strong> It allows Mirage to model and optimize data movement between Global Memory and Shared Memory, which is critical for performance (e.g., tiling for MatMul).</p> <p><strong>Code Snippet (<code class="language-plaintext highlighter-rouge">src/threadblock/graph.h</code>)</strong>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Graph</span> <span class="p">{</span>
<span class="nl">public:</span>
  <span class="c1">// ...</span>
  <span class="c1">// Create an input operator (Loads data from Global -&gt; Shared Memory)</span>
  <span class="n">STensor</span> <span class="n">new_input</span><span class="p">(</span><span class="n">mirage</span><span class="o">::</span><span class="n">kernel</span><span class="o">::</span><span class="n">DTensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">dtensor</span><span class="p">,</span> <span class="p">...);</span>

  <span class="c1">// Perform computation in Shared Memory</span>
  <span class="n">STensor</span> <span class="n">matmul</span><span class="p">(</span><span class="n">STensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">A</span><span class="p">,</span> <span class="n">STensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">B</span><span class="p">);</span>
  <span class="n">STensor</span> <span class="n">reduction</span><span class="p">(</span><span class="n">STensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">A</span><span class="p">,</span> <span class="kt">int</span> <span class="n">dim</span><span class="p">);</span>

  <span class="c1">// Mark output (Stores data from Shared -&gt; Global Memory)</span>
  <span class="n">mirage</span><span class="o">::</span><span class="n">kernel</span><span class="o">::</span><span class="n">DTensor</span> <span class="n">mark_output</span><span class="p">(</span><span class="n">STensor</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">stensor</span><span class="p">,</span> <span class="p">...);</span>

  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">mirage</span><span class="o">::</span><span class="n">threadblock</span><span class="o">::</span><span class="n">TBOperator</span> <span class="o">*&gt;</span> <span class="n">operators</span><span class="p">;</span>
  <span class="c1">// ...</span>
<span class="p">};</span>
</code></pre></div></div> <h4 id="23-putting-it-together-the-example">2.3 Putting it together (The Example)</h4> <p>We can inspect the graph structure using the following script:</p> <h5 id="creating-from-c">Creating from C++</h5> <p>Refer to <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00/cpp_examples/">cpp_examples</a></p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// TODO</span>
</code></pre></div></div> <h5 id="creating-from-python">Creating from Python</h5> <p>Let’s create a simple kernel graph and visualize it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">python3</span> <span class="n">tutorial</span><span class="o">/</span><span class="n">c1_architecture</span><span class="o">/</span><span class="n">kernel_graph</span><span class="p">.</span><span class="n">py</span>
</code></pre></div></div> <p>This will generate a DOT file <a href="tutorial/c1_architecture/rmsnorm.dot"><code class="language-plaintext highlighter-rouge">rmsnorm.dot</code></a> and a PNG file <a href="tutorial/c1_architecture/rmsnorm.png"><code class="language-plaintext highlighter-rouge">rmsnorm.png</code></a> in the current directory.</p> <p>We can manually create a hierarchical one:</p> <blockquote> <p>Refer to <a href="demo/reference_mugraphs/rms_norm.py">demo/reference_mugraphs/rms_norm.py</a></p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">python3</span> <span class="n">tutorial</span><span class="o">/</span><span class="n">c1_architecture</span><span class="o">/</span><span class="n">hierarchical_graph</span><span class="p">.</span><span class="n">py</span>
</code></pre></div></div> <p>This generates a DOT file <a href="tutorial/c1_architecture/rmsnorm_hierarchical.dot"><code class="language-plaintext highlighter-rouge">rmsnorm_hierarchical.dot</code></a> and a PNG file <a href="tutorial/c1_architecture/rmsnorm_hierarchical.png"><code class="language-plaintext highlighter-rouge">rmsnorm_hierarchical.png</code></a>.</p> <p>These visualization products help us understand the graph structure.</p> <h3 id="3-search-algorithms">3. Search Algorithms</h3> <p>Mirage doesn’t just “compile” code; it <strong>searches</strong> for the best implementation. The search engine is located in <code class="language-plaintext highlighter-rouge">src/search</code>.</p> <h4 id="30-cython-entry">3.0 Cython Entry</h4> <p>In <code class="language-plaintext highlighter-rouge">include/mirage/search/search_c.h</code>, declare the cython entry point:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">cython_search</span><span class="p">(</span><span class="n">mirage</span><span class="o">::</span><span class="n">kernel</span><span class="o">::</span><span class="n">Graph</span> <span class="k">const</span> <span class="o">*</span><span class="n">input_graph</span><span class="p">,</span>
                  <span class="kt">char</span> <span class="k">const</span> <span class="o">*</span><span class="n">backend</span><span class="p">,</span>
                  <span class="kt">int</span> <span class="n">max_num_graphs</span><span class="p">,</span>
                  <span class="n">mirage</span><span class="o">::</span><span class="n">kernel</span><span class="o">::</span><span class="n">Graph</span> <span class="o">**</span><span class="n">new_graphs</span><span class="p">,</span>
                  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">MInt3</span><span class="o">&gt;</span> <span class="n">imap_to_explore</span><span class="p">,</span>
                  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">MInt3</span><span class="o">&gt;</span> <span class="n">omap_to_explore</span><span class="p">,</span>
                  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">MDim3</span><span class="o">&gt;</span> <span class="n">grid_dim_to_explore</span><span class="p">,</span>
                  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">MDim3</span><span class="o">&gt;</span> <span class="n">block_dim_to_explore</span><span class="p">,</span>
                  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">fmap_to_explore</span><span class="p">,</span>
                  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">frange_to_explore</span><span class="p">,</span>
                  <span class="kt">char</span> <span class="k">const</span> <span class="o">*</span><span class="n">filename</span><span class="p">,</span>
                  <span class="kt">bool</span> <span class="n">verbose</span><span class="p">,</span>
                  <span class="kt">char</span> <span class="k">const</span> <span class="o">*</span><span class="n">default_config</span><span class="p">,</span>
                  <span class="kt">bool</span> <span class="n">is_formal_verified</span><span class="p">);</span>
</code></pre></div></div> <p>I think here is the right place to describe the search’s parameters:</p> <ul> <li><code class="language-plaintext highlighter-rouge">input_graph</code>: The input kernel graph.</li> <li><code class="language-plaintext highlighter-rouge">backend</code>: The backend to use (e.g., “cuda”).</li> <li><code class="language-plaintext highlighter-rouge">max_num_graphs</code>: The maximum number of graphs to generate.</li> <li><code class="language-plaintext highlighter-rouge">new_graphs</code>: The output graphs.</li> <li><code class="language-plaintext highlighter-rouge">imap_to_explore</code>: <code class="language-plaintext highlighter-rouge">imap</code> specifies how the input tensor is partitioned into sub-tensors for individual blocks.</li> <li><code class="language-plaintext highlighter-rouge">omap_to_explore</code>: <code class="language-plaintext highlighter-rouge">omap</code> specifies how the outputs of all blocks are concatenated to construct the final output of the kernel operator.</li> <li><code class="language-plaintext highlighter-rouge">grid_dim_to_explore</code>: specify the number of blocks along the x, y, and z dimensions.</li> <li><code class="language-plaintext highlighter-rouge">block_dim_to_explore</code>: specify the organization of threads within the block.</li> <li><code class="language-plaintext highlighter-rouge">fmap_to_explore</code>: specify which part of the input tensor to load in each iteration.</li> <li><code class="language-plaintext highlighter-rouge">frange_to_explore</code>: TODO</li> <li><code class="language-plaintext highlighter-rouge">filename</code>: The filename to save the search results.</li> <li><code class="language-plaintext highlighter-rouge">verbose</code>: Whether to print verbose output.</li> <li><code class="language-plaintext highlighter-rouge">default_config</code>: TODO</li> <li><code class="language-plaintext highlighter-rouge">is_formal_verified</code>: TODO.</li> </ul> <p>It is implemented in <code class="language-plaintext highlighter-rouge">src/search/search_c.cc</code>.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">cython_search</span><span class="p">(...)</span> <span class="p">{</span>
  <span class="c1">// 1. If a checkpoint file exists, load graphs from it instead of searching</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">filename</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">//..</span>
    <span class="n">new_graphs</span><span class="p">[</span><span class="n">num</span><span class="p">]</span> <span class="o">=</span> <span class="k">new</span> <span class="n">kernel</span><span class="o">::</span><span class="n">Graph</span><span class="p">();</span>
    <span class="n">from_json</span><span class="p">(</span><span class="n">graph</span><span class="p">,</span> <span class="o">*</span><span class="n">new_graphs</span><span class="p">[</span><span class="n">num</span><span class="p">]);</span>
  <span class="p">}</span>

  <span class="p">{</span>
    <span class="c1">// 2. Initialize Search Configuration</span>
    <span class="n">search</span><span class="o">::</span><span class="n">GeneratorConfig</span> <span class="n">config</span> <span class="o">=</span>
        <span class="n">search</span><span class="o">::</span><span class="n">GeneratorConfig</span><span class="o">::</span><span class="n">get_default_config</span><span class="p">();</span>
    <span class="c1">//..</span>

    <span class="c1">// 3. Set Exploration Parameters (Search Space Pruning)</span>
    <span class="c1">// These vectors limit the search to specific configurations if provided.</span>
    <span class="c1">// .. imap, omap, grid_dim, block_dim, fmap, frange</span>

    <span class="c1">// 4. Run the Search</span>
    <span class="c1">// Instantiate the Generator</span>
    <span class="n">search</span><span class="o">::</span><span class="n">KernelGraphGenerator</span> <span class="n">gen</span><span class="p">(</span>
        <span class="o">*</span><span class="n">input_graph</span><span class="p">,</span> <span class="n">config</span><span class="p">,</span> <span class="n">result_filename</span><span class="p">,</span> <span class="n">verbose</span><span class="p">);</span>
    
    <span class="c1">// Start the search process</span>
    <span class="n">gen</span><span class="p">.</span><span class="n">generate_kernel_graphs</span><span class="p">();</span>

    <span class="c1">// 5. Collect and Return Results</span>
    <span class="c1">//..</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>We’ll dive into <code class="language-plaintext highlighter-rouge">KernelGraphGenerator</code> in the next section.</p> <h4 id="31-backtracking-search">3.1 Backtracking Search</h4> <p>The core algorithm is a backtracking search that incrementally builds the MuGraph. It starts with an empty graph (containing only input tensors) and tries to append operators until the graph produces the desired output.</p> <p><strong>Code Snippet (<code class="language-plaintext highlighter-rouge">src/search/search.cc</code>, <code class="language-plaintext highlighter-rouge">include/mirage/search/search.h</code>)</strong>:</p> <h5 id="preprocess">Preprocess</h5> <p><code class="language-plaintext highlighter-rouge">KernelGraphGenerator::KernelGraphGenerator(..)</code> calls <code class="language-plaintext highlighter-rouge">preprocess</code>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="n">KernelGraphGenerator</span><span class="o">::</span><span class="n">preprocess</span><span class="p">(</span><span class="n">kernel</span><span class="o">::</span><span class="n">Graph</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">computation_graph</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// 1. Get input attributes: enumerate computation_graph.operators, check op_type == KN_INPUT_OP, and store in computation_graph_input_attrs</span>
  <span class="c1">//..</span>

  <span class="c1">// 2. Get possible abstract expressions, showing the kernel graph's computation tasks.</span>
  <span class="c1">//    They will be used for pruning illegal graphs during search.</span>
  <span class="c1">//    </span>
  <span class="n">abstract_expr_eval</span><span class="p">(</span><span class="n">computation_graph</span><span class="p">,</span> <span class="n">computation_graph_exprs</span><span class="p">);</span>

  <span class="c1">// 3. Some ”range“ things. I'm not sure what they are for.</span>
  <span class="c1">// ..</span>

  <span class="c1">// 4. Get output expressions (abstract)</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">kernel</span><span class="o">::</span><span class="n">KNOperator</span> <span class="o">*</span><span class="n">op</span> <span class="o">:</span> <span class="n">computation_graph</span><span class="p">.</span><span class="n">operators</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">op_type</span> <span class="o">==</span> <span class="n">type</span><span class="o">::</span><span class="n">KNOperatorType</span><span class="o">::</span><span class="n">KN_OUTPUT_OP</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">computation_graph_output_exprs</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span>
          <span class="n">computation_graph_exprs</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="n">op</span><span class="o">-&gt;</span><span class="n">input_tensors</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">guid</span><span class="p">));</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="c1">// 4.5  Initalize final expressions</span>
  <span class="c1">//      THIS CONSTRUCTS AN E-GRAPH</span>
  <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">final_expr</span> <span class="o">:</span> <span class="n">computation_graph_output_exprs</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">initialize_final_expr</span><span class="p">(</span><span class="n">final_expr</span><span class="p">);</span>
    <span class="c1">// ⤷ get_egraph(expr-&gt;to_egg().c_str()) // This build an e-graph!</span>
  <span class="p">}</span>
  
  <span class="c1">// 5. Initialize verifier, formal / probabilistic</span>
  <span class="c1">// ..</span>
<span class="p">}</span>
</code></pre></div></div> <h5 id="generate-kernel-graphs">Generate Kernel Graphs</h5> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="n">KernelGraphGenerator</span><span class="o">::</span><span class="n">generate_kernel_graphs</span><span class="p">()</span> <span class="p">{</span>
  
  <span class="c1">// Create a SearchContext</span>
  <span class="n">SearchContext</span> <span class="n">c</span><span class="p">;</span>
  <span class="n">c</span><span class="p">.</span><span class="n">level</span> <span class="o">=</span> <span class="n">SearchLevel</span><span class="o">::</span><span class="n">LV_KERNEL</span><span class="p">;</span>
  <span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">kernel</span><span class="o">::</span><span class="n">Graph</span><span class="o">&gt;</span><span class="p">();</span>

  <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">SerializedSearchContext</span><span class="o">&gt;</span> <span class="n">verified_graphs</span><span class="p">;</span>

  <span class="c1">// This is the main backtracking search function</span>
  <span class="n">generate_next_operator</span><span class="p">(</span>
      <span class="n">c</span><span class="p">,</span>
      <span class="p">[</span><span class="k">this</span><span class="p">](</span><span class="n">SearchContext</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">c</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">c</span><span class="p">.</span><span class="n">level</span> <span class="o">==</span> <span class="n">SearchLevel</span><span class="o">::</span><span class="n">LV_KERNEL</span> <span class="o">&amp;&amp;</span>
                <span class="k">this</span><span class="o">-&gt;</span><span class="n">verify</span><span class="p">(</span><span class="o">*</span><span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span><span class="p">);</span>
      <span class="p">},</span>
      <span class="n">verified_graphs</span><span class="p">,</span>
      <span class="cm">/*search_depth=*/</span><span class="mi">0</span><span class="p">,</span>
      <span class="cm">/*is_a_new_thread_start=*/</span><span class="nb">true</span><span class="p">);</span>


  <span class="n">save_results</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="n">KernelGraphGenerator</span><span class="o">::</span><span class="n">generate_next_operator</span><span class="p">(</span><span class="n">SearchContext</span> <span class="o">&amp;</span><span class="n">c</span><span class="p">,</span> <span class="p">...)</span> <span class="p">{</span>
    <span class="c1">// 1. Check if we found a valid graph</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">verify</span><span class="p">(</span><span class="n">c</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">verified_graphs</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">SerializedSearchContext</span><span class="p">(</span><span class="n">c</span><span class="p">));</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="c1">// 2. TODO: Something about abstract expr</span>
    <span class="n">std</span><span class="o">::</span><span class="n">unordered_map</span><span class="o">&lt;</span><span class="n">type</span><span class="o">::</span><span class="n">GuidType</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">AbstractExpr</span> <span class="k">const</span><span class="o">&gt;&gt;</span> <span class="n">algebraic_expr</span><span class="p">;</span>
    <span class="n">abstract_expr_eval</span><span class="p">(</span><span class="o">*</span><span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span><span class="p">,</span> <span class="n">algebraic_expr</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">c</span><span class="p">.</span><span class="n">tb_graph</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">abstract_expr_eval</span><span class="p">(</span><span class="o">*</span><span class="n">c</span><span class="p">.</span><span class="n">tb_graph</span><span class="p">,</span> <span class="n">algebraic_expr</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="c1">// 2.5 Lambda: check if adding an operator yields a valid abstract expression</span>
    <span class="c1">//     What is valid? check_abstract_expr checks if the expression is an subexpr to any final exprs</span>
  
    <span class="k">auto</span> <span class="n">infer_and_check_abstract_expr</span> <span class="o">=</span> <span class="p">[</span><span class="o">&amp;</span><span class="p">](</span><span class="k">auto</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">input_tensors</span><span class="p">,</span>
                                           <span class="k">auto</span> <span class="n">op_type</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">AbstractExpr</span> <span class="k">const</span><span class="o">&gt;&gt;</span> <span class="n">input_exprs</span> <span class="o">=</span>
          <span class="n">vector_map</span><span class="p">(</span><span class="n">input_tensors</span><span class="p">,</span>
                    <span class="p">[</span><span class="o">&amp;</span><span class="p">](</span><span class="k">auto</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">t</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">algebraic_expr</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="n">t</span><span class="p">.</span><span class="n">guid</span><span class="p">);</span> <span class="p">});</span>
      <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">AbstractExpr</span> <span class="k">const</span><span class="o">&gt;</span> <span class="n">expr</span> <span class="o">=</span>
          <span class="n">get_abstract_expr</span><span class="p">(</span><span class="n">op_type</span><span class="p">,</span> <span class="n">input_tensors</span><span class="p">,</span> <span class="n">input_exprs</span><span class="p">);</span>
      <span class="k">return</span> <span class="nf">check_abstract_expr</span><span class="p">(</span><span class="n">expr</span><span class="p">);</span>
    <span class="p">};</span>


    <span class="c1">// 2. Try adding a Kernel Operator (KNOperator)</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">type</span><span class="o">::</span><span class="n">KNOperatorType</span> <span class="n">op_type</span> <span class="o">:</span> <span class="n">dim_strategy</span><span class="p">.</span><span class="n">get_knop_cand</span><span class="p">())</span> <span class="p">{</span>
        <span class="c1">// Case K1: finish and verify the current graph</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">op_type</span> <span class="o">!=</span> <span class="n">type</span><span class="o">::</span><span class="n">KNOperatorType</span><span class="o">::</span><span class="n">KN_CUSTOMIZED_OP</span><span class="p">)</span> <span class="p">{</span>
          <span class="c1">// Case K2: generate a pre-defined kernel operator</span>

          <span class="c1">// .. Some checking</span>
          <span class="c1">// Add to the graph</span>
          <span class="n">KNOperator</span> <span class="o">*</span><span class="n">new_op</span> <span class="o">=</span> <span class="n">create_op</span><span class="p">(</span><span class="o">*</span><span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span><span class="p">,</span> <span class="n">op_type</span><span class="p">,</span> <span class="n">input_tensors</span><span class="p">);</span>
          <span class="k">if</span> <span class="p">(</span><span class="n">new_op</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span><span class="o">-&gt;</span><span class="n">operators</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">new_op</span><span class="p">);</span>
            <span class="c1">// Search deeper</span>
            <span class="n">generate_next_operator</span><span class="p">(</span>
                <span class="n">c</span><span class="p">,</span> <span class="n">verify</span><span class="p">,</span> <span class="n">verified_graphs</span><span class="p">,</span> <span class="n">search_depth</span> <span class="o">+</span> <span class="mi">1</span><span class="p">);</span>
          <span class="p">}</span>
          <span class="c1">// Backtrack</span>
          <span class="k">while</span> <span class="p">(</span><span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span><span class="o">-&gt;</span><span class="n">operators</span><span class="p">.</span><span class="n">back</span><span class="p">()</span> <span class="o">!=</span> <span class="n">old_last_op</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">delete</span> <span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span><span class="o">-&gt;</span><span class="n">operators</span><span class="p">.</span><span class="n">back</span><span class="p">();</span>
            <span class="n">c</span><span class="p">.</span><span class="n">kn_graph</span><span class="o">-&gt;</span><span class="n">operators</span><span class="p">.</span><span class="n">pop_back</span><span class="p">();</span>
          <span class="p">}</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
          <span class="c1">// Case K3: generate a graph-def kernel operator</span>
          <span class="c1">// .. Some checking</span>
          
          <span class="c1">// Enumerate grid_dim, block_dim, input_map, forloop_dim, forloop_range</span>
          <span class="k">for</span> <span class="p">(</span><span class="n">dim3</span> <span class="n">grid_dim</span> <span class="o">:</span> <span class="n">dim_strategy</span><span class="p">.</span><span class="n">get_grid_dim_cand</span><span class="p">(..))</span> <span class="p">{</span>
            <span class="k">for</span> <span class="p">(</span><span class="n">dim3</span> <span class="n">block_dim</span> <span class="o">:</span>
                 <span class="n">dim_strategy</span><span class="p">.</span><span class="n">get_block_dim_cand</span><span class="p">(..))</span> <span class="p">{</span>
              <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">int3</span><span class="o">&gt;</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">input_map</span> <span class="o">:</span>
                   <span class="n">dim_strategy</span><span class="p">.</span><span class="n">get_input_map_cand</span><span class="p">(..))</span> <span class="p">{</span>
                <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">forloop_dim</span> <span class="o">:</span>
                     <span class="n">dim_strategy</span><span class="p">.</span><span class="n">get_forloop_dim_cand</span><span class="p">(..))</span> <span class="p">{</span>
                  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">forloop_range</span> <span class="o">:</span>
                       <span class="n">dim_strategy</span><span class="p">.</span><span class="n">get_forloop_range_cand</span><span class="p">(..))</span> <span class="p">{</span>
                    <span class="n">c</span><span class="p">.</span><span class="n">tb_graph</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">threadblock</span><span class="o">::</span><span class="n">Graph</span><span class="o">&gt;</span><span class="p">(</span>
                        <span class="n">grid_dim</span><span class="p">,</span>
                        <span class="n">block_dim</span><span class="p">,</span>
                        <span class="n">forloop_range</span><span class="p">,</span>
                        <span class="n">config</span><span class="p">.</span><span class="n">reduction_dimx</span><span class="p">);</span>
                    
                    <span class="c1">// Create input tensors</span>
                    <span class="c1">// ..</span>

                    <span class="n">c</span><span class="p">.</span><span class="n">level</span> <span class="o">=</span> <span class="n">SearchLevel</span><span class="o">::</span><span class="n">LV_THREADBLOCK</span><span class="p">;</span>
                    <span class="c1">// Search deeper</span>
                    <span class="n">generate_next_operator</span><span class="p">(</span>
                        <span class="n">c</span><span class="p">,</span> <span class="n">verify</span><span class="p">,</span> <span class="n">verified_graphs</span><span class="p">,</span> <span class="n">search_depth</span> <span class="o">+</span> <span class="mi">1</span><span class="p">);</span>
                    
                    <span class="c1">// Backtrack</span>
                    <span class="n">c</span><span class="p">.</span><span class="n">level</span> <span class="o">=</span> <span class="n">SearchLevel</span><span class="o">::</span><span class="n">LV_KERNEL</span><span class="p">;</span>
                    <span class="n">c</span><span class="p">.</span><span class="n">tb_graph</span> <span class="o">=</span> <span class="nb">nullptr</span><span class="p">;</span>
                  <span class="p">}</span>
                <span class="p">}</span>
              <span class="p">}</span>
            <span class="p">}</span>
          <span class="p">}</span>
        <span class="p">}</span>

    <span class="p">}</span>
    
    <span class="c1">// 3. Try adding a Customized Operator (Thread Block Graph Search)</span>
    <span class="c1">//    I think this looks very similar to the Kernel Search, except no graph-def kernels</span>
    <span class="c1">// ...</span>
<span class="p">}</span>
</code></pre></div></div> <h4 id="32-dimensionality-strategy-dimstrategy">3.2 Dimensionality Strategy (<code class="language-plaintext highlighter-rouge">DimStrategy</code>)</h4> <p>To make the search efficient, Mirage uses <code class="language-plaintext highlighter-rouge">DimStrategy</code> to propose only “promising” candidates. It decides:</p> <ul> <li>Which operators to try next (<code class="language-plaintext highlighter-rouge">get_knop_cand</code>).</li> <li>How to split tensors across thread blocks (Grid Dimensions).</li> <li>How to tile tensors within a block (Block Dimensions).</li> </ul> <p><strong>Code Snippet (<code class="language-plaintext highlighter-rouge">src/search/dim_strategy.cc</code>)</strong>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">type</span><span class="o">::</span><span class="n">KNOperatorType</span><span class="o">&gt;</span> <span class="n">DimStrategy</span><span class="o">::</span><span class="n">get_knop_cand</span><span class="p">()</span> <span class="p">{</span>
  <span class="c1">// Returns a list of operators to explore (e.g., MATMUL, EXP, ADD), random_shuffled</span>
  <span class="k">return</span> <span class="n">config</span><span class="p">.</span><span class="n">knop_to_explore</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">dim3</span><span class="o">&gt;</span> <span class="n">DimStrategy</span><span class="o">::</span><span class="n">get_grid_dim_cand</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">DTensor</span><span class="o">&gt;</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">tensors</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Proposes grid dimensions (e.g., {128, 1, 1}, {256, 1, 1}) based on input shapes</span>
    <span class="c1">// ...</span>
<span class="p">}</span>
</code></pre></div></div> <h4 id="33-pruning-and-verification">3.3 Pruning and Verification</h4> <p>The search space is huge. Mirage uses <strong>Abstract Interpretation</strong> to prune invalid branches early. It maintains an abstract state (e.g., “this tensor represents $X \times W$”) and checks if adding an operator gives an subexpr of any final expressions.</p> <p>The core function here is <code class="language-plaintext highlighter-rouge">check_abstract_expr</code>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">bool</span> <span class="n">KernelGraphGenerator</span><span class="o">::</span><span class="n">check_abstract_expr</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">AbstractExpr</span> <span class="k">const</span><span class="o">&gt;</span> <span class="n">expr</span><span class="p">,</span> <span class="p">..)</span> <span class="p">{</span>
  <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">final_expr</span> <span class="o">:</span> <span class="n">computation_graph_output_exprs</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">subexpr_to_final_expr</span><span class="p">(</span><span class="n">expr</span><span class="p">))</span> <span class="p">{</span>
      <span class="k">return</span> <span class="nb">true</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">subexpr_to_final_expr</span><span class="p">(</span>
    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">AbstractExpr</span> <span class="k">const</span><span class="o">&gt;&gt;</span> <span class="k">const</span> <span class="o">&amp;</span><span class="n">exprs</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// The `egg_equiv` functions use e-graph's pattern matching to get the expressions that appear in the e-graph, indicating equivalance to any sub-expr of final expressions.</span>
  <span class="kt">bool</span> <span class="o">*</span><span class="n">results_in_raw_array</span> <span class="o">=</span>
      <span class="n">egg_equiv</span><span class="p">(</span><span class="n">exprs_c_str</span><span class="p">,</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">exprs</span><span class="p">.</span><span class="n">size</span><span class="p">()));</span>
  <span class="c1">// ..</span>
<span class="p">}</span>
</code></pre></div></div> <p>For implementation of <code class="language-plaintext highlighter-rouge">egg_equiv</code>, refer to <code class="language-plaintext highlighter-rouge">src/search/abstract_expr/abstract_subexpr/src/lib.rs</code>. <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00/cpp_examples/egg_tests.cc"><code class="language-plaintext highlighter-rouge">cpp_examples/egg_tests.cc</code></a> provides an example of how the e-graph things work here, run the script to try:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// THIS REQUIRES libcuda.so
</code></pre></div></div> <p>Once a complete graph is generated, the <strong>Verifier</strong> (<code class="language-plaintext highlighter-rouge">src/search/verification</code>) checks if it is functionally equivalent to the user’s specification.</p> <p>TODO: Add verification details.</p> <h3 id="4-compiler-pipeline">4. Compiler Pipeline</h3> <p>When you run <code class="language-plaintext highlighter-rouge">mi.superoptimize(graph)</code> (defined in <a href="https://github.com/arch-of-shadow/mirage/tree/uv_tutorial_00/python/mirage/kernel.py"><code class="language-plaintext highlighter-rouge">python/mirage/kernel.py</code></a>), the following pipeline executes:</p> <ol> <li><strong>Input Specification</strong>: You define the target computation (e.g., RMSNorm + Linear) using the Mirage Python API.</li> <li><strong>Search (<code class="language-plaintext highlighter-rouge">src/search</code>)</strong>: <ul> <li><code class="language-plaintext highlighter-rouge">KernelGraphGenerator</code> initializes the search.</li> <li>It explores the space of Kernel Graphs and Thread Block Graphs.</li> <li>It uses <code class="language-plaintext highlighter-rouge">DimStrategy</code> to guide the search and <code class="language-plaintext highlighter-rouge">AbstractExpr</code> to prune.</li> <li>Valid graphs are verified and collected.</li> </ul> </li> <li><strong>Transpilation (<code class="language-plaintext highlighter-rouge">src/transpiler</code>)</strong>: <ul> <li>The best discovered MuGraph is passed to the Transpiler.</li> <li><strong><code class="language-plaintext highlighter-rouge">transpiler_kn.cc</code></strong>: Generates the host code (kernel launch).</li> <li><strong><code class="language-plaintext highlighter-rouge">transpiler_tb.cc</code></strong>: Generates the device code (CUDA/Triton) for the Thread Block Graph. It handles complex details like: <ul> <li><code class="language-plaintext highlighter-rouge">get_dtensor_ptr</code>: Resolving global memory pointers.</li> <li><code class="language-plaintext highlighter-rouge">generate_tma_code_hopper</code>: Generating Tensor Memory Accelerator (TMA) instructions for NVIDIA Hopper GPUs.</li> </ul> </li> </ul> </li> <li><strong>Code Generation &amp; Execution</strong>: <ul> <li>The transpiler outputs a <code class="language-plaintext highlighter-rouge">.cu</code> file.</li> <li>NVCC compiles this file into a binary.</li> <li>Mirage loads and executes the binary.</li> </ul> </li> </ol> <p>For details,</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">KNGraph</span><span class="p">:</span>
  <span class="k">def</span> <span class="nf">superoptimize</span><span class="p">(</span>
    <span class="n">self</span><span class="p">,</span>
    <span class="n">imaps</span><span class="p">:</span> <span class="nb">list</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">omaps</span><span class="p">:</span> <span class="nb">list</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">griddims</span><span class="p">:</span> <span class="nb">list</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">blockdims</span><span class="p">:</span> <span class="nb">list</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">fmaps</span><span class="p">:</span> <span class="nb">list</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">franges</span><span class="p">:</span> <span class="nb">list</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">verbose</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
    <span class="n">config</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">backend</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">warmup_iters</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">16</span><span class="p">,</span>
    <span class="n">profile_iters</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">1000</span><span class="p">,</span>
    <span class="n">use_graph_dataset</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">use_cached_graphs</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">save_codes</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
    <span class="n">is_formal_verified</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
  <span class="p">):</span>
    <span class="c1"># Some checkpoint handling
</span>    <span class="c1"># ..
</span>
    <span class="c1"># Search for mugraphs
</span>    <span class="n">cygraphs</span> <span class="o">=</span> <span class="nf">search</span><span class="p">(</span>
        <span class="n">self</span><span class="p">.</span><span class="n">cygraph</span><span class="p">,</span>
        <span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">,</span>
        <span class="n">imaps</span><span class="o">=</span><span class="n">imaps</span><span class="p">,</span>
        <span class="n">omaps</span><span class="o">=</span><span class="n">omaps</span><span class="p">,</span>
        <span class="n">griddims</span><span class="o">=</span><span class="n">griddims</span><span class="p">,</span>
        <span class="n">blockdims</span><span class="o">=</span><span class="n">blockdims</span><span class="p">,</span>
        <span class="n">fmaps</span><span class="o">=</span><span class="n">fmaps</span><span class="p">,</span>
        <span class="n">franges</span><span class="o">=</span><span class="n">franges</span><span class="p">,</span>
        <span class="n">previous_checkpoint</span><span class="o">=</span><span class="n">previous_checkpoint</span><span class="p">,</span>
        <span class="n">verbose</span><span class="o">=</span><span class="n">verbose</span><span class="p">,</span>
        <span class="n">default_config</span><span class="o">=</span><span class="n">config</span><span class="p">,</span>
        <span class="n">is_formal_verified</span><span class="o">=</span><span class="n">is_formal_verified</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">all_graphs</span> <span class="o">=</span> <span class="p">[</span><span class="nc">KNGraph</span><span class="p">(</span><span class="n">g</span><span class="p">)</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">cygraphs</span><span class="p">]</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Finished search, discovering {} mugraphs ...</span><span class="sh">"</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">all_graphs</span><span class="p">)))</span>
    
    <span class="c1"># Backend-specific profiling and selection
</span>    <span class="k">if</span> <span class="n">backend</span> <span class="o">==</span> <span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">:</span>
        <span class="c1"># profile and use the best graph
</span>        <span class="c1"># ..
</span>        <span class="n">g</span><span class="p">.</span><span class="nf">compile</span><span class="p">(</span>
            <span class="n">async_</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
            <span class="n">inputs</span><span class="o">=</span><span class="n">input_tensors</span><span class="p">,</span>
            <span class="n">pipeline_stages</span><span class="o">=</span><span class="n">pipeline_stages</span><span class="p">,</span>
            <span class="n">num_warp_groups</span><span class="o">=</span><span class="n">num_warp_groups</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="n">starter</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nc">Event</span><span class="p">(</span><span class="n">enable_timing</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">ender</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nc">Event</span><span class="p">(</span><span class="n">enable_timing</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">synchronize</span><span class="p">()</span>
        <span class="n">starter</span><span class="p">.</span><span class="nf">record</span><span class="p">()</span>
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">profile_iters</span><span class="p">):</span>
            <span class="nf">g</span><span class="p">(</span><span class="n">inputs</span><span class="o">=</span><span class="n">input_tensors</span><span class="p">)</span>
        <span class="n">ender</span><span class="p">.</span><span class="nf">record</span><span class="p">()</span>
        <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">synchronize</span><span class="p">()</span>
        <span class="n">perf</span> <span class="o">=</span> <span class="n">starter</span><span class="p">.</span><span class="nf">elapsed_time</span><span class="p">(</span><span class="n">ender</span><span class="p">)</span> <span class="o">/</span> <span class="n">profile_iters</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">muGraph {}: profiled performance (ms) = {}</span><span class="sh">"</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="n">idx</span><span class="p">,</span> <span class="n">perf</span><span class="p">))</span>
        <span class="k">if</span> <span class="n">perf</span> <span class="o">&lt;</span> <span class="n">best_perf</span><span class="p">:</span>
            <span class="n">best_graph</span><span class="p">,</span> <span class="n">best_perf</span> <span class="o">=</span> <span class="n">g</span><span class="p">,</span> <span class="n">perf</span>
        <span class="k">return</span> <span class="n">best_graph</span>
    <span class="k">elif</span> <span class="n">backend</span> <span class="o">==</span> <span class="sh">"</span><span class="s">nki</span><span class="sh">"</span><span class="p">:</span>
        <span class="c1"># ..
</span>    <span class="k">elif</span> <span class="n">backend</span> <span class="o">==</span> <span class="sh">"</span><span class="s">triton</span><span class="sh">"</span><span class="p">:</span>
        <span class="c1"># ..
</span>        <span class="k">return</span> <span class="n">best_graph</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">assert</span> <span class="bp">False</span><span class="p">,</span> <span class="sh">"</span><span class="s">Unsupported backend</span><span class="sh">"</span>
        <span class="k">return</span> <span class="bp">None</span>
</code></pre></div></div> <p>For running on a non-GPU machine, you can use the <code class="language-plaintext highlighter-rouge">FAKE_GPU</code> option in <code class="language-plaintext highlighter-rouge">config.cmake</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 tutorial/c1_architecture/run_search.py
</code></pre></div></div> <p>But to my suprise, it gives:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mirage::DeviceMemoryManager: gpu_id(0) num_gpus(1)========== Search Configuration ==========
  max num threadblock graph op: 9
  max num kernel_graph op: 5
  max num threadblock graphs: 1
  max num threadblock graph inputs: 3
  max num threadblock graph outputs: 2
  search_thread: 16
  imaps to explore:
  imap combs to explore:
  omaps to explore:
  grid dims to explore:
  block dims to explore:
  fmaps to explore:
  franges to explore:4 16 64 
num_thread = 16
num_tasks = 0 tasks901, Random tests: 404, Valid mugraphs: 0, Time: 7.874953

[Search] Second step finished. Time elapsed: 7.921125sec
[Search] Total states explored: 53934
[Search] Random tests performed: 404
[Serach] Valid kernel graphs explored: 0
Finished search, discovering 0 mugraphs ...
</code></pre></div></div> <p>There are no valid mugraphs found. It seems the verifier is not working well in FAKE_GPU mode.</p> <p>When running on a real GPU machine, you can try:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 demo/demo_rms_norm.py 
</code></pre></div></div> <p>It gives:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mirage::DeviceMemoryManager: gpu_id(0) num_gpus(1)========== Search Configuration ==========
  max num threadblock graph op: 9
  max num kernel_graph op: 5
  max num threadblock graphs: 1
  max num threadblock graph inputs: 3
  max num threadblock graph outputs: 2
  search_thread: 16
  imaps to explore:
  imap combs to explore:
  omaps to explore:
  grid dims to explore:
  block dims to explore:
  fmaps to explore:
  franges to explore:4 16 64 
num_thread = 16
num_tasks = 0 tasks5801, Random tests: 1855, Valid mugraphs: 9, Time: 25.430370

Transpiling 9 muGraphs ...
muGraph 0: profiled performance (ms) = 0.1168353271484375
muGraph 1: profiled performance (ms) = 0.0968253402709961
muGraph 2: profiled performance (ms) = 0.753455078125
muGraph 3: profiled performance (ms) = 0.10145689392089843
muGraph 4: profiled performance (ms) = 0.1066618881225586
muGraph 5: profiled performance (ms) = 0.09312665557861328
muGraph 6: profiled performance (ms) = 0.10233241271972657
muGraph 7: profiled performance (ms) = 0.10170265960693359
muGraph 8: profiled performance (ms) = 0.725074951171875
Best muGraph run time (ms):  0.09327104187011719
</code></pre></div></div> <h2 id="summary">Summary</h2> <p>Mirage represents a shift from heuristic-based compilers to search-based superoptimizers.</p> <ol> <li><strong>MuGraph</strong> captures the critical distinction between compute and memory hierarchies.</li> <li><strong>Backtracking Search</strong> explores tiling and fusion strategies automatically.</li> <li><strong>Abstract Interpretation</strong> prunes the search space to keep runtime manageable.</li> </ol> <p>While this post focused on the architecture and search, the final piece of the puzzle is the <strong>Transpiler</strong>, which generates the intricate CUDA code (including Tensor Memory Accelerator instructions for Hopper GPUs) from the MuGraph. That, however, is a topic for another day.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Diving into Mirage's MuGraph IR and search algorithms - how a superoptimizer finds GPU kernels that hand-written fusion rules could never dream of]]></summary></entry><entry><title type="html">A First Look at PyPTO. “Tile &amp;amp; Human”</title><link href="https://uv-xiao.github.io/blog/2025/first-look-pypto/" rel="alternate" type="text/html" title="A First Look at PyPTO. “Tile &amp;amp; Human”"/><published>2025-11-10T00:00:00+00:00</published><updated>2025-11-10T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/first-look-pypto</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/first-look-pypto/"><![CDATA[<blockquote> <p>Reference: <a href="https://zhuanlan.zhihu.com/p/1966608093946320154">Article by Zhihu user 风雨携晴</a></p> </blockquote> <blockquote> <p>Reference: CANN Public Resources: <a href="https://gitcode.com/cann/cann-recipes-infer/blob/master/docs/models/deepseek-v3.2-exp/deepseek_v3.2_exp_pypto_operator_guide.md#%E7%AE%80%E4%BB%BB">Documentation</a>, <a href="https://gitcode.com/cann/cann-recipes-infer/tree/master/ops/pypto">Code</a></p> </blockquote> <h2 id="pyptos-core-philosophy-human-in-the-loop">PyPTO’s Core Philosophy: “Human-in-the-Loop”</h2> <p>PyPTO was designed with a clear understanding from the start: the operator optimization process is full of NP-Hard problems, and trying to find the optimal solution through automated configuration search in a short amount of time is unrealistic.</p> <p>Therefore, PyPTO’s design philosophy is not to “directly solve the problem,” but rather to <strong>build a mechanism for solving the problem</strong>.</p> <p>It chose a path similar to hardware RTL development. In the RTL development flow, from timing analysis to place and route, the tools do not attempt to solve all NP-Hard problems. Instead, they provide feasibility judgments and final performance evaluations based on the designer’s (a human’s) experience and constraints.</p> <p>PyPTO hopes to draw from this model by <strong>introducing human experience into the optimization loop</strong>, reducing the complexity that users have to face directly and allowing developers to focus on leveraging their professional insights.</p> <p>To achieve this goal, PyPTO is <strong>centered around on-chip SRAM management, building a white-box toolchain</strong> with rich debugging tools and intuitive visualization capabilities.</p> <h2 id="pypto-introduction-and-key-features">PyPTO Introduction and Key Features</h2> <h3 id="motivation">Motivation</h3> <p><code class="language-plaintext highlighter-rouge">Operator Optimization</code> (Compute/Memory Opt.) -&gt; <code class="language-plaintext highlighter-rouge">Operator Fusion</code> (Reduce overhead, expand optimization space) -&gt; <code class="language-plaintext highlighter-rouge">New Programming Paradigm</code> (To handle the high complexity brought by fusion)</p> <p>Ultimately, PyPTO chose <strong>Tile</strong> as the new programming paradigm to support efficient fused operator design.</p> <h3 id="summary-of-features">Summary of Features</h3> <ol> <li><strong>Automated SRAM Management</strong>: It can automatically generate read/write instructions without needing to manually manage SRAM. At the same time, developers can control Tiling partitioning and SRAM utilization strategies through parameter configuration, which is very similar to using <code class="language-plaintext highlighter-rouge">directives</code> to guide synthesis in HLS.</li> <li><strong>Abstract PTO (Parallel Tile Operations) Instructions</strong>: It defines a set of platform-independent PTO instructions. By implementing these instructions on different hardware platforms, it achieves automatic operator code migration.</li> <li><strong>MPMD (Multiple Program Multiple Data) Runtime</strong>: Dependencies between tasks are managed at the task level rather than through global synchronization, improving execution efficiency.</li> </ol> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/in_posts/pypto-480.webp 480w,/assets/img/in_posts/pypto-800.webp 800w,/assets/img/in_posts/pypto-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/in_posts/pypto.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h3 id="abstraction-layers">Abstraction Layers</h3> <p>PyPTO’s data object abstraction hierarchy is very clear:</p> <p><code class="language-plaintext highlighter-rouge">Tensor</code> -&gt; <code class="language-plaintext highlighter-rouge">[Tiling]</code> -&gt; <code class="language-plaintext highlighter-rouge">Tile</code></p> <p>Logically, developers only need to care about the computation logic, without needing to be aware of the underlying memory, scheduling, and hardware details. The table below shows some of its core operations (PTO’s O):</p> <table> <thead> <tr> <th>Category</th> <th>Example Instruction</th> <th>Function Description</th> </tr> </thead> <tbody> <tr> <td><strong>Element-wise Op</strong></td> <td><code class="language-plaintext highlighter-rouge">TADD</code></td> <td>Performs element-wise addition on two Tile data blocks</td> </tr> <tr> <td><strong>Tile-Scalar Op</strong></td> <td><code class="language-plaintext highlighter-rouge">TADDS</code></td> <td>Adds a scalar to each element in a Tile data block</td> </tr> <tr> <td><strong>Axis Op</strong></td> <td><code class="language-plaintext highlighter-rouge">TROWSUM</code></td> <td>Performs a sum operation on each row of a Tile data block</td> </tr> <tr> <td><strong>Matrix Multiply Op</strong></td> <td><code class="language-plaintext highlighter-rouge">MAMULB</code></td> <td>Performs matrix multiplication on two Tile data blocks</td> </tr> <tr> <td><strong>Fixed Pipe Op</strong></td> <td><code class="language-plaintext highlighter-rouge">TMOV</code></td> <td>Moves a Tile element from a source register to a destination register</td> </tr> <tr> <td><strong>Memory Op</strong></td> <td><code class="language-plaintext highlighter-rouge">TLOAD</code> / <code class="language-plaintext highlighter-rouge">TSTORE</code></td> <td>Loads data from memory to a Tile or stores it back</td> </tr> <tr> <td><strong>Complex Op</strong></td> <td><code class="language-plaintext highlighter-rouge">TCVT</code></td> <td>Performs type conversion on a Tile data block</td> </tr> </tbody> </table> <h2 id="core-mechanisms-tiling-management-and-human-in-the-loop">Core Mechanisms: Tiling Management and Human-In-The-Loop</h2> <h3 id="tiling-management">Tiling Management</h3> <p>PyPTO provides two ways to manage Tiling:</p> <ol> <li><strong>Framework-Built-in Tiling</strong>: By setting the <code class="language-plaintext highlighter-rouge">Tile Shape</code> parameter, the framework automatically expands the Tensor computation graph into a Tile computation graph.</li> <li><strong>Custom Tiling</strong>: Users can define their own partitioning of the Tile computation graph and manually insert data movement instructions.</li> </ol> <p>The inputs and outputs of each partitioned subgraph (Kernel) are in Global Memory, while the subgraph internally uses only on-chip SRAM. This partitioning is a complex trade-off process that needs to consider <code class="language-plaintext highlighter-rouge">operation affinity</code>, <code class="language-plaintext highlighter-rouge">subgraph isomorphism</code> (to improve reusability), and <code class="language-plaintext highlighter-rouge">subgraph-level parallelism</code>.</p> <h3 id="human-in-the-loop-optimization-approach">Human-In-The-Loop Optimization Approach</h3> <p>The entire process, from operator description to final execution on hardware, involves multiple stages such as Tile expansion, graph partitioning, and runtime scheduling. Many of these stages face NP-Hard problems, making it unrealistic to rely entirely on the framework to automatically generate optimal solutions.</p> <p>Therefore, <strong>human participation becomes crucial</strong>. PyPTO’s approach is to provide the necessary interfaces to allow users to participate in this optimization process. <code class="language-plaintext highlighter-rouge">Tiling</code> management is one area where users can intervene, but what about graph partitioning and runtime scheduling? This is a very interesting question in PyPTO’s design.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/in_posts/pypto-human-in-the-loop-480.webp 480w,/assets/img/in_posts/pypto-human-in-the-loop-800.webp 800w,/assets/img/in_posts/pypto-human-in-the-loop-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/in_posts/pypto-human-in-the-loop.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="code-example-deepseek-indexer-prolog-operator-analysis">Code Example: DeepSeek Indexer Prolog Operator Analysis</h2> <p>Next, we’ll dive deep into PyPTO’s programming practices through an example of a fused operator, <code class="language-plaintext highlighter-rouge">Indexer Prolog</code>. This operator is the first half of the Attention calculation in DeepSeek-V3.2-Exp, and its core value lies in:</p> <ol> <li><strong>Fast Filtering</strong>: Efficiently computes similarity scores between a Query Token and all historical Tokens.</li> <li><strong>Quantization Acceleration</strong>: Significantly reduces computation and memory usage through W8A8/A8/C8 quantization.</li> <li><strong>Outlier Suppression</strong>: Uses the Hadamard transform to improve quantization accuracy.</li> </ol> <p>You can find its implementation in <a href="https://gitcode.com/cann/cann-recipes-infer/tree/master/ops/pypto/src/lightning_indexer_prolog_pto/op_kernel/">this repository</a>.</p> <h3 id="overall-structure">Overall Structure</h3> <ul> <li><code class="language-plaintext highlighter-rouge">.h</code> <strong>file</strong>: Defines the operator contract (input/output) and Tiling configuration (declarations only).</li> <li><code class="language-plaintext highlighter-rouge">.cpp</code> <strong>file</strong>: Implements the IR construction logic (compile-time metaprogramming).</li> </ul> <h3 id="h-file-operator-contract-and-configuration"><code class="language-plaintext highlighter-rouge">.h</code> File: Operator Contract and Configuration</h3> <p>The role of the header file is very clear:</p> <ul> <li><strong>Compile-Time Interface</strong>: Declares the Shape, Type, and Layout of the operator’s inputs/outputs to the compiler.</li> <li><strong>Performance Configuration Table</strong>: Centralizes all Tiling-related parameters to support AutoTuning.</li> </ul> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// quant_lightning_indexer_prolog.h</span>

<span class="c1">// 1. Tiling and performance-related parameter configuration</span>
<span class="k">struct</span> <span class="nc">QuantIndexerConfigs</span> <span class="p">{</span>
    <span class="c1">// Tile params</span>
    <span class="n">std</span><span class="o">::</span><span class="n">array</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="n">TILE_CUBE_DIM</span><span class="o">&gt;</span> <span class="n">qLinear</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">array</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="n">TILE_CUBE_DIM</span><span class="o">&gt;</span> <span class="n">qHd</span><span class="p">;</span>
    <span class="c1">// ... other parameters ...</span>

    <span class="c1">// Config params</span>
    <span class="n">std</span><span class="o">::</span><span class="n">set</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">unrollList</span> <span class="o">=</span> <span class="p">{</span><span class="mi">32</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">1</span><span class="p">};</span>
    <span class="kt">int</span> <span class="n">cycleUpperBound</span> <span class="o">=</span> <span class="mi">8192</span><span class="p">;</span>
    <span class="c1">// ...</span>
<span class="p">};</span>

<span class="c1">// 2. Input/Output Contract</span>
<span class="k">struct</span> <span class="nc">QuantIndexerPrologInput</span> <span class="p">{</span> <span class="k">const</span> <span class="n">Tensor</span> <span class="o">&amp;</span><span class="n">x</span><span class="p">;</span> <span class="p">...</span> <span class="p">};</span>
<span class="k">struct</span> <span class="nc">QuantIndexerPrologOutput</span> <span class="p">{</span> <span class="n">Tensor</span> <span class="o">&amp;</span><span class="n">qInt8</span><span class="p">;</span> <span class="p">...</span> <span class="p">};</span>

<span class="c1">// 3. Function declaration (for compile-time linking)</span>
<span class="kt">void</span> <span class="nf">QuantLightningIndexerProlog</span><span class="p">(...);</span>
</code></pre></div></div> <h3 id="cpp-file-ir-construction-compile-time-metaprogramming"><code class="language-plaintext highlighter-rouge">.cpp</code> File: IR Construction (Compile-Time Metaprogramming)</h3> <p>The core purpose of the <code class="language-plaintext highlighter-rouge">.cpp</code> file is to <strong>construct the Intermediate Representation (IR) of the computation graph</strong>, not to execute the computation.</p> <h4 id="operator-entry-point">Operator Entry Point</h4> <p>The <code class="language-plaintext highlighter-rouge">QuantLightningIndexerProlog</code> function is the operator’s entry point. It is responsible for registering the function and orchestrating the computation flow.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// Operator Entry Point
void QuantLightningIndexerProlog(...) {
    // Set Machine Global Config, such as scheduling policy
    config::SetRuntimeOption("machine_sched_mode", ...);

    // Register the function and declare inputs, outputs, buffers, etc.
    FUNCTION("QuantLightningIndexerProlog",
             {inputs.x, inputs.qNorm, ...}, // Inputs
             {outputs.qInt8, outputs.qScale, ...}, // Outputs
             {...}) // What are these?
    {
        // Call the core computation graph construction logic
        QuantLightningIndexerPrologCompute(inputs, outputs, attrs, configs);
    }
}
</code></pre></div></div> <h4 id="operator-construction-quantlightningindexerprologcompute">Operator Construction (<code class="language-plaintext highlighter-rouge">QuantLightningIndexerPrologCompute</code>)</h4> <p>This function is the real core. It uses the APIs provided by PyPTO to <strong>declaratively build the computation graph</strong>.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">QuantLightningIndexerPrologCompute</span> <span class="p">(...)</span> <span class="p">{</span>
    <span class="c1">// 1. Set Pass options, this is the core interface PyPTO provides for manual optimization</span>
    <span class="n">config</span><span class="o">::</span><span class="n">SetPassOption</span><span class="p">(</span><span class="s">"nbuffer_merge_mode"</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>
    <span class="c1">// ...</span>

    <span class="c1">// 2. Get Tensor metadata</span>
    <span class="n">DataType</span> <span class="n">xDtype</span> <span class="o">=</span> <span class="n">inputs</span><span class="p">.</span><span class="n">x</span><span class="p">.</span><span class="n">GetDataType</span><span class="p">();</span>
    <span class="n">SymbolicScalar</span> <span class="n">t</span> <span class="o">=</span> <span class="n">GetInputShape</span><span class="p">(</span><span class="n">inputs</span><span class="p">.</span><span class="n">x</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span> <span class="c1">// Get dynamic axis</span>

    <span class="c1">// 3. Create new Tensors based on metadata (for intermediate computations)</span>
    <span class="n">Tensor</span> <span class="n">wQb</span><span class="p">(...,</span> <span class="s">"wQb"</span><span class="p">,</span> <span class="n">TileOpFormat</span><span class="o">::</span><span class="n">TILEOP_NZ</span><span class="p">);</span>
    <span class="c1">// ...</span>

    <span class="c1">// 4. Define the computation logic (in a loop)</span>
    <span class="c1">// This is a compile-time loop that will be unrolled during compilation</span>
    <span class="n">LOOP</span><span class="p">(</span><span class="s">"QuantIndexerPrologLoop"</span><span class="p">,</span> <span class="p">...,</span> <span class="n">unrollList</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">unrollLength</span> <span class="o">:</span> <span class="n">unrollList</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">UNROLL</span><span class="p">(</span><span class="n">unrollLength</span><span class="p">)</span> <span class="p">{</span>
                <span class="c1">// 5. Manually set the Tiling Shape</span>
                <span class="c1">// Note: Tiling here is not fully automatic; developers need to set it manually</span>
                <span class="n">TileShape</span><span class="o">::</span><span class="n">Current</span><span class="p">().</span><span class="n">SetCubeTile</span><span class="p">({</span><span class="n">qLinear</span><span class="p">[</span><span class="n">L0M_INDEX</span><span class="p">],</span> <span class="p">...});</span>

                <span class="c1">// 6. Construct a computation graph node</span>
                <span class="k">auto</span> <span class="n">qS32</span> <span class="o">=</span> <span class="n">Matrix</span><span class="o">::</span><span class="n">Matmul</span><span class="o">&lt;</span><span class="p">...</span><span class="o">&gt;</span><span class="p">(</span><span class="n">DT_INT32</span><span class="p">,</span> <span class="n">qNorm</span><span class="p">,</span> <span class="n">wQb</span><span class="p">);</span>

                <span class="c1">// 7. You can also call functions to construct subgraphs</span>
                <span class="n">std</span><span class="o">::</span><span class="n">tuple</span><span class="o">&lt;</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">Tensor</span><span class="o">&gt;</span> <span class="n">qRes</span> <span class="o">=</span> <span class="n">PrologQuant</span><span class="p">(</span><span class="n">qHadamard</span><span class="p">);</span>

                <span class="c1">// 8. Write the final result back to the output Tensor</span>
                <span class="n">Assemble</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">(</span><span class="n">qRes</span><span class="p">),</span> <span class="p">...,</span> <span class="n">outputs</span><span class="p">.</span><span class="n">qInt8</span><span class="p">);</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="summary-of-core-concepts">Summary of Core Concepts</h3> <p>From this code analysis, we can draw several core conclusions about PyPTO:</p> <ul> <li><strong>Declarative Programming</strong>: The code in the <code class="language-plaintext highlighter-rouge">.cpp</code> file does not execute computations but instead returns IR AST nodes (like the <code class="language-plaintext highlighter-rouge">PrologQuant()</code> function).</li> <li><strong>Compile-Time Loops</strong>: <code class="language-plaintext highlighter-rouge">LOOP</code> / <code class="language-plaintext highlighter-rouge">UNROLL</code> are compile-time directives used for code expansion and generating different Kernel versions, not runtime loops.</li> <li><strong>Explicit Optimization</strong>: The operator optimization process, such as Tiling and passing pass parameters, is done explicitly in the code.</li> </ul> <h2 id="thoughts-and-discussion">Thoughts and Discussion</h2> <p>PyPTO is undoubtedly an <strong>EDSL</strong> similar to Chisel (for hardware design) and Halide (for high-performance computing). Its <code class="language-plaintext highlighter-rouge">.cpp</code> file is essentially for constructing a computation graph, and it has strong characteristics of a <code class="language-plaintext highlighter-rouge">schedule language</code>, but it doesn’t seem to pursue a strict <strong>logic-scheduling separation</strong> like Halide (which is a design choice worth discussing in itself).</p> <p>Looking at the operator construction code, apart from Tiling which requires deep user involvement, tasks like <strong>graph partitioning, mapping, scheduling, and concurrency are all handled automatically by the compiler/runtime</strong>. At this level, the programming abstraction provided by PyPTO is quite high, similar to Triton.</p> <p>However, the amount of <code class="language-plaintext highlighter-rouge">boilerplate</code> code in practice still seems a bit high, which might be an inherent challenge of the C++ EDSL approach. A point of critique here is, isn’t it called PyPTO? Why is everything written in C++?</p> <p>But this is not the core issue. I believe the essence of a programming language’s design lies in its <strong>abstraction layer</strong>. PyPTO has chosen a high level of abstraction, and its greatest challenge is: <strong>how to design the interaction mechanism between the user and the compiler to truly achieve a <code class="language-plaintext highlighter-rouge">human-"HAPPY"-in-the-loop</code> and bring out the effectiveness of the “white-box” toolchain.</strong></p> <p>Is the <code class="language-plaintext highlighter-rouge">config::SetPassOption</code> seen in the code the best way to do this? This remains to be seen with further experimentation. But for now, I haven’t gotten an environment where I can run this. I will provide updates later.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Exploring Huawei's PyPTO - a tile-based programming paradigm that puts humans back in the optimization loop, because sometimes NP-Hard problems need human wisdom]]></summary></entry><entry><title type="html">(Paper Reading) ACT, Automatically Generating Compiler Backends from Tensor Accelerator ISA Descriptions</title><link href="https://uv-xiao.github.io/blog/2025/act-paper/" rel="alternate" type="text/html" title="(Paper Reading) ACT, Automatically Generating Compiler Backends from Tensor Accelerator ISA Descriptions"/><published>2025-10-29T00:00:00+00:00</published><updated>2025-10-29T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/act-paper</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/act-paper/"><![CDATA[<pre><code class="language-txt">@misc{act-arxiv,
  title = {ACT: Automatically Generating Compiler Backends from Tensor Accelerator ISA Descriptions},
  author = {Jain, Devansh and Pardeshi, Akash and Frigo, Marco and Patel, Krut and Khulbe, Kaustubh and Arora, Jai and Mendis, Charith},
  year = {2025},
  eprint = {2510.09932},
  archiveprefix = {arXiv},
  primaryclass = {cs.PL},
  url = {https://arxiv.org/abs/2510.09932},
  doi = {10.48550/arXiv.2510.09932},
  month = oct
}
</code></pre> <p>This paper introduces the “ACT Backend Generator: Automatically generates sound and complete compiler backends just from TAIDL specification” of the ACT (Accelerator Compiler Toolkit) Ecosystem, an ecosystem that automatically generates complete compiler backends and essential software tooling from high-level ISA specifications of tensor accelerators.</p> <p>This work is very interesting. It is even just the work I want to do! Consider we have a set of RISC-V extension instructions described, the automatically generated compiler is the key to use them properly for final acceleration effects. We have done the APS (ICCAD ’25) work, and a powerful compiler is definitely a missing piece now.</p> <h3 id="the-big-picture-what-problem-is-act-solving">The Big Picture: What Problem is ACT Solving?</h3> <p>Imagine you are a hardware architect and you’ve just designed a new, super-fast chip (a “tensor accelerator”) specifically for running AI models. For anyone to use your chip, they need a compiler that can translate standard AI programs (like those written in TensorFlow or PyTorch) into the specific machine code your chip understands.</p> <p><strong>The Problem:</strong> Writing this compiler backend is incredibly difficult, time-consuming, and needs to be redone for every new accelerator design. Most new accelerators either don’t get a proper compiler, forcing people to write code by hand, or they get a very limited one that only supports a few operations. This severely limits their adoption.</p> <p><strong>ACT’s Solution:</strong> ACT is a “compiler-compiler”. You don’t write the compiler backend yourself. Instead, you provide ACT with a high-level description of your new accelerator as ISA—what memory it has and what instructions it can execute. From this description alone, ACT automatically generates a complete, correct, and efficient compiler backend for you.</p> <hr/> <h3 id="formal-things">Formal Things</h3> <p>Let’s think about the formal things from the paper in an intuitive style.</p> <h4 id="1-how-to-describe-a-tensor-accelerator-isa-description">1. How to Describe a Tensor Accelerator? (ISA Description)</h4> <p>ACT needs a “blueprint” of the accelerator. This is just the <a href="https://dl.acm.org/doi/10.1145/3725843.3756075">TAIDL at MICRO’25</a>.</p> <p>This blueprint has two parts:</p> <ul> <li><strong>Data Model (<code class="language-plaintext highlighter-rouge">D^H</code>):</strong> This describes the memory hierarchy. Think of it as listing the available storage. For the paper’s example accelerator, <code class="language-plaintext highlighter-rouge">H_QKV</code>, this is: <ul> <li><code class="language-plaintext highlighter-rouge">d0</code>: A big, main memory (Global Memory).</li> <li><code class="language-plaintext highlighter-rouge">d1</code>: A small, fast scratchpad memory (Main Scratchpad).</li> <li><code class="language-plaintext highlighter-rouge">d2</code>: Another even smaller, faster buffer (Intermediate Buffer).</li> </ul> </li> <li> <p><strong>Instruction Set (<code class="language-plaintext highlighter-rouge">Θ^H</code>):</strong> This is the list of commands the accelerator understands. The key insight of ACT is how it describes what these commands <em>do</em>. Instead of inventing a new language, it describes them using the same basic <strong>tensor operators</strong> (like <code class="language-plaintext highlighter-rouge">reshape</code>, <code class="language-plaintext highlighter-rouge">dot</code>, <code class="language-plaintext highlighter-rouge">transpose</code>) from XLA-HLO IR.</p> <ul> <li><strong>Example: The <code class="language-plaintext highlighter-rouge">load_rm</code> instruction.</strong> This instruction moves data from main memory (<code class="language-plaintext highlighter-rouge">d0</code>) to the scratchpad (<code class="language-plaintext highlighter-rouge">d1</code>). The paper doesn’t just treat it as a black box. It formally describes its behavior as <code class="language-plaintext highlighter-rouge">y = bitcvt(reshape(x1))</code>. This means the instruction takes a 1D stream of bytes (<code class="language-plaintext highlighter-rouge">x1</code>), reshapes it into a 2D matrix, and bit-casts the data type.</li> <li><strong>Parameterized Instructions:</strong> Notice that <code class="language-plaintext highlighter-rouge">load_rm</code> is parameterized. It can load a variable number of rows (<code class="language-plaintext highlighter-rouge">n</code>). This is a huge challenge that ACT is designed to handle. <code class="language-plaintext highlighter-rouge">n</code> is a “computational attribute” (α), while the memory addresses it reads from/writes to are “addressing attributes” (β).</li> </ul> </li> </ul> <p>Here, the interesting things are the “computational attribute” (α) and “addressing attributes” (β). They are also the “design space” of the tensor compilation problem consider in this paper.</p> <blockquote> <p>But, are they truly general enough?</p> </blockquote> <h4 id="2-what-does-it-mean-for-compiled-code-to-be-correct-semantic-equivalence-r">2. What Does It Mean for Compiled Code to Be “Correct”? (Semantic Equivalence, ≡R)</h4> <p>Although the paper treats the “correctness” as the most critical question, I take more interest in compiler rather than verification. So I won’t describe any details here but only summarize some key ideas.</p> <ul> <li><strong>The Intuitive Idea:</strong> Imagine you have the original program (a computation graph <code class="language-plaintext highlighter-rouge">G</code>) and the final assembly code (<code class="language-plaintext highlighter-rouge">ASM</code>). To check if they are equivalent, you perform the following thought experiment (visualized in Figure 5): <ol> <li>Take some input tensors (<code class="language-plaintext highlighter-rouge">X</code>).</li> <li><strong>LHS (Left-Hand Side):</strong> Run the assembly code. This involves loading the inputs into the accelerator’s main memory, executing the instruction stream, and then reading the final result back from memory.</li> <li><strong>RHS (Right-Hand Side):</strong> Run the original high-level graph <code class="language-plaintext highlighter-rouge">G</code> on the same inputs to get the “golden” output tensor.</li> <li><strong>Comparison:</strong> Flatten both the final output from the LHS and the golden output from the RHS into a single, long stream of bytes. If these two byte streams are absolutely identical for <em>any possible input <code class="language-plaintext highlighter-rouge">X</code></em>, then the assembly code is semantically equivalent to the original graph.</li> </ol> </li> </ul> <blockquote> <p>Here, byte-flattening (<code class="language-plaintext highlighter-rouge">bflat</code>) looks kind of verbose…</p> </blockquote> <h4 id="3-what-makes-a-good-compiler-backend-generator-soundness--completeness">3. What Makes a Good Compiler Backend Generator? (Soundness &amp; Completeness)</h4> <ul> <li><strong>Soundness:</strong> “If ACT generates a compiler backend, and that backend successfully compiles a program, the resulting assembly code is <strong>guaranteed to be correct</strong>.” It will never produce a wrong answer. This is the bare minimum for a compiler.</li> <li><strong>Completeness:</strong> “If it is <em>possible</em> to write correct assembly code for a given program on a given accelerator, the backend generated by ACT is <strong>guaranteed to find a solution</strong>.” It won’t fail and give up if a valid program exists. This is a very strong and desirable property.</li> </ul> <p>This is what the paper wants to achieve.</p> <hr/> <h3 id="the-compilation-flow-in-action">The Compilation Flow in Action</h3> <p>Now, let’s walk through how ACT actually compiles a program, using the paper’s running example: compiling the <code class="language-plaintext highlighter-rouge">G_QKV</code> kernel (from Fig. 2) for the <code class="language-plaintext highlighter-rouge">H_QKV</code> accelerator (from Fig. 3). The process is shown in Figure 8.</p> <p><img src="" alt=""/></p> <p>The compilation is a two-phase process:</p> <ol> <li><strong>Phase 1: Instruction Selection.</strong> What is the sequence of accelerator instructions we should use?</li> <li><strong>Phase 2: Memory Allocation.</strong> Where in the scratchpads should we store the intermediate data?</li> </ol> <h4 id="phase-1-instruction-selection-finding-the-what-or-solving-α">Phase 1: Instruction Selection (Finding the “What”; Or solving α)</h4> <p>This phase’s goal is to convert the high-level tensor graph into a graph of accelerator instructions. It uses a powerful technique called <strong>Equality Saturation</strong> with an <strong>e-graph</strong>.</p> <ul> <li> <p><strong>What is an E-Graph?</strong> An e-graph is a data structure that efficiently stores a massive collection of programs that are all semantically equivalent to each other.</p> </li> <li><strong>Step 1: E-Graph Initializer (§5.1)</strong> <ul> <li><strong>Action:</strong> The initial <code class="language-plaintext highlighter-rouge">G_QKV</code> computation graph is loaded into the e-graph.</li> <li><strong>Example:</strong> Figure 9(a) shows the initial e-graph. At this point, it just contains the original program’s operators (<code class="language-plaintext highlighter-rouge">reshape</code>, <code class="language-plaintext highlighter-rouge">bitcvt</code>, etc.).</li> </ul> </li> <li><strong>Step 2: Rewrite Applier / E-Graph Explorer (§5.2)</strong> <ul> <li><strong>Action:</strong> This is the core of instruction selection. ACT automatically generates rewrite rules from the ISA description. <ul> <li>An “IR-to-ISA” rule is created for each instruction. For example: <code class="language-plaintext highlighter-rouge">bitcvt(reshape(x))</code> <strong>can be rewritten as</strong> <code class="language-plaintext highlighter-rouge">load_rm(x)</code>. Another rule might be <code class="language-plaintext highlighter-rouge">dot(x1, x2)</code> <strong>can be rewritten as</strong> <code class="language-plaintext highlighter-rouge">gemm(x1, x2)</code>.</li> </ul> </li> <li>ACT repeatedly applies these rules to the e-graph, discovering new, equivalent ways to compute the same results using the accelerator’s instructions.</li> <li><strong>Example:</strong> In Figure 9(b), you can see the e-graph has been “explored”. New nodes for <code class="language-plaintext highlighter-rouge">load_rm</code>, <code class="language-plaintext highlighter-rouge">transpose</code> (which could map to <code class="language-plaintext highlighter-rouge">load_cm</code>), <code class="language-plaintext highlighter-rouge">gemm</code>, and <code class="language-plaintext highlighter-rouge">mov</code> have been added. The e-graph now contains both the original implementation <em>and</em> several possible implementations using the <code class="language-plaintext highlighter-rouge">HQKV</code> instruction set.</li> </ul> </li> <li><strong>Step 3: Graph Extractor (§5.3)</strong> <ul> <li><strong>Action:</strong> After exploring the possibilities, ACT extracts one valid “plan” from the e-graph. This plan is a graph made up entirely of accelerator instructions. It’s called a <strong>pii graph</strong> (partially instantiated instruction graph) because we know <em>what</em> instructions to run (e.g., <code class="language-plaintext highlighter-rouge">load_rm</code>), but we don’t yet know the exact memory addresses (the <code class="language-plaintext highlighter-rouge">?</code> in <code class="language-plaintext highlighter-rouge">load_rm64,?</code>).</li> <li><strong>Example:</strong> The pii graph shown in Figure 10(a) is one such plan extracted from the explored e-graph of Figure 9(b). It’s a concrete sequence of operations for the <code class="language-plaintext highlighter-rouge">HQKV</code> accelerator.</li> </ul> </li> </ul> <h4 id="phase-2-memory-allocation-finding-the-where-or-solving-β">Phase 2: Memory Allocation (Finding the “Where”; Or solving β)</h4> <p>Now that we have a plan (the pii graph), we need to figure out where to place all the intermediate tensors in the scratchpads (<code class="language-plaintext highlighter-rouge">d1</code>, <code class="language-plaintext highlighter-rouge">d2</code>).</p> <ul> <li><strong>Step 4: Topological Ordering Generator (§6.1)</strong> <ul> <li><strong>Action:</strong> The instructions in the pii graph must be executed in an order that respects data dependencies (you can’t use a result before it’s computed). There can be multiple valid orders. ACT chooses an order that tries to minimize the amount of memory needed at any given time, which makes the next step more likely to succeed.</li> <li><strong>Example:</strong> The circled numbers (① through ⑨) in Figure 10(a) show the chosen execution order.</li> </ul> </li> <li><strong>Step 5: Constraint Satisfaction Problem Generator &amp; Solver (§6.2)</strong> <ul> <li><strong>Action:</strong> This is like solving a complex logic puzzle. ACT generates a set of rules (constraints) that a valid memory allocation must obey: <ol> <li><strong>Liveness:</strong> If two tensors need to exist in memory at the same time, they cannot be allocated to overlapping memory regions.</li> <li><strong>ISA Constraints:</strong> Each instruction has rules about where its inputs can come from and where its output can go. For example, the <code class="language-plaintext highlighter-rouge">gemm</code> instruction might require its inputs to be in scratchpad <code class="language-plaintext highlighter-rouge">d1</code> and its output to be in buffer <code class="language-plaintext highlighter-rouge">d2</code>.</li> <li><strong>Dataflow:</strong> The output address of one instruction must match the input address for the next instruction that uses its data.</li> </ol> </li> <li>ACT feeds all these rules to a <strong>CP-SAT solver</strong>, a generic tool for solving these kinds of puzzles. The solver’s job is to find values for all the unknown address attributes (the <code class="language-plaintext highlighter-rouge">?</code>s).</li> <li><strong>Example:</strong> The solver figures out the exact data slices for each operation, shown by the purple annotations in Figure 10(a). For instance, it determines the first <code class="language-plaintext highlighter-rouge">load_rm</code> should write to <code class="language-plaintext highlighter-rouge">d1[0:64]</code>.</li> </ul> </li> <li><strong>Step 6: Code Emitter (§6.3)</strong> <ul> <li><strong>Action:</strong> The solver returns a complete, valid solution. The final step is to simply print out the instructions with their now-known attributes into an assembly file.</li> <li><strong>Example:</strong> The final, complete assembly code is shown in Figure 10(b). The plan from Phase 1 has been successfully turned into a concrete, executable program.</li> </ul> </li> </ul> <hr/> <h3 id="an-intuitive-guide-to-the-proofs">An Intuitive Guide to the Proofs</h3> <p>This section proves that the whole process is sound and complete.</p> <h4 id="proving-soundness-our-output-is-always-correct">Proving Soundness (“Our output is always correct”)</h4> <p>The proof relies on chaining together two key ideas (Lemma 1 and Lemma 3):</p> <ol> <li><strong>Phase 1 is Sound:</strong> The rewrite rules used in the e-graph only add truly equivalent computations. So, the pii graph extracted at the end of Phase 1 is guaranteed to be semantically equivalent to the original program.</li> <li><strong>Phase 2 is Sound:</strong> The memory allocation phase only fills in addresses; it doesn’t change the computation itself. The way it constructs the final assembly from the pii graph is proven to preserve the program’s meaning.</li> </ol> <p>Since both phases are sound, the entire compilation from the original graph to the final assembly is sound. <strong>What goes in is what comes out, just compiled.</strong></p> <h4 id="proving-completeness-if-a-solution-exists-we-will-find-it">Proving Completeness (“If a solution exists, we will find it”)</h4> <p>This is more complex but follows a beautiful logical argument (combining Lemmas 2, 4, and 5):</p> <ol> <li><strong>First, prove that any possible valid assembly program can be mapped back to a pii graph</strong> (Lemma 5). This means our intermediate “plan” format is expressive enough to capture any solution.</li> <li><strong>Next, prove that our exploration in Phase 1 is guaranteed to find <em>every</em> possible pii graph</strong>, including the one that corresponds to the solution (Lemma 2). It uses a bounded enumeration strategy that ensures it will eventually find any valid plan, no matter how complex.</li> <li><strong>Finally, prove that if a pii graph has a valid memory allocation, Phase 2 is guaranteed to find it</strong> (Lemma 4). This is because it considers all possible instruction orderings and the CP-SAT solver is complete for the constraints given.</li> </ol> <p><strong>The final argument:</strong> If a valid assembly program exists, then by (1) a corresponding pii graph must exist. By (2), our Phase 1 will eventually find this pii graph. And by (3), our Phase 2 will successfully find a memory allocation for it. Therefore, the compiler will successfully produce a valid output. <strong>ACT will not fail if a solution is possible.</strong></p> <blockquote> <p>But how long will the “completeness” requires? Seems no guarantees.</p> </blockquote> <h2 id="thinking">Thinking</h2> <ul> <li>The story is cool. We always want an automatic compiler generation.</li> <li>The problem is “what is the limit of the automatic generation” and “how good will the automatic one be”? <ul> <li>I think ACT is clever to focus on problem not that complex. Just Allo-QKV / Gemini / Intel AMX. The experiments look already rich. Within the range, ACT is a good one.</li> <li>Can ACT handle more complex tasks? I think tiling, data layout, fusion, communication, and etc. are also non-negligible. Tiling appears but does not get explained in the paper. I need to run the code for confirmation.</li> </ul> </li> </ul>]]></content><author><name></name></author><summary type="html"><![CDATA[Paper reading on ACT - a compiler-compiler that auto-generates compiler backends from tensor accelerator ISA descriptions, solving the eternal pain of custom accelerator adoption]]></summary></entry><entry><title type="html">LLM in very daily life? Enjoy Claude Code and Gemini-cli’s super power!</title><link href="https://uv-xiao.github.io/blog/2025/LLM-in-life/" rel="alternate" type="text/html" title="LLM in very daily life? Enjoy Claude Code and Gemini-cli’s super power!"/><published>2025-07-05T00:00:00+00:00</published><updated>2025-07-05T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/LLM-in-life</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/LLM-in-life/"><![CDATA[<blockquote class="block-tip"> <p>不到两个月过去，很多国内国外的LLM Agentic CLI陆续出现了(QWen Code,Trae Agent, etc.). 我就不做比较和更新了，毕竟简单的日常任务也不是很能体现模型能力。ZZW过去两个月每天都在用这个激励系统，可视化了一下得分：</p> </blockquote> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/in_posts/points_trend_20250824-480.webp 480w,/assets/img/in_posts/points_trend_20250824-800.webp 800w,/assets/img/in_posts/points_trend_20250824-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/in_posts/points_trend_20250824.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>这篇文章记录了我用LLM agents开发一个极简应用（甚至谈不上应用，只是一个简单的程序），<del>用来激励女票的考公复习</del>。因为考公这个事也非常中国特色，代码也都是中文，所以这个blog也就不用<del>翻译成</del>英文了。</p> <p>当前效果：<a href="https://github.com/uv-xiao/cse-incentive-agent">github.com/uv-xiao/cse-incentive-agent</a></p> <h2 id="llm-agents">LLM Agents</h2> <p>使用LLM agents (<a href="https://www.anthropic.com/claude-code">Claude Code</a> 和 <a href="https://github.com/google-gemini/gemini-cli">Gemini CLI</a>)做科研（写代码、写论文）是非常方便且生产力爆炸的实践。我会写另一篇blog来描述我最近用他们做科研代码开发的经历。</p> <h2 id="背景考公激励">背景：考公激励</h2> <p>前几天女票发我了一个小红书笔记：<a href="https://www.xiaohongshu.com/discovery/item/6862b8f50000000023004419?source=webshare&amp;xhsshare=pc_web&amp;xsec_token=CBVBdciZz6KZBOI7BAFkOBHM0JbYN2EbkL2IXJKMPE0uM=&amp;xsec_source=pc_share">笔记链接</a></p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/in_posts/2025-07-05-cse_incentive-480.webp 480w,/assets/img/in_posts/2025-07-05-cse_incentive-800.webp 800w,/assets/img/in_posts/2025-07-05-cse_incentive-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/in_posts/2025-07-05-cse_incentive.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>我寻思这要是我自己天天帮她记录也怪麻烦的，而且也不能提供足够的情绪价值。于是我决定用LLM agents来实现这个功能，并且要求它们提供充分的情绪价值嘿嘿</p> <h2 id="实现">实现</h2> <h3 id="安装claude-code和gemini-cli">安装Claude Code和Gemini CLI</h3> <p>首先就是要setup一些LLM agents。去各自的网站下载安装，发挥梯子的本事和蹭别人会员（Claude Code Max要200刀一个月wtf）的脸皮。我是二者混合使用的（Claude Code写代码和维护项目，生成的代码调用Gemini CLI实现文字类功能），但只用Gemini CLI好像也没问题，毕竟就是写点小Python，也不太需要调工具调试。</p> <h3 id="初始化项目">初始化项目</h3> <p>最开始我创建的文件结构：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cse_incentive/
+-- .claude/
|   +-- settings.local.json
+-- resources/
|   +-- red_notebook_image.jpg
+-- README.md
</code></pre></div></div> <p>也就是把那个小红书图片放进resources文件夹，在<code class="language-plaintext highlighter-rouge">.claude</code>文件夹里粘贴一个<code class="language-plaintext highlighter-rouge">settings.local.json</code>（设置<code class="language-plaintext highlighter-rouge">"permissions"</code>的<code class="language-plaintext highlighter-rouge">"defaultMode"</code>为<code class="language-plaintext highlighter-rouge">"bypassPermissions"</code>），然后写一个<code class="language-plaintext highlighter-rouge">README.md</code>。</p> <p><code class="language-plaintext highlighter-rouge">README.md</code>的内容：</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># ZZW考公Agent</span>

ZZW考公需要好好学习，因此需要激励和监督。

本项目作为ZZW考公的Agent，使用Claude Code驱动，实现考公学习期间的情感支持和进度跟进。

<span class="gu">## 资源</span>

<span class="sb">`resources/red_black_list.jpg`</span>是一张图片，是网上找的，内容是对不同考公学习期间的行为的加分/扣分，以及基于积分的鼓励。

可以在网上检索更多相关的考公相关的事件、安排、激励、惩罚等，添加到<span class="sb">`resources`</span>目录，作为参考。

<span class="gu">## 功能</span>

<span class="gs">**每日问卷**</span>：生成每日问卷，交给ZZW填写，根据问卷内容生成每日总结。
<span class="p">-</span> 问卷形式应该减少ZZW填写的难度，以选择填空为主。
<span class="p">-</span> 问卷内容是对当天考公内容学习时长、做题情况、情绪状态、身体状态、学习计划完成情况等。
<span class="p">-</span> 问卷应该提供充足的情绪价值，以鼓励ZZW继续学习。

<span class="gs">**每日总结**</span>：根据每日问卷内容，生成每日总结报告。
<span class="p">-</span> 总结内容应剖析ZZW的学习、做题、情绪、身体状态，并给出改进建议。
<span class="p">-</span> 总结内容应提供充足的情绪价值，以鼓励ZZW继续学习。
<span class="p">-</span> 报告应为pdf格式，包括emoji和图片等元素，呈现丰富多彩的视觉效果。

<span class="gs">**积分奖励/惩罚**</span>： 根据每日问卷内容，生成积分奖励/惩罚。
<span class="p">-</span> 积分奖励/惩罚应根据问卷内容，给出相应的奖励/惩罚。
<span class="p">-</span> 需要在"每日总结"中描述积分奖励/惩罚的原因。
<span class="p">-</span> 积分表需要有积分变化曲线，以直观展示ZZW的积分变化。
<span class="p">-</span> 积分表存储为本地文件，并提供程序编辑和可视化。

<span class="gu">## 技术栈</span>
<span class="p">
-</span> Claude Code
<span class="p">  -</span> 实现所需的文件结构和代码
<span class="p">  -</span> 每日根据"question", "answer:<span class="nt">&lt;answer</span> <span class="na">from</span> <span class="na">zzw</span><span class="nt">&gt;</span>", 生成报告，并调用程序更新积分表。
<span class="p">  -</span> 报告生成过程也可以查看积分表。
<span class="p">
-</span> Gemini-cli
<span class="p">  -</span> 由于gemini-cli有更好的语言理解能力和图片生成能力，因此生成报告时，调用gemini-cli实现问卷解析、报告生成、图片生成。
<span class="p">
-</span> 代码
<span class="p">  -</span> 使用python实现积分表的存储、更新、可视化。
</code></pre></div></div> <h3 id="让llm开干">让LLM开干</h3> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">/init</code></p> </blockquote> <p>初始化Claude Code，生成<code class="language-plaintext highlighter-rouge">CLAUDE.md</code>，里面包含Claude Code自己分析设计的项目结构和功能:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># CLAUDE.md</span>

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

<span class="gu">## Project Overview</span>

This is a Civil Service Exam Study Agent (考公Agent) designed to support ZZW in their civil service exam preparation journey. The system provides daily questionnaires, generates encouraging summary reports, and maintains a points-based reward/punishment system to gamify the study process.

<span class="gu">## Development Commands</span>

Since this is a Python project in early development, common commands will include:

python -m venv venv
source venv/bin/activate  # On Linux/Mac

pip install -r requirements.txt

python main.py

pytest tests/<span class="sb">


</span><span class="gu">## Architecture &amp; Core Components</span>

The system consists of three main functional modules:

<span class="gu">### 1. Daily Questionnaire Module</span>
...

<span class="gu">### 2. Summary Report Generator</span>
...

<span class="gu">### 3. Points Management System</span>
...

<span class="gu">## Important Implementation Notes</span>
<span class="p">
1.</span> <span class="gs">**Scoring Rules**</span>: The red_black_list.jpg contains detailed scoring criteria that must be implemented exactly as shown, including...
<span class="p">
2.</span> <span class="gs">**Data Persistence**</span>: Implement a simple file-based storage system (JSON or SQLite) to maintain...
<span class="p">
3.</span> <span class="gs">**Report Generation**</span>: Use gemini-cli for creating visually appealing PDF reports with...
<span class="p">
4.</span> <span class="gs">**User Experience**</span>: Keep the questionnaire simple and quick to complete to ensure daily compliance.

<span class="gu">## External Tool Integration</span>
<span class="p">
-</span> <span class="gs">**gemini-cli**</span>: Used for generating summary reports and creating visual content
<span class="p">-</span> <span class="gs">**Claude Code**</span>: Primary development environment for implementation

<span class="gu">## File Structure (Planned)</span>

diary/
+-- main.py                 # Entry point
+-- modules/
|   +-- questionnaire.py    # Daily questionnaire generation
|   +-- scoring.py          # Points calculation logic
|   +-- report_generator.py # PDF report creation
|   +-- data_manager.py     # Data persistence
+-- data/                   # Store user responses and points
+-- reports/                # Generated PDF reports
+-- resources/              # Static resources including red_black_list.jpg
</code></pre></div></div> <p>看起来已经不错了，初具雏形！但我比较喜欢用<a href="https://pixi.sh/latest/">pixi</a>来管理项目，于是我让Claude Code使用它:</p> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">update CLAUDE.md with pixi-based python management.</code></p> </blockquote> <p>接下来就让Claude Code完成代码实现。</p> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">read @README.md carefully, know what i want, and search things online (use builtin tools and gemini) to collect more resources. Then implement the required codebase. Also update CLAUDE.md to make the claude code to serve well / be qualified for the described job. Also generate a USAGE.md to instruct the subsequent colaboration between I and claude code. </code></p> </blockquote> <p>这一个指令就能让Claude Code完成项目初始化、代码实现、文档撰写等所有工作。</p> <p>可以查看下生成的<code class="language-plaintext highlighter-rouge">USAGE.md</code>中关于使用方法的说明:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gu">### 每日学习流程</span>
<span class="p">1.</span> <span class="gs">**早晨规划**</span>
<span class="p">   -</span> 打开系统，查看昨日报告和积分
<span class="p">   -</span> 制定今日学习计划
<span class="p">2.</span> <span class="gs">**晚上总结**</span>
<span class="p">   -</span> 运行程序：<span class="sb">`pixi run python main.py`</span>
<span class="p">   -</span> 选择 "1. 填写今日问卷"
<span class="p">   -</span> 认真回答14个问题（约2-3分钟）
<span class="p">   -</span> 查看生成的报告和获得的积分
<span class="p">3.</span> <span class="gs">**查看进度**</span>
<span class="p">   -</span> 选择 "3. 查看积分历史" 查看最近记录
<span class="p">   -</span> 选择 "6. 可视化进度" 生成进度图表
</code></pre></div></div> <p>这里有个问题，因为我这个工具是需要使用Claude Code做更新并使用Gemini CLI生成报告的，这俩工具都能跑的环境其实是我在学校的服务器，而不是ZZW的电脑！（而且也不打算教会ZZW使用命令行和Python这些东西）。总而言之，我才是这个工具的直接用户，总不能每天把生成的问卷一个题一个题微信给ZZW吧！所以我觉得应该让这个工具支持Excel格式的问卷导出和导入。</p> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">由于ZZW不会使用python，每日运行main.py时，应该生成需要回答的问题的文本格式（比如excel），我会发送给zzw进行回答；然后python程序应该能识别回答后的文件，提取其中的答案，并完成报告生成和积分更新。此外，不但需要等级系统，还需要积分兑换系统（根据resources/red_black_list.jpg）和网上资源，查找可以兑换的奖励；claude也需要支持更新奖励列表。完成上述更新后，更新USAGE.md和README.md以保持最新。</code></p> </blockquote> <p>好了！我们已经得到了这个工具的初步版本（也是github repo的第一个commit），可以让ZZW试用了哈哈</p> <h3 id="改进">改进</h3> <p>执行<code class="language-plaintext highlighter-rouge">pixi run python main.py</code>，得到：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>欢迎使用 ZZW考公学习日记系统！

这是你的专属学习伙伴，我会陪伴你走过考公之路。
让我们一起努力，向着目标前进！💪

🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟
<span class="o">============================================================</span>
🎯 ZZW考公学习日记系统
<span class="o">============================================================</span>


📋 主菜单:
💰 当前积分: 0分
<span class="nt">----------------------------------------</span>
1. 📤 导出今日问卷 <span class="o">(</span>Excel<span class="o">)</span>
2. 📥 导入问卷答案 <span class="o">(</span>Excel<span class="o">)</span>
3. 🎁 查看积分商城
4. 🛒 兑换奖励
5. 📄 查看今日报告
6. 📊 查看积分历史
7. 📈 查看学习统计
8. 📅 生成周总结
9. 📉 可视化进度
10. 💾 导出数据
0. 退出系统
</code></pre></div></div> <p>啧啧这情绪价值拉满！</p> <p>我把生成的问卷发给了ZZW，有一些修改的意见，我直接发给Claude Code，让他调整：</p> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">删除小额奖励和中等奖励</code></p> </blockquote> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">我希望删除的不是积分奖励，而是积分兑换系统里的小额奖励和中等奖励</code></p> </blockquote> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">ZZW提出了如下反馈：学习时间和练习题的选项数值都太低了，她每天都是远超问卷选项中的数量，需要大幅提高；除了做题之外，ZZW还同时需要写她的毕业论文，需要添加对应的积分奖励（比如与每日创作多少字数关联）；ZZW还会背诵考公需要的知识，或者看网课学习，请上网查找相关信息，并添加积分奖励。请根据上述反馈更新工具。</code></p> </blockquote> <blockquote class="block-tip"> <p><code class="language-plaintext highlighter-rouge">目前积分奖励和惩罚的积分数值差别很大，请适当提高积分惩罚以示警戒</code></p> </blockquote> <p>这些就不太重要了！</p> <h2 id="结论">结论</h2> <p>也没啥结论可下，总之就是在把LLM当问答助手之外，可以尝试用它来实现一些日常生活中的小工具，比如这个考公激励工具。但这也确实需要写门槛，至少得能访问现在这几个比较猛的LLM Agents（希望国内的公司加加油，来点deepseek code, qwen-cli, <del>doubao</del>之类的），还要有一点代码开发基础（至少能调这些cli或者运行生成的程序？好吧，可能也不需要，如果LLM直接搞出来个什么网页、小程序之类的）。</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Share an expierence of using LLM agents for life (rather than for work)]]></summary></entry><entry><title type="html">What if You Never Had to Choose? A Journey into E-graphs and Equality Saturation</title><link href="https://uv-xiao.github.io/blog/2025/egg-basics/" rel="alternate" type="text/html" title="What if You Never Had to Choose? A Journey into E-graphs and Equality Saturation"/><published>2025-07-02T00:00:00+00:00</published><updated>2025-07-02T00:00:00+00:00</updated><id>https://uv-xiao.github.io/blog/2025/egg-basics</id><content type="html" xml:base="https://uv-xiao.github.io/blog/2025/egg-basics/"><![CDATA[<iframe src="https://uv-xiao.github.io/assets/slide/presentations/egg-basic/egg" width="100%" height="500px" frameborder="0" allowfullscreen=""></iframe> <p>I still remember my first serious attempt at writing a compiler optimizer. I had this clever rule: <code class="language-plaintext highlighter-rouge">(a * 2) / 2 → a</code>. Simple, right? But then I realized that by eagerly applying it, I was destroying the pattern <code class="language-plaintext highlighter-rouge">(a * 2)</code> that another rule needed for vectorization. This is the optimizer’s eternal dilemma: every choice you make closes doors to other opportunities.</p> <p>For decades, we’ve played this high-stakes game of optimization whack-a-mole, carefully ordering our transformations and hoping we don’t paint ourselves into a corner. But what if I told you there’s a way to have your cake and eat it too? What if you could apply ALL your optimizations at once and then pick the best result?</p> <p>Welcome to the world of <strong>e-graphs</strong> and <strong>equality saturation</strong> - where we don’t choose between optimizations, we choose from <em>all possible</em> optimizations. It’s like having a time machine for your compiler passes, except it actually works.</p> <h2 id="the-problem-why-traditional-optimization-is-like-playing-jenga">The Problem: Why Traditional Optimization is Like Playing Jenga</h2> <p>Before we dive into the solution, let’s understand why optimization is so darn hard. Traditional compilers use <strong>term rewriting</strong> - they see a pattern in your code and replace it with something better. Sounds great, right? Well…</p> <h2 id="term-rewriting-the-old-school-approach">Term Rewriting: The Old School Approach</h2> <h3 id="how-it-works-the-cookbook-analogy">How It Works (The Cookbook Analogy)</h3> <p>Think of <strong>term rewriting</strong> [Baader &amp; Nipkow, “Term rewriting and all that,” 1998] like following a very literal recipe. You have your ingredients (the program), and a set of substitution rules (the recipe steps). When you see “2 cups of sugar”, you can replace it with “1 cup of honey”.</p> <p>In programming terms:</p> <ul> <li><strong>Terms</strong>: Your code expressions, like <code class="language-plaintext highlighter-rouge">(a + 0) + (b + 0)</code></li> <li><strong>Rules</strong>: Transformations like “x + 0 → x” (because adding zero is pointless)</li> <li><strong>Context</strong>: The surrounding code that doesn’t change</li> </ul> <p><strong>A Simple Example:</strong> Let’s optimize <code class="language-plaintext highlighter-rouge">(a + 0) + (b + 0)</code> with the rule <code class="language-plaintext highlighter-rouge">x + 0 → x</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Original: (a + 0) + (b + 0)
Apply rule to first part: a + (b + 0)
Apply rule to second part: a + b
</code></pre></div></div> <p>Seems great! But here’s the catch…</p> <h3 id="the-problems-why-this-gets-messy-fast">The Problems: Why This Gets Messy Fast</h3> <p><strong>The Termination Trap:</strong> Sometimes rules can ping-pong forever:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x + y → y + x  (commutivity)
y + x → x + y  (oops, infinite loop!)
</code></pre></div></div> <p><strong>The Confluence Conundrum:</strong> Different rule orders give different results:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(a * 2) / 2
  ↓ (optimize multiply)     ↓ (optimize divide)
(a &lt;&lt; 1) / 2                a * (2/2)
  ↓                         ↓
???                         a
</code></pre></div></div> <p>One path leads to <code class="language-plaintext highlighter-rouge">a</code>, the other to… who knows? This is why compiler writers lose sleep.</p> <h3 id="the-band-aid-rewrite-strategies">The Band-Aid: Rewrite Strategies</h3> <p>Compiler writers tried to fix this with “strategies” - basically recipes for <em>how</em> to apply rules. It’s like saying “always start cooking from the outside in” or “season as you go”.</p> <div class="language-scala highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// "Apply this optimization to the outermost loops first"</span>
<span class="k">def</span> <span class="nf">outermost</span> <span class="k">=</span> <span class="n">applyToOuterLoopsFirst</span>

<span class="c1">// "Then tile the matrix multiply"</span>
<span class="nf">tile</span><span class="o">(</span><span class="mi">32</span><span class="o">,</span><span class="mi">32</span><span class="o">)</span> <span class="n">after</span> <span class="n">outermost</span>
</code></pre></div></div> <p>But this is still just organized guessing. What if the optimal strategy for one program is terrible for another? Enter e-graphs, stage left.</p> <h2 id="e-graphs-the-keep-everything-revolution">E-graphs: The “Keep Everything” Revolution</h2> <h3 id="the-big-idea-detectives-corkboard-analogy">The Big Idea (Detective’s Corkboard Analogy)</h3> <p>Imagine you’re a detective trying to solve a case. Traditional optimization is like having to choose one lead and throw away all the others. E-graphs [Nelson &amp; Oppen, “Fast decision procedures based on congruence closure,” 1980]? That’s like having a corkboard where you can pin up EVERY clue and connect them with red string.</p> <p>An <strong>e-graph</strong> (equality graph) is a data structure that stores multiple equivalent expressions simultaneously. When you discover that <code class="language-plaintext highlighter-rouge">x * 2</code> equals <code class="language-plaintext highlighter-rouge">x &lt;&lt; 1</code>, you don’t replace one with the other - you just connect them with a piece of string saying “these are the same!”</p> <p>The magic is that e-graphs store all this information incredibly efficiently by sharing common sub-expressions. It’s like realizing that five different theories all use the same piece of evidence, so you only need to pin it up once.</p> <h3 id="how-e-graphs-actually-work">How E-graphs Actually Work</h3> <p>An e-graph has two main concepts:</p> <p><strong>E-nodes</strong>: These are your actual expressions, like <code class="language-plaintext highlighter-rouge">a + b</code> or <code class="language-plaintext highlighter-rouge">foo(x, y)</code>. Think of them as the individual clues on your corkboard.</p> <p><strong>E-classes</strong>: These are groups of equivalent e-nodes. If <code class="language-plaintext highlighter-rouge">x * 2</code>, <code class="language-plaintext highlighter-rouge">x &lt;&lt; 1</code>, and <code class="language-plaintext highlighter-rouge">x + x</code> are all equivalent, they live in the same e-class. It’s like having a circle drawn around related clues.</p> <p>The clever bit? E-graphs use a <strong>union-find</strong> data structure (think: extremely efficient way to group things) to keep track of which expressions are equivalent. When you discover a new equivalence, you just union two e-classes together. Boom! Instant optimization propagation.</p> <h3 id="the-e-graph-api-just-four-operations">The E-graph API: Just Four Operations</h3> <p>E-graphs are beautifully simple. You only need four operations:</p> <ol> <li><strong>add(expression)</strong>: “Hey e-graph, remember this expression”</li> <li><strong>merge(id1, id2)</strong>: “BTW, these two things are equal”</li> <li><strong>find(id)</strong>: “What group is this expression in?”</li> <li><strong>ematch(pattern)</strong>: “Find me all expressions that look like this”</li> </ol> <p>That’s it! With these four operations, you can build incredibly sophisticated optimizers. It’s like LEGO blocks for compiler writers.</p> <h3 id="e-matching-finding-patterns-in-the-chaos">E-matching: Finding Patterns in the Chaos</h3> <p>E-matching is like playing Where’s Waldo, but Waldo could be wearing different outfits (equivalent expressions). You give it a pattern like “find all <code class="language-plaintext highlighter-rouge">x + 0</code> expressions” and it searches through the entire e-graph.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">find_optimization_opportunities</span><span class="p">(</span><span class="n">pattern</span><span class="p">,</span> <span class="n">egraph</span><span class="p">):</span>
    <span class="c1"># "Show me everything that looks like x + 0"
</span>    <span class="k">for</span> <span class="n">group</span> <span class="ow">in</span> <span class="n">egraph</span><span class="p">.</span><span class="nf">all_groups</span><span class="p">():</span>
        <span class="k">for</span> <span class="n">expression</span> <span class="ow">in</span> <span class="n">group</span><span class="p">.</span><span class="n">expressions</span><span class="p">:</span>
            <span class="k">if</span> <span class="nf">looks_like</span><span class="p">(</span><span class="n">pattern</span><span class="p">,</span> <span class="n">expression</span><span class="p">):</span>
                <span class="k">yield</span> <span class="sh">"</span><span class="s">Found one!</span><span class="sh">"</span><span class="p">,</span> <span class="n">expression</span>
</code></pre></div></div> <p>The beautiful part? Because the e-graph maintains equivalences, when you find <code class="language-plaintext highlighter-rouge">(a * 2) + 0</code>, you automatically know about <code class="language-plaintext highlighter-rouge">(a &lt;&lt; 1) + 0</code> too!</p> <h3 id="the-magic-non-destructive-rewriting">The Magic: Non-Destructive Rewriting</h3> <p>Here’s where e-graphs blow your mind. In traditional rewriting:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(a * 2) / 2 → a    // Bye bye, original expression!
</code></pre></div></div> <p>In e-graphs:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(a * 2) / 2 ≅ a    // Both exist! They're friends now!
</code></pre></div></div> <p>The process:</p> <ol> <li>Find all instances of your pattern (like <code class="language-plaintext highlighter-rouge">(x * 2) / 2</code>)</li> <li>Calculate what they should become (<code class="language-plaintext highlighter-rouge">x</code>)</li> <li>Don’t replace! Just merge the e-classes</li> </ol> <p>It’s like saying “these two expressions are pen pals” instead of “this expression is dead to me.” Much more civilized.</p> <h3 id="visual-example-the-old-way-vs-the-e-graph-way">Visual Example: The Old Way vs. The E-graph Way</h3> <pre><code class="language-mermaid">graph TB
    subgraph "Traditional Rewriting"
        A1["(a * 2) / 2"] --&gt;|destroy| A2["(a &lt;&lt; 1) / 2"]
        A2 --&gt;|destroy| A3["a"]
        style A1 fill:#ffcccc,stroke:#ff0000,stroke-width:2px,text-decoration:line-through
        style A2 fill:#ffcccc,stroke:#ff0000,stroke-width:2px,text-decoration:line-through
    end
    
    subgraph "E-graph Approach"
        B1["E-class 1"]
        B1 --&gt; B2["(a * 2) / 2"]
        B1 --&gt; B3["(a &lt;&lt; 1) / 2"]
        B1 --&gt; B4["a"]
        B1 --&gt; B5["..."]
        style B1 fill:#ccffcc,stroke:#00ff00,stroke-width:2px
    end
</code></pre> <h2 id="equality-saturation-going-full-detective-mode">Equality Saturation: Going Full Detective Mode</h2> <h3 id="the-phase-ordering-problem-why-order-matters">The Phase Ordering Problem (Why Order Matters)</h3> <p>Remember that game where you have to get the fox, chicken, and grain across the river? Traditional optimization is like that, but with 1000 items and no clear rules. This is the dreaded <strong>phase ordering problem</strong> [Tate et al., “Equality saturation: A new approach to optimization,” POPL 2009].</p> <p>Example with <code class="language-plaintext highlighter-rouge">(a * 2) / 2</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Path 1: Optimize multiplies first
(a * 2) / 2 → (a &lt;&lt; 1) / 2 → ... → ???

Path 2: Apply algebra first  
(a * 2) / 2 → a * (2/2) → a * 1 → a ✓
</code></pre></div></div> <p>One path gets you the optimal result, the other gets you stuck. But how do you know which path to take? You don’t. Until now.</p> <h3 id="equality-saturation-the-try-everything-algorithm">Equality Saturation: The “Try Everything” Algorithm</h3> <p>Here’s the brilliant insight: What if we just… didn’t choose? What if we applied ALL the optimizations at once and sorted it out later?</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">equality_saturation</span><span class="p">(</span><span class="n">expr</span><span class="p">,</span> <span class="n">rules</span><span class="p">):</span>
    <span class="n">egraph</span> <span class="o">=</span> <span class="nf">start_with</span><span class="p">(</span><span class="n">expr</span><span class="p">)</span>
    
    <span class="k">while</span> <span class="sh">"</span><span class="s">we</span><span class="sh">'</span><span class="s">re still finding new stuff</span><span class="sh">"</span><span class="p">:</span>
        <span class="k">for</span> <span class="n">rule</span> <span class="ow">in</span> <span class="n">rules</span><span class="p">:</span>
            <span class="c1"># Find everywhere this rule could apply
</span>            <span class="n">matches</span> <span class="o">=</span> <span class="n">egraph</span><span class="p">.</span><span class="nf">find_pattern</span><span class="p">(</span><span class="n">rule</span><span class="p">.</span><span class="n">pattern</span><span class="p">)</span>
            
            <span class="k">for</span> <span class="n">match</span> <span class="ow">in</span> <span class="n">matches</span><span class="p">:</span>
                <span class="c1"># Apply the rule (non-destructively!)
</span>                <span class="n">new_expr</span> <span class="o">=</span> <span class="n">rule</span><span class="p">.</span><span class="nf">apply_to</span><span class="p">(</span><span class="n">match</span><span class="p">)</span>
                <span class="n">egraph</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">new_expr</span><span class="p">)</span>
                <span class="n">egraph</span><span class="p">.</span><span class="nf">merge</span><span class="p">(</span><span class="n">match</span><span class="p">,</span> <span class="n">new_expr</span><span class="p">)</span>
        
        <span class="k">if</span> <span class="sh">"</span><span class="s">nothing new was discovered</span><span class="sh">"</span><span class="p">:</span>
            <span class="k">break</span>  <span class="c1"># We're saturated!
</span>    
    <span class="k">return</span> <span class="n">egraph</span><span class="p">.</span><span class="nf">pick_best_expression</span><span class="p">()</span>
</code></pre></div></div> <p><strong>The Algorithm in Plain English:</strong></p> <ol> <li>Start with your expression</li> <li>Apply every possible rule everywhere you can</li> <li>Keep all the results (don’t throw anything away!)</li> <li>Repeat until you can’t find any new equivalences</li> <li>Pick the best final result</li> </ol> <p>It’s like letting a hyperactive monkey loose in an optimization candy store, then picking the best candy it found. Chaotic? Yes. Effective? Absolutely.</p> <h3 id="extraction-picking-the-best-from-the-buffet">Extraction: Picking the Best from the Buffet</h3> <p>Okay, so we’ve applied every optimization known to mankind and our e-graph is stuffed with equivalent expressions. Now what? We need to pick the “best” one. This is called <strong>extraction</strong>.</p> <p><strong>The Catch:</strong> Remember when I said this was all magical? Well, here’s the punchline - picking the optimal expression from an e-graph is NP-hard. It’s like being told the treasure is buried “somewhere on Earth.” Thanks, very helpful.</p> <p><strong>The Problem:</strong></p> <ul> <li>You need to pick one expression from each e-class</li> <li>But the choices are interdependent (picking <code class="language-plaintext highlighter-rouge">a + b</code> in one place might make <code class="language-plaintext highlighter-rouge">b + a</code> better elsewhere)</li> <li>Finding the global optimum is computationally brutal</li> </ul> <p>But don’t worry! We have tricks up our sleeve…</p> <h4 id="approach-1-the-mathematicians-solution-ilp">Approach 1: The Mathematician’s Solution (ILP)</h4> <p>You can formulate extraction as an Integer Linear Programming problem. Basically, you’re asking a very expensive solver to find the optimal solution.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Minimize: total cost of selected expressions
Subject to:
  - Pick at least one thing from each e-class
  - If you pick an expression, pick all its children
  - Don't create cycles (no circular reasoning!)
</code></pre></div></div> <p><strong>Pro:</strong> Guaranteed optimal solution! <strong>Con:</strong> Might take until the heat death of the universe for large programs.</p> <h4 id="approach-2-the-graph-theorists-dream-dynamic-programming">Approach 2: The Graph Theorist’s Dream (Dynamic Programming)</h4> <p>If your e-graph has nice structure (technically: low treewidth), you can use dynamic programming. It’s like solving a puzzle by breaking it into smaller, manageable pieces.</p> <p><strong>The Idea:</strong></p> <ul> <li>Decompose the e-graph into a tree of “bags”</li> <li>Solve each bag independently</li> <li>Combine solutions cleverly</li> </ul> <p><strong>When It Works:</strong> Great for e-graphs that look like trees with a few extra edges <strong>When It Doesn’t:</strong> Not so great for e-graphs that look like hairballs</p> <h4 id="approach-3-the-engineers-good-enough-heuristics">Approach 3: The Engineer’s “Good Enough” (Heuristics)</h4> <p>Most real systems say “perfect is the enemy of good” and use heuristics:</p> <p><strong>Bottom-up Greedy:</strong> Start from the leaves, pick the cheapest option at each level</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">extract_greedy</span><span class="p">(</span><span class="n">eclass</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">eclass</span><span class="p">.</span><span class="nf">is_leaf</span><span class="p">():</span>
        <span class="k">return</span> <span class="nf">cheapest_option</span><span class="p">(</span><span class="n">eclass</span><span class="p">)</span>
    
    <span class="c1"># Try each option with greedy children
</span>    <span class="n">best</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="k">for</span> <span class="n">expr</span> <span class="ow">in</span> <span class="n">eclass</span><span class="p">:</span>
        <span class="n">cost</span> <span class="o">=</span> <span class="n">expr</span><span class="p">.</span><span class="n">cost</span> <span class="o">+</span> <span class="nf">sum</span><span class="p">(</span><span class="nf">extract_greedy</span><span class="p">(</span><span class="n">child</span><span class="p">)</span> <span class="k">for</span> <span class="n">child</span> <span class="ow">in</span> <span class="n">expr</span><span class="p">.</span><span class="n">children</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">cost</span> <span class="o">&lt;</span> <span class="n">best</span><span class="p">.</span><span class="n">cost</span><span class="p">:</span>
            <span class="n">best</span> <span class="o">=</span> <span class="n">expr</span>
    <span class="k">return</span> <span class="n">best</span>
</code></pre></div></div> <p><strong>Pool Extraction:</strong> The “throw spaghetti at the wall” approach:</p> <ol> <li>Generate 100 different extractions using various strategies</li> <li>Add some random ones for spice</li> <li>Evaluate them all</li> <li>Pick the winner</li> </ol> <p>Surprisingly effective! Like how a room full of monkeys with typewriters occasionally produces Shakespeare.</p> <h2 id="enter-egg-the-game-changer-">Enter egg: The Game Changer 🥚</h2> <p>For years, e-graphs were like fusion power - always 10 years away from being practical. They were slow, hard to implement, and each one was a custom job. Then came <strong>egg</strong> <a href="https://arxiv.org/abs/2004.03082">Willsey et al., “egg: Fast and extensible equality saturation,” POPL 2021</a> (e-graphs good!).</p> <h3 id="why-egg-changed-everything">Why egg Changed Everything</h3> <p>Before egg: “I’ll spend 6 months implementing an e-graph for my specific problem” After egg: “I’ll have a working optimizer by lunch”</p> <p><strong>The Secret Sauce:</strong></p> <ol> <li> <p><strong>Rebuilding Magic:</strong> Instead of constantly maintaining the e-graph (expensive!), egg says “let’s batch everything and fix it periodically.” It’s like cleaning your room once a week instead of after every sock.</p> </li> <li><strong>E-class Analysis:</strong> This is the killer feature. You can attach arbitrary data to e-classes. Is this expression a constant? What’s its type? Does it have side effects? Now your rewrites can be smart: <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Only optimize division if we know it's not divide-by-zero!</span>
<span class="nd">rewrite!</span><span class="p">(</span><span class="s">"div-safe"</span><span class="p">;</span> <span class="s">"(/ ?x ?y)"</span> <span class="k">=&gt;</span> <span class="s">"?x"</span> <span class="k">if</span> <span class="nf">is_one</span><span class="p">(</span><span class="o">?</span><span class="n">y</span><span class="p">))</span>
</code></pre></div> </div> </li> <li><strong>It’s FAST:</strong> 20× faster than previous implementations. That’s the difference between waiting minutes and waiting days.</li> </ol> <h3 id="e-class-analysis-making-your-e-graph-smart">E-class Analysis: Making Your E-graph Smart</h3> <p>Remember how I said e-graphs keep all equivalent expressions? Well, what if each group could also remember facts about itself? That’s e-class analysis.</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// "Hey e-graph, remember that this expression is always positive"</span>
<span class="c1">// "This one is a constant with value 42"</span>
<span class="c1">// "This expression has no side effects"</span>
</code></pre></div></div> <p><strong>Real Examples:</strong></p> <p><strong>Constant Folding:</strong></p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">analysis</span><span class="p">:</span> <span class="n">ConstantFold</span>
<span class="n">e</span><span class="o">-</span><span class="n">class</span><span class="p">:</span> <span class="p">{</span><span class="mi">2</span><span class="o">+</span><span class="mi">2</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">2</span><span class="o">*</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="o">&lt;&lt;</span><span class="mi">1</span><span class="p">}</span>
<span class="n">analysis</span> <span class="n">says</span><span class="p">:</span> <span class="s">"BTW, this whole e-class equals 4"</span>
</code></pre></div></div> <p><strong>Range Analysis:</strong></p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">analysis</span><span class="p">:</span> <span class="n">IntervalAnalysis</span>  
<span class="n">e</span><span class="o">-</span><span class="n">class</span><span class="p">:</span> <span class="p">{</span><span class="n">x</span><span class="o">*</span><span class="n">x</span><span class="p">,</span> <span class="nf">pow</span><span class="p">(</span><span class="n">x</span><span class="p">,</span><span class="mi">2</span><span class="p">)}</span>
<span class="n">analysis</span> <span class="n">says</span><span class="p">:</span> <span class="s">"This is always ≥ 0"</span>
<span class="c1">// Now you can optimize sqrt(x*x) → abs(x)!</span>
</code></pre></div></div> <p>It’s like each e-class has a personal assistant keeping track of important facts.</p> <h3 id="conditional-rewrites-smart-optimizations">Conditional Rewrites: Smart Optimizations</h3> <p>With e-class analysis, your rewrites can be conditional. It’s like having optimization rules that say “only do this if you’re sure it’s safe.”</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Old way: "Always optimize x/y to something"</span>
<span class="c1">// New way: "Only optimize x/y if y isn't zero"</span>

<span class="nd">rewrite!</span><span class="p">(</span><span class="s">"safe-div"</span><span class="p">;</span> <span class="s">"(/ ?x ?y)"</span> <span class="k">=&gt;</span> <span class="s">"?x"</span> 
  <span class="k">if</span> <span class="nf">is_one</span><span class="p">(</span><span class="o">?</span><span class="n">y</span><span class="p">));</span>  <span class="c1">// Only if y = 1</span>

<span class="nd">rewrite!</span><span class="p">(</span><span class="s">"pythagoras"</span><span class="p">;</span> <span class="s">"(sqrt (+ (* ?x ?x) (* ?y ?y)))"</span> <span class="k">=&gt;</span> <span class="s">"(abs ?z)"</span>
  <span class="k">if</span> <span class="nf">is_complex</span><span class="p">(</span><span class="o">?</span><span class="n">z</span><span class="p">)</span> <span class="o">&amp;&amp;</span> <span class="o">?</span><span class="n">z</span> <span class="o">=</span> <span class="o">?</span><span class="n">x</span> <span class="o">+</span> <span class="o">?</span><span class="n">y</span><span class="o">*</span><span class="n">i</span><span class="p">);</span>  <span class="c1">// Complex number optimization!</span>
</code></pre></div></div> <p><strong>My Favorite Examples:</strong></p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Strength reduction, but only for powers of 2</span>
<span class="nd">rewrite!</span><span class="p">(</span><span class="s">"mul-to-shift"</span><span class="p">;</span> <span class="s">"(* ?x ?c)"</span> <span class="k">=&gt;</span> <span class="s">"(&lt;&lt; ?x ?n)"</span>
  <span class="k">if</span> <span class="nf">is_power_of_two</span><span class="p">(</span><span class="o">?</span><span class="n">c</span><span class="p">)</span> <span class="o">&amp;&amp;</span> <span class="o">?</span><span class="n">n</span> <span class="o">=</span> <span class="nf">log2</span><span class="p">(</span><span class="o">?</span><span class="n">c</span><span class="p">));</span>

<span class="c1">// Remove assertions that we can prove are true</span>
<span class="nd">rewrite!</span><span class="p">(</span><span class="s">"remove-true-assert"</span><span class="p">;</span> <span class="s">"(assert ?cond ?expr)"</span> <span class="k">=&gt;</span> <span class="s">"?expr"</span>
  <span class="k">if</span> <span class="nf">can_prove</span><span class="p">(</span><span class="o">?</span><span class="n">cond</span><span class="p">));</span>
</code></pre></div></div> <p>It’s optimization with a brain!</p> <h2 id="quick-comparison-traditional-vs-e-graphs">Quick Comparison: Traditional vs E-graphs</h2> <p>Let me break it down for you:</p> <table> <thead> <tr> <th>Feature</th> <th>Traditional Term Rewriting</th> <th>E-graphs + Equality Saturation</th> </tr> </thead> <tbody> <tr> <td><strong>Core Idea</strong></td> <td>Apply one rule at a time, destroy the original</td> <td>Keep everything, explore all paths</td> </tr> <tr> <td><strong>Analogy</strong></td> <td>Following a strict recipe</td> <td>Detective’s corkboard with red string</td> </tr> <tr> <td><strong>Phase Ordering</strong></td> <td>“Pick the right order or suffer”</td> <td>“What’s phase ordering?” 😎</td> </tr> <tr> <td><strong>Optimization Quality</strong></td> <td>Depends on rule order</td> <td>Finds optimal (given enough time)</td> </tr> <tr> <td><strong>Speed</strong></td> <td>Fast per rule</td> <td>Slower but does everything at once</td> </tr> <tr> <td><strong>Memory Usage</strong></td> <td>Minimal</td> <td>“RAM is cheap, right?”</td> </tr> <tr> <td><strong>When to Use</strong></td> <td>Simple, local optimizations</td> <td>Complex, whole-program optimization</td> </tr> <tr> <td><strong>Main Weakness</strong></td> <td>Gets stuck in local optima</td> <td>Extraction is NP-hard</td> </tr> </tbody> </table> <h2 id="real-world-success-stories">Real-World Success Stories</h2> <h3 id="herbie-making-floating-point-math-not-suck">Herbie: Making Floating-Point Math Not Suck</h3> <p>Floating-point arithmetic is where good programmers go to cry. You write <code class="language-plaintext highlighter-rouge">sqrt(x+1) - sqrt(x)</code> and for large x, you get… 0? Thanks, floating point! Enter Herbie <a href="https://herbie.uwplse.org/pldi15-paper.pdf">Panchekha et al., “Automatically improving accuracy for floating point expressions,” PLDI 2015</a>.</p> <p><strong>The Herbie Magic:</strong> Herbie uses egg to find mathematically equivalent expressions that don’t suffer from catastrophic cancellation:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Original: sqrt(x+1) - sqrt(x)
Problem: When x is large, both sqrts are nearly equal → precision loss

Herbie finds: 1/(sqrt(x+1) + sqrt(x))
Why better: No subtraction of nearly-equal numbers!
</code></pre></div></div> <p><strong>The Punchline:</strong></p> <ul> <li>3000× faster than the old Herbie (not a typo!)</li> <li>Accuracy improved from 53% to 99.7%</li> <li>Turns numerical analysis PhD problems into “push button, get answer”</li> </ul> <p>I’ve used Herbie on real code. It’s like having a numerical analysis expert on speed dial.</p> <h3 id="hardware-design-making-chips-cheaper">Hardware Design: Making Chips Cheaper</h3> <p><strong>The Problem:</strong> You’re designing a chip. Every gate costs money. Every nanosecond of delay costs performance.</p> <p><strong>E-graphs to the Rescue:</strong></p> <div class="language-verilog highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Original hardware:</span>
<span class="n">result</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">==</span> <span class="n">y</span><span class="p">)</span> <span class="o">?</span> <span class="n">x</span> <span class="o">+</span> <span class="n">y</span> <span class="o">:</span> <span class="mi">0</span>

<span class="c1">// E-graph finds:</span>
<span class="n">result</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">==</span> <span class="n">y</span><span class="p">)</span> <span class="o">?</span> <span class="mi">2</span><span class="o">*</span><span class="n">y</span> <span class="o">:</span> <span class="mi">0</span>  <span class="c1">// One less adder input!</span>
</code></pre></div></div> <p><strong>Real Example from E-Syn:</strong> They used egg + machine learning to optimize hardware designs:</p> <ol> <li>E-graph explores equivalent circuits</li> <li>ML model predicts area/delay without synthesis</li> <li>Pick the best one</li> </ol> <p>Result? 30% smaller circuits with same functionality. That’s millions of dollars saved in chip production!</p> <h3 id="compiler-magic-cranelift-and-friends">Compiler Magic: Cranelift and Friends</h3> <p><strong>Cranelift ægraphs:</strong> WebAssembly’s compiler using e-graphs:</p> <ul> <li>Problem: How do you apply e-graphs when you have control flow?</li> <li>Solution: “Let’s just e-graph the straight-line code and keep the control flow separate”</li> <li>Result: Fast compilation with good optimization</li> </ul> <p><strong>Diospyros</strong> <a href="https://dl.acm.org/doi/10.1145/3445814.3446707">VanHattum et al., “Vectorization for digital signal processors via equality saturation,” ASPLOS 2021</a>: Vectorization on steroids:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// Original loop:
for i in 0..n:
    c[i] = sqrt(a[i]) + b[i]

// Diospyros finds the exact vector instructions:
vec_sqrt_add(a, b)  // One instruction instead of n*2!
</code></pre></div></div> <p><strong>The Catch:</strong> E-graphs can explode for large programs. Optimizing 8×8×8 matrix multiply? Hope you have RAM to spare!</p> <p><strong>Isaria’s Innovation:</strong> “What if we automatically learned rewrite rules from the instruction manual?”</p> <ul> <li>Feed it ISA documentation</li> <li>It generates rewrite rules automatically</li> <li>Your compiler understands new instructions without manual work</li> </ul> <h2 id="egglog-the-next-generation-">Egglog: The Next Generation 🤯</h2> <h3 id="what-if-e-graphs-could-think">What if E-graphs Could Think?</h3> <p>Egglog <a href="https://arxiv.org/abs/2304.04332">Zhang et al., “Better Together: Unifying Datalog and Equality Saturation,” PLDI 2023</a> is what happens when someone looks at e-graphs and says “this is cool, but what if we added Prolog?” It’s e-graphs meets logic programming, and it’s wild.</p> <p><strong>The Big Idea:</strong> Instead of just tracking equivalences, what if we could reason about relationships?</p> <div class="language-prolog highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">//</span> <span class="nv">Traditional</span> <span class="ss">e</span><span class="o">-</span><span class="ss">graph</span><span class="o">:</span> <span class="s2">"These expressions are equal"</span>
<span class="o">//</span> <span class="nv">Egglog</span><span class="o">:</span> <span class="s2">"These expressions are equal AND here's why AND here's what else we can deduce"</span>
</code></pre></div></div> <p><strong>Example That Blew My Mind:</strong></p> <pre><code class="language-datalog">(datatype Math
  (Num i64)
  (Add Math Math)
  (Mul Math Math))

// Normal rewrites
(rewrite (Add (Num 0) x) x)
(rewrite (Mul (Num 1) x) x)

// But also relations!
(relation is-positive (Math))
(rule ((= x (Mul y y))) ((is-positive x)))  // Squares are positive

// Now optimizations can use reasoning
(rewrite (sqrt (Mul x x)) x 
  if (is-positive x))  // Safe because we proved it!
</code></pre> <h3 id="egglog-superpowers">Egglog Superpowers</h3> <p><strong>Program Analysis on Steroids:</strong></p> <pre><code class="language-datalog">// "Which basic blocks can reach the exit?"
(relation reaches-exit (BasicBlock))
(rule ((is-exit ?block)) ((reaches-exit ?block)))
(rule ((edge ?from ?to) (reaches-exit ?to)) 
      ((reaches-exit ?from)))

// Now optimize: "Remove code that can't reach exit!"
</code></pre> <p><strong>Incremental Everything:</strong></p> <ul> <li>Your program changed? Egglog updates just what’s needed</li> <li>No more “rebuild the world” after every edit</li> <li>It’s like Git for your optimizer’s brain</li> </ul> <p><strong>The Killer App I’m Waiting For:</strong> Security analysis + optimization in one pass. “This is equivalent AND secure AND fast.”</p> <h2 id="the-future-where-this-is-all-going">The Future: Where This is All Going</h2> <h3 id="the-scalability-wall">The Scalability Wall</h3> <p>Current challenge: E-graphs get HUGE for big programs. Like, “eat all your RAM and ask for seconds” huge.</p> <h3 id="ml--e-graphs--️">ML + E-graphs = ❤️</h3> <p>The hot new thing: Teaching e-graphs to be smarter with machine learning.</p> <p><strong>Learning What’s Important:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Old way: Try everything!
# New way: ML says "try this first, it usually works"
</span><span class="n">model</span><span class="p">.</span><span class="nf">predict_best_rewrite</span><span class="p">(</span><span class="n">expression</span><span class="p">)</span>
</code></pre></div></div> <p>There are some works on EGRAPH 2025 for this topic.</p> <h3 id="the-theory-gap">The Theory Gap</h3> <p>We’re using e-graphs everywhere, but the theory is playing catch-up:</p> <ul> <li>When exactly does equality saturation terminate?</li> <li>Can we prove extraction finds the optimal solution?</li> <li>What are the complexity bounds?</li> </ul> <p>It’s like we invented cars before understanding thermodynamics. It works, but we’re not always sure why!</p> <h2 id="wrapping-up-why-this-matters">Wrapping Up: Why This Matters</h2> <h3 id="the-journey-so-far">The Journey So Far</h3> <p>We’ve come a long way from “apply rules and hope for the best”:</p> <ol> <li><strong>Term Rewriting</strong>: The old way. One rule at a time, fingers crossed.</li> <li><strong>E-graphs</strong>: Keep everything! Build a map of all possibilities.</li> <li><strong>Equality Saturation</strong>: Explore everything! Find the best path.</li> <li><strong>egg</strong>: Make it fast and usable for mortals.</li> <li><strong>Egglog</strong>: Add reasoning and logic to the mix.</li> </ol> <h3 id="why-you-should-care">Why You Should Care</h3> <p>E-graphs aren’t just another optimization technique. They’re a fundamentally different way of thinking about program transformation:</p> <p><strong>Before</strong>: “What’s the right order to apply optimizations?” <strong>After</strong>: “What if we didn’t have to choose?”</p> <p>It’s like the difference between taking one photo and hoping it’s good vs. taking 1000 photos and picking the best one. Except the camera (egg) is really fast now.</p> <h3 id="my-take">My Take</h3> <p>After working with e-graphs, going back to traditional optimization feels like coding with one hand tied behind my back. Yes, extraction is NP-hard. Yes, e-graphs can explode in size. But the ability to explore the entire optimization space and <em>know</em> you’re not missing opportunities? That’s powerful.</p> <h3 id="the-bottom-line">The Bottom Line</h3> <p>E-graphs and equality saturation represent a paradigm shift in how we think about optimization. By keeping all possibilities alive and exploring them simultaneously, we’ve opened doors that were previously locked by the phase ordering problem.</p> <p>Is it perfect? No. Is it the future? I think so.</p>]]></content><author><name></name></author><category term="survey"/><category term="term-rewriting"/><category term="e-graphs"/><category term="optimization"/><summary type="html"><![CDATA[How e-graphs turn the optimizer's dilemma into a solved problem by keeping all the good ideas at once, featuring real examples, bad jokes, and the magic of egg]]></summary></entry></feed>